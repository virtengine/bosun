incident

# Bosun Local Ops Monitor — 2026-04-25 08:29:14 local

## Startup / scope
- Read the latest three monitor notes before touching runtime state:
  - `.bosun-monitor/session-20260425-075850.md`
  - `.bosun-monitor/session-20260425-072106.md`
  - `.bosun-monitor/session-20260425-063312.md`
- Rebuilt the blocker list from `session-20260425-075850.md`.
- Kept the daemon locked/off and stayed simulator-first.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Current open-blocker list
1. **Simulator gate still blocked** — no real non-monitor task has yet completed validation -> push -> PR -> review/fix -> merge.
2. **Task-row canonical branch linkage is now fixed and live-proven** — explicit branch metadata now stays primary in `prLinkage` instead of falling back to the oldest stale linkage entry.
3. **Task metadata persistence remains fixed and live-proven** — the raw task row still persists canonical `baseBranch`, `branchName`, and `worktreePath`.
4. **New narrowest blocker is now branch-ref contamination, not task-row linkage drift** — a fresh worktree is still created from an already-contaminated existing task branch ref.
5. **Throughput remains incident-level** — still no non-monitor merge landed.

## Durable source fix landed this cycle
1. **Promote explicit branch metadata ahead of stale historical PR linkage**
   - File:
     - `kanban/kanban-adapter.mjs`
   - Change:
     - added `buildPrLinkageKey(...)` and `promotePrimaryPrLinkage(...)`
     - `buildPrLinkagePatch(...)` now keeps the freshest explicit linkage first
     - internal `updateTask(...)` now promotes explicit `branchName` / PR linkage data to the front of `prLinkage` and `meta.prLinkage`
   - Why:
     - the raw task row still held two linkage records for the same task, and the old diagnostics branch remained entry `0`
     - later base-only/status-only task updates would re-promote that stale first linkage back into `branchName`

## Validation completed this cycle
- `npx vitest run tests\kanban-adapter.test.mjs -t "promotes explicit branch metadata ahead of stale historical PR linkage|does not resurrect stale branch linkage on status updates without new PR linkage data|preserves canonical PR linkage fields when reload data only carries meta linkage"` passed.
- `npm run build` passed.
- `npm test` did not produce a clean full-suite result in this environment during this cycle:
  - the pretest chain hung/truncated while running through `demo-defaults:sync`
  - standalone `npm run demo-defaults:sync` succeeded immediately afterward
  - no failing Bosun test signal tied to this source fix was isolated from that run

## Fresh simulator evidence
- Restarted the same real simulator task on the latest patch set:
  - `bosun simulate task restart --json --config-dir .bosun --repo-root .`
  - parent run: `cb162c4f-a3fa-450f-a74c-faf3343f0f1d`
- The rerun completed with:
  - `_active-runs.json = []`
  - task status back to `blocked`
  - no PR created
  - no review/fix/re-review
  - no merge
- Fresh raw task-store proof after the rerun:
  - `status = "blocked"`
  - `baseBranch = "bosun/codex-self-improvement-loop-commits"`
  - `branchName = "task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f"`
  - `prLinkage[0].branchName = "task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f"`
  - stale linkage remains only as the secondary entry:
    - `prLinkage[1].branchName = "task/TASKDIAGLOCA-diagnostics-export"`
- Fresh worktree creation proof from the simulator transcript:
  - `action.acquire_worktree` reported `created = true`
  - canonical branch/base used:
    - `branch = "task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f"`
    - `baseBranch = "bosun/codex-self-improvement-loop-commits"`
- Fresh contamination proof after that newly created worktree was released:
  - released worktree file:
    - `.bosun\worktrees\task-f82209dfe236-666ab4d38c\shell\codex-sdk-import.mjs`
    - still contains:
      - `export { Codex } from "@openai/codex-sdk";`
  - current local base branch file:
    - `shell\codex-sdk-import.mjs`
    - still contains the dynamic-import version
  - branch ref comparison at repo root:
    - `git show task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f:shell/codex-sdk-import.mjs`
      - stale named-export version
    - `git show bosun/codex-self-improvement-loop-commits:shell/codex-sdk-import.mjs`
      - dynamic-import version
    - `git diff --name-only bosun/codex-self-improvement-loop-commits..task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
      - count: `688`
- Ancillary cleanup evidence:
  - `release-worktree` completed
  - `sweep-task-wts` still reported an `EPERM` on the released directory path
  - but this is **not** the primary blocker for this cycle because the new worktree was created fresh before the stale file appeared again

## What the simulator proved this cycle
### 1. The stale branch-linkage reversion is really fixed
- Before this cycle, the raw task row still promoted:
  - `task/TASKDIAGLOCA-diagnostics-export`
  - back into `branchName`
- After rerun `cb162c4f-a3fa-450f-a74c-faf3343f0f1d`, the raw task row now keeps:
  - `branchName = task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
  - and the canonical branch is entry `0` in both `prLinkage` and `meta.prLinkage`

### 2. The next blocker is no longer task-row state; it is the local task branch ref itself
- The latest rerun did **not** reuse the old worktree as the source of truth.
- It created a fresh task worktree on the canonical branch and canonical base branch.
- That fresh worktree still materialized the stale `shell\codex-sdk-import.mjs` file.
- Therefore the contamination source is the existing local branch ref:
  - `task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
  - not the previously stale task-row linkage metadata

### 3. Real task path still inspected end to end up to the current blocker
- Task:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
- Latest parent run:
  - `cb162c4f-a3fa-450f-a74c-faf3343f0f1d`
- Observed path this cycle:
  - claim acquired
  - worktree created
  - implementation/validation lifecycle ran
  - task returned to `blocked`
  - worktree released
  - no push
  - no PR creation
  - no review/fix/re-review
  - no merge

## Runtime state
- Simulator gate: **blocked**
- Daemon allowed this cycle: **no**
- Active run at handoff:
  - none
- Latest completed run:
  - `cb162c4f-a3fa-450f-a74c-faf3343f0f1d`
- Active sink paths:
  - simulator path only
  - daemon paths intentionally still locked/off
- Real workflows/tasks progressing:
  - yes through worktree creation and task execution
  - no through PR/review/merge

## Throughput
- Throughput target **not met**.
- Still no non-monitor merge.
- Repeated simulator cycles on the same real task without reaching PR creation remain **incident** severity.

## Git / source state
- Branch:
  - `bosun/codex-self-improvement-loop-commits`
- `package.json` version:
  - `0.43.0`
- Relevant monitor-related diffs touched this cycle:
  - `kanban/kanban-adapter.mjs`
  - `tests/kanban-adapter.test.mjs`
- PR / CI status:
  - no PR created
  - no CI triggered

## Narrowest unresolved blocker now
### Symptom
- Even after task-row canonical branch linkage is fixed, the real simulator task still blocks before PR creation.
- A freshly created task worktree still contains the stale named-export `shell\codex-sdk-import.mjs`.

### Proof
- Latest simulator run:
  - `.bosun\workflow-runs\cb162c4f-a3fa-450f-a74c-faf3343f0f1d.json`
- Latest simulator task cache:
  - `.bosun\.cache\task-simulator-last-run.json`
- Raw task-store proof after the rerun:
  - canonical `branchName` and `prLinkage[0]` now stay on the task branch
- Fresh worktree creation proof:
  - simulator transcript recorded `created = true` for `action.acquire_worktree`
- Branch contamination proof:
  - `git show task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f:shell/codex-sdk-import.mjs`
    - stale named-export file
  - `git show bosun/codex-self-improvement-loop-commits:shell/codex-sdk-import.mjs`
    - dynamic-import file
  - `git diff --name-only bosun/codex-self-improvement-loop-commits..task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
    - `688` changed files

### Code path / blocker
- `workflow/workflow-nodes/actions.mjs`
  - `action.acquire_worktree`
  - current fresh-create path:
    - if the local task branch already exists, Bosun runs:
      - `git worktree add <worktreePath> <branch>`
    - only the non-existent-branch path uses:
      - `git worktree add <worktreePath> -b <branch> <baseBranch>`
- That means the latest rerun can create a **fresh directory** while still attaching it to an already-contaminated local task branch ref.
- The next decision is therefore a task-branch hygiene policy question:
  - when should Bosun discard/recreate an existing Bosun-owned task branch from the current base branch
  - versus preserving existing task commits on that branch

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first and keep the daemon locked/off.
3. Resume from:
   - latest completed run `.bosun\workflow-runs\cb162c4f-a3fa-450f-a74c-faf3343f0f1d.json`
4. Continue from the new narrow blocker:
   - existing contaminated local task branch ref, not stale task-row linkage
5. Trace the narrowest safe source fix in `action.acquire_worktree` / task-branch hygiene:
   - determine when an existing Bosun-owned task branch with no PR should be recreated from `baseBranch`
   - or otherwise reset/repair before `git worktree add <path> <branch>` attaches a fresh worktree to it
6. Keep the daemon locked/off until a real task completes validation -> push -> PR -> review/fix -> merge end to end.

incident
