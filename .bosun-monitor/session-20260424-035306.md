status: incident

# Bosun Local Ops Monitor — 2026-04-24 03:53:06

## What changed this cycle
- Landed a durable Copilot fallback fix for the pool launcher path.
  - `shell/copilot-shell.mjs`
    - extracted `importCopilotSdkModuleWithCompat()` from the existing loader logic so the `vscode-jsonrpc/node` extensionless shim and retry path can be reused outside the interactive shell codepath
  - `agent/agent-launcher.mjs`
    - `importCopilotSdkModule()` now routes through that shared compat loader instead of raw-importing `@github/copilot-sdk`
  - `tests/copilot-shell.test.mjs`
    - added a focused regression proving the compat loader applies the shim and retries the SDK import
    - normalized the path assertion so it passes on Windows

## Validation
- Passed: `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/copilot-shell.test.mjs tests/agent-pool.test.mjs`
  - result: `3 test files`, `100 tests passed`
- Passed: `npm run build`
- Passed direct runtime probe:
  - `importCopilotSdkModuleWithCompat()` loaded a real `CopilotClient`
  - the helper applied the compatibility shim and created:
    - `node_modules\vscode-jsonrpc\node`

## Runtime health / throughput re-check
- Restarted Bosun onto the new source.
  - current daemon PID: `29324`
  - child monitor PID: `15668`
- `node cli.mjs task stats --json --config-dir .bosun --repo-root .`
  - `draft=45`
  - `todo=11`
  - `inprogress=23`
  - `inreview=0`
  - `done=104`
  - `blocked=35`
- Recent non-monitor throughput is still below target.
  - `gh pr list --state merged --search "base:main merged:>=<last hour>" --json ...` returned `[]`
  - last-hour merged non-monitor PR count: `0`
- There are currently no active-run matches for the target sibling tasks in `_active-runs.json`.
  - this is no longer a live-session hang
  - it is now a post-run validation / PR-handoff stall

## Required end-to-end real task path inspection
- Inspected the full live task path for:
  - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - title: `[s] feat(observability): emit structured logs and metrics for planner-to-task handoff`
- Current task record:
  - status `inprogress`
  - branch `task/97d41516caca-s-feat-observability-emit-structured-logs-and-me`
  - `prUrl = null`
- Authoritative run-family outcome from `.bosun\workflow-runs\index.json`:
  - `40da7eec-fa0f-489a-b654-04692192b76b` — Backend Agent — `completed`
  - later descendants `db1a1703-...`, `4294c02f-...`, and `86d60fb4-...` — Backend Agent — `paused`, `resumeResult=duplicate_task_run`
  - lifecycle parents `9484e481-...`, `59c5c966-...`, and `6215f600-...` — `paused`, `resumeResult=duplicate_task_run`
- Backend completion detail for `40da7eec-...`:
  - `issueAdvisor.status = "completed"`
  - `issueAdvisor.summary = "Workflow completed successfully."`
  - but the PR path did not run:
    - `allPassed.result = false`
    - `pushBranch = null`
    - `createPr = null`
    - `notifyPrFailed = null`
  - `setValidationSummary` shows:
    - `implement.success: false`
    - `build.passed: false`
    - `test-final.passed: false`
    - `lint.passed: true`
  - the same summary contains build failure proof:
    - `Failed to acquire local-process runner lease after 1 attempt(s): Runner process spawn failed: spawn npm ENOENT`

## What the runtime proved
- The old planner-stage stall is no longer the leading blocker.
  - the sibling family moved through planner/tests/implement in prior runs
  - the next visible gate is validation/PR handoff
- The newly-landed Copilot compat fix is real and locally runnable.
  - the shared loader now applies the `vscode-jsonrpc` shim successfully
  - focused tests for the loader and pool path pass
- However, throughput is still incident-level because successful PR handoff is still not happening.

## Narrowest unresolved blocker
### Symptom
Real backend task runs are skipping PR handoff because validation fails before `push-branch` / `create-pr`, and at least one of those validation failures is a Bosun-side Windows command-runner problem: `spawn npm ENOENT`.

### Proof
- Real task path inspected: `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
- Completed backend run: `40da7eec-fa0f-489a-b654-04692192b76b`
- Backend outputs show:
  - `allPassed.result = false`
  - `pushBranch = null`
  - `createPr = null`
- Validation summary inside that run contains:
  - `build.passed: false`
  - `test-final.passed: false`
  - build error:
    - `Failed to acquire local-process runner lease after 1 attempt(s): Runner process spawn failed: spawn npm ENOENT`
- Daemon logs in the same phase repeatedly show:
  - `node:SKIPPED create-pr`
  - `node:start set-validation-summary`
  - `node:start auto-fix`
- Last-hour merged non-monitor PR count remains `0`

### Code path
- Workflow node registry:
  - `validation.build`
  - `validation.tests`
  - both call `executeValidationCommand(...)`
- Shared command launch layer:
  - child-process spawn / local-process runner lease path used by validation commands
- Related already-fixed but older failure path:
  - `agent/agent-launcher.mjs` Copilot fallback import now routes through `shell/copilot-shell.mjs` compat loader

## Next start point
1. Start from the validation failure in completed backend run `40da7eec-...`, not the already-fixed planner import path.
2. Trace `validation.build` / `validation.tests` into the local-process runner / spawn path and prove why Windows worktrees are surfacing `spawn npm ENOENT`.
3. Re-check whether a fresh retry after that fix allows:
   - `allPassed.result = true`
   - `push-branch`
   - `create-pr`
4. Keep the Copilot fallback fix in place; do not reopen it unless `vscode-jsonrpc/node` import failures reappear after the shared compat loader path is exercised again.

incident
