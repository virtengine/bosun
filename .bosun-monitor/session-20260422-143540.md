incident

## Runtime state
- Branch: `bosun/codex-self-improvement-loop-commits`
- Package version: `0.43.0`
- After `git fetch origin main`, `git rev-list --left-right --count HEAD...origin/main` returned `190 0` (local branch ahead 190, behind 0 vs refreshed `origin/main`)
- Daemon restarted twice during this cycle; current PID is `83664`
- Active sinks advancing under the patched daemon: `logs/daemon.log`, `logs/session-snapshots.json`, `.bosun/workflow-runs/index.json`
- Latest startup workflow runs under the patched daemon:
  - `Task Batch Processor` run `7cb70519-6ffd-4250-b695-eac2f1ff3bb5`
  - `Bosun PR Watchdog` run `4225e4ba-d9f9-4070-be3d-603b2c7e43fb`
- `_active-runs.json` was empty immediately after the second restart
- Startup log still emits `execution ledger write failed: Cannot create a string longer than 0x1fffffe8 characters`; not root-caused this cycle

## Throughput
- Target not met
- `git log origin/main --merges --since="2 hours ago"` produced no output, so no recent merge throughput was visible on refreshed `origin/main`
- Real workflows are running, but no real task throughput is happening:
  - `Task Batch Processor` startup run `7cb70519-6ffd-4250-b695-eac2f1ff3bb5` completed `0/0`
  - `Bosun PR Watchdog` startup run `4225e4ba-d9f9-4070-be3d-603b2c7e43fb` completed with `review-needed=false`

## End-to-end task path inspected
- Representative task: `a6a0198e-a547-4eb7-872c-af2c83604b5f`
- Post-restart task detail (`bosun task get`):
  - `status=inprogress`
  - `blockedReason="Repeated no-output agent starts detected; stopping retries"`
  - `cooldownUntil=2026-04-22T04:00:13Z`
  - `consecutiveNoCommits=0`
- Managed worktree still exists: `.bosun/worktrees/task-a6a0198ea547-f6fd5d131e`
- Task detail topology remains blank for `latestRunId`, `workflowName`, and `sessionId`
- Before the trigger fix, active run `1d50a5e6-bdde-4b0d-9b9c-eff61ac9eccc` showed `nodeOutputs.trigger.task` already truncated to a minimal object during `reason="direct_task"`

## Symptom -> proof -> code path -> fix
- Symptom: direct-task resume runs dropped persisted task fields such as `consecutiveNoCommits`, `cooldownUntil`, and `blockedReason` from `nodeOutputs.trigger.task`
- Proof:
  - Pre-fix active run artifact `.bosun/workflow-runs/1d50a5e6-bdde-4b0d-9b9c-eff61ac9eccc.json` showed a minimal `trigger.task`
  - The representative task detail still contained `cooldownUntil` and `blockedReason`, so truncation happened before run-detail serialization
  - Focused regression reproduced the carried-minimal-snapshot path
- Code path:
  - `workflow/workflow-nodes/triggers.mjs`
  - `resolveExplicitTaskContext()` returned matching `ctx.data.task` / `taskDetail` candidates immediately, before consulting `kanban.getTask()` or `taskStore.getTask()`
  - `trigger.task_available` direct-task branch returned that minimal object unchanged
- Fix applied:
  - `resolveExplicitTaskContext()` now hydrates explicit-task context from `kanban.getTask()` / `taskStore.getTask()` first and merges carried candidate fields only after successful lookup
  - Added regression `rehydrates explicit task context when the carried snapshot is minimal` in `tests/workflow-task-lifecycle.test.mjs`

## Validation
- Focused regression: `npx vitest run tests/workflow-task-lifecycle.test.mjs -t "explicit task context|minimal"` -> pass
- Build: `npm run build` -> pass
- File diagnostics: no errors in `workflow/workflow-nodes/triggers.mjs` or `tests/workflow-task-lifecycle.test.mjs`
- Earlier in this cycle, before the narrower trigger fix:
  - Focused kanban anti-thrash regressions -> pass
  - `npm test` -> pass
  - `npm run build` -> pass
  - `npm run prepush:check` -> pass

## Remaining blocker
- Symptom: after the patched daemon restart, `_active-runs.json` is empty while real tasks `a6a0198e-a547-4eb7-872c-af2c83604b5f` and `8ed214ec-7eac-4887-8abd-97aa09c61694` remain `inprogress` with `blockedReason` + `cooldownUntil`, and `Task Batch Processor` still dispatches `0/0`
- Proof:
  - `bosun task list --status inprogress` still shows both tasks as `inprogress`
  - `_active-runs.json` after the second restart was `[]`
  - Managed worktrees still exist for both tasks under `.bosun/worktrees/`
  - Latest startup batch run `7cb70519-6ffd-4250-b695-eac2f1ff3bb5` saw no dispatchable real todo tasks; the only todo tasks are old diagnostics fixtures
- Likely code path:
  - `workflow/workflow-engine.mjs`
    - `_detectInterruptedRuns()`
    - `resumeInterruptedRuns()`
    - `_shouldResumeTaskScopedRunFromScratch()` / retry decision around `task_claim_missing_on_resume`
  - `task/task-executor.mjs`
    - in-progress recovery branch around `resumable.push(...)`, `transitionTaskStatus(..., "todo")`, and `skipped ... no-commit blocked task(s)` logging

## Narrowest next starting point
- Start at `workflow/workflow-engine.mjs` around the `task_claim_missing_on_resume` retry decision for startup-interrupted runs.
- Use representative task `a6a0198e-a547-4eb7-872c-af2c83604b5f` plus its latest task-lifecycle run lineage to determine whether startup recovery is marking the run unresumable, retrying from scratch without rebuilding `_active-runs.json`, or dropping the task before either resume or reset-to-todo happens.