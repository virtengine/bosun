# Bosun local ops monitor handoff — 20260429-092553

Status: incident

## Startup sequence completed
- Re-read required notes before runtime work:
  - `session-20260429-090525.md`
  - `session-20260429-084433.md`
  - `session-20260429-082621.md`
- Rebuilt the blocker list from `session-20260429-090525.md`.
- Confirmed branch/status/version/npm link before new runtime work:
  - branch: `bosun/codex-self-improvement-loop-commits`
  - package version: `0.43.0`
  - global `bosun` junction still points to `D:\source\repos\virtengine-gh\bosun`
- Stayed simulator-first.
- Kept the daemon gate closed.

## Open blockers
1. **Simulator gate still blocked**
   - no real non-monitor task has completed implementation -> push -> PR -> review/fix -> merge -> local sync.
2. **Resume-lineage selection blocker is fixed**
   - `bosun simulate task resume` no longer retries the cached completed root run `02695bfc-f5b4-4424-a055-1ffbf695919a`.
   - live simulator replays now create:
     - `409779bf-14d6-478c-92d7-81f2b032736d`
     - `e623ab61-b397-453c-9dbb-12ceef79d52e`
   - both have:
     - `parentRunId = 3adb4f2b-401c-4565-b90e-36e68a66c4b4`
     - `retryOf = 3adb4f2b-401c-4565-b90e-36e68a66c4b4`
3. **Current narrowest blocker moved to retry-reset / claim-liveness**
   - even after choosing stale implement run `3adb4f2b-401c-4565-b90e-36e68a66c4b4`, retry still replays from `claim-task -> run-agent-plan` instead of resuming `run-agent-implement`.

## Symptom -> proof -> code path -> fix or blocker
1. **Simulator resume was blindly retrying the cached state-file run id**
   - **Proof:** `.bosun/.cache/task-simulator-last-run.json` still held root run `02695bfc-...`, and `bosun simulate task resume` was creating descendants of that root instead of the stale implement frontier.
   - **Code path:** `task/task-simulate-cli.mjs` `resolveResumeRunId()`.
   - **Fix:** resume now calls `engine.getRunHistory(...)` through the live engine binding, filters to the same task/workflow lineage, and selects the most advanced same-lineage candidate instead of blindly using the cached run id.

2. **Detached `getRunHistory()` call masked the real history on live engine instances**
   - **Proof:** targeted CLI regression passed with fake runtimes, but live engine still fell back to saved root run until the method was invoked with the engine binding.
   - **Code path:** same `task/task-simulate-cli.mjs`.
   - **Fix:** invoke `runtime.engine.getRunHistory(...)` directly rather than detaching the method reference.

3. **Task-lifecycle retry reset is still too early after correct lineage selection**
   - **Proof:**
     - stale source run `3adb4f2b-...` detail still shows:
       - `run-agent-plan = completed`
       - `run-agent-tests = completed`
       - `run-agent-implement = running`
     - corrected resume replay `e623ab61-...` has:
       - `retryOf = 3adb4f2b-...`
       - latest checkpoint back at `run-agent-plan -> agent.started`
     - `e623ab61-...` `dagState.revisions[0]` preserved only:
       - `trigger`
       - `check-slots`
       - `allocate-slot`
     - `focusNodeIds` restart from `claim-task` onward.
   - **Code path:** `workflow/workflow-engine.mjs`
     - `_resolveRetryResumeResetStartNodeId()`
     - task-claim liveness lookup via `_getLiveLocalTaskClaim()`
   - **Blocker:** workflow-engine still sees no live local claim for task `e274fdfc-65a8-44ff-8495-7fa6b1965896`, so retry reset continues to rebuild from `claim-task`.

4. **Local task-claim liveness is stale even while simulator startup logs a shared-state override**
   - **Proof:**
     - `.cache/bosun/task-claims.json` still points the task claim at dead PID `25748`
     - probing `engine._getLiveLocalTaskClaim('e274fdfc-65a8-44ff-8495-7fa6b1965896')` returns `null`
     - this matches the incorrect reset behavior above
   - **Code path / blocker:** task-claim refresh / ownership handoff around simulator retry startup and workflow-engine retry reset still unresolved.

## Evidence matrix
- **Durable source fixes**
  - `task/task-simulate-cli.mjs`
    - resume selects the most advanced same-lineage task run instead of blindly retrying the cached run id
    - live engine `getRunHistory()` binding fixed
  - `tests/task-simulate-cli.test.mjs`
    - regressions for:
      - newer same-lineage run selection
      - more-progressed same-lineage frontier selection
      - live engine binding for `getRunHistory()`
  - `workflow/workflow-engine.mjs`
    - retry reset now skips claim-task reset when a live local task claim is visible
  - `tests/workflow-engine.test.mjs`
    - regression for localized task-lifecycle retry when a live claim still exists

- **Deterministic tests**
  - Passed:
    - `node tools/vitest-runner.mjs run tests/task-simulate-cli.test.mjs -t "resume calls engine.retryRun with mode=from_failed using saved runId|resume forwards explicit --mode to engine.retryRun|resume prefers a newer same-lineage task run over the cached completed root run|resume prefers the most advanced same-lineage frontier over a newer but less progressed retry|resume calls getRunHistory with the live engine binding before choosing a newer lineage run"`
    - `node tools/vitest-runner.mjs run tests/workflow-engine.test.mjs -t "restarts task-lifecycle retries from claim-task when prior runtime state carried task resources|restarts task-lifecycle retries from claim-task when task resources only survive in node outputs|keeps task-lifecycle retry localized when a live local task claim still exists|keeps localized resume when interrupted run detail still carries a claim token"`

- **Runtime cleanup / one-off actions**
  - Stopped stale simulator PIDs:
    - `5316`
    - `25748`
    - `5836`
    - `29980`
    - `30256`
  - Restored `.bosun/.cache/task-simulator-last-run.json` after an in-process probe temporarily polluted it with `fake-retry`.

- **Generated / mirror syncs**
  - one-time local vitest hydration by the repo test runner

- **Uncommitted changes**
  - this cycle updated:
    - `task/task-simulate-cli.mjs`
    - `tests/task-simulate-cli.test.mjs`
    - `workflow/workflow-engine.mjs`
    - `tests/workflow-engine.test.mjs`
  - prior uncommitted repo dirt remains outside this fix set

- **Unresolved blockers**
  - retry reset still restarts from `claim-task` even after correct stale-run selection
  - local task-claim registry still advertises dead PID `25748`, so workflow-engine live-claim detection fails
  - simulator gate still not proven end to end

## Validation status
- Source validation passed for the resume-lineage fix and live-claim retry-localization guard.
- Live simulator validation proved:
  - resume-lineage selection is fixed
  - the next blocker is retry-reset / claim-liveness, not stale lineage choice
- Live simulator end-to-end validation remains blocked before `run-agent-implement` can be resumed, so PR/review/merge were not reached.

## Simulator gate / daemon status
- **Simulator gate:** blocked
- **Daemon:** still locked this cycle; not used as health proof

## Runtime state / throughput
- Active sink paths inspected:
  - `.bosun/workflow-runs/execution-ledger/3adb4f2b-401c-4565-b90e-36e68a66c4b4.json`
  - `.bosun/workflow-runs/execution-ledger/e623ab61-b397-453c-9dbb-12ceef79d52e.json`
  - `.cache/bosun/task-claims.json`
- Deep real-task inspection:
  - yes — task `e274fdfc-65a8-44ff-8495-7fa6b1965896`, stale run `3adb4f2b-...`, corrected retry descendants `409779bf-...` and `e623ab61-...`, run detail DAG revisions, active-runs index, and task-claim liveness path
- Throughput target:
  - not met
  - no PR
  - no merge

## Local source / merge status
- Branch remains `bosun/codex-self-improvement-loop-commits`.
- Global npm link still resolves to this checkout.
- Latest merged `origin/main` was **not** re-fetched/reconfirmed this cycle because simulator validation remained in-flight.

## Narrowest next starting point
1. Re-open from this note first.
2. Start with task-claim liveness for `e274fdfc-65a8-44ff-8495-7fa6b1965896`:
   - why `.cache/bosun/task-claims.json` still points at dead PID `25748`
   - why simulator/shared-state override logs do not leave a live local claim visible to workflow-engine retry reset
3. Then re-run:
   - `bosun simulate task resume --json --diagnose --config-dir .bosun --repo-root .`
4. Confirm the next retry descendant preserves completed nodes through:
   - `run-agent-plan`
   - `run-agent-tests`
   - and resumes at `run-agent-implement`
5. Keep the daemon gate closed until the same real task clears implement -> push -> PR -> review/fix -> merge -> local sync end to end.

Status: incident
