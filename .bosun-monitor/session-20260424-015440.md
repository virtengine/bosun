status: incident

# Bosun Local Ops Monitor — 2026-04-24 01:54:40

## What changed this cycle
- Landed another recovery patch in `workflow/workflow-engine.mjs`.
  - `buildActiveRunIndexEntry(...)` now persists run lineage/session topology fields into `_active-runs.json` instead of only `taskId`/`sessionId`.
  - `resumeInterruptedRuns()` now treats paused same-task ancestors marked `duplicate_task_run` / `covered_by_root_interrupted_run` as promotable recovery owners, and adds a regression path for duplicate parent -> duplicate child -> newly interrupted descendant chains.
- Added/extended recovery coverage in `tests/workflow-engine.test.mjs`.

## Validation
- `npx vitest run tests\workflow-engine.test.mjs -t '(prefers the interrupted same-task ancestor run over a newer child workflow run|re-activates a duplicate same-task ancestor when a newer interrupted descendant restarts)'` passed.
- `npm run build` passed.
- The older full-suite monitor run still ends with the same unrelated pre-existing failure in `tests/monitor-workflow-startup-guards.test.mjs` asserting a removed `infra/monitor.mjs` source string.
- `npm run prepush:check` is still blocked by unrelated local packaging/files-array drift in `infra/storage-janitor.mjs` and several `shell/*.mjs` files.

## Runtime proof
- Restarted the workspace daemon from PID `23472` onto patched source; new daemon PID is `15420`.
- Re-checked the live sibling family:
  - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - task `7f707e6d-5a61-480d-8346-59991af8d1bf`
  - task `0de863b2-94b7-4cd0-b71b-7d137cb5ba4c`
- Post-restart daemon log proved the interrupted cohort now included the old `Task Lifecycle` parents again, not just backend children:
  - `980a5f32-6d4b-4617-a02f-20faab589d19`
  - `35dacfce-7cdf-40c6-8056-c1c3db6bcdc1`
  - `b4c04dfe-64b3-4847-95bb-d3e99acf46a6`
  - plus child backend runs `1962517d-...`, `a2a1479b-...`, `3264d1fc-...`
- Even with that richer cohort, the live index still converged incorrectly:
  - `980a...`, `35da...`, `b4c0...` remain `paused`, `resumable=false`, `resumeResult=duplicate_task_run`
  - descendant backend runs remain the winners (`c775f774-...`, `e8183e7f-...`, `fb1c05df-...`)
- Real task-path outcome is still unhealthy:
  - `97d...` latest run `c775f774-...`, no PR, lifetime `lastStatus = "no_output"`
  - `7f70...` latest run `e8183e7f-...`, no PR
  - `0de8...` latest run `fb1c05df-...`, no PR, lifetime `lastStatus = "failed"`
- Global throughput remains stuck around `done=104`, `inprogress=15`, `blocked=35`, with no new non-monitor PR/merge outcome proved.

## Narrowest unresolved blocker
### Symptom
Live restart recovery still prefers descendant `Backend Agent` runs over same-task `Task Lifecycle` ancestors, even when the ancestor runs are present in the interrupted cohort.

### Proof
- Daemon log after the patched restart shows the sibling-family lifecycle parents were detected again in the startup cohort (`859141` / `859144` region in `logs\daemon.log`).
- The same restart also logged another duplicate-task dedupe pass (`Skipped 3 duplicate interrupted run(s)` at `859600`).
- The authoritative run index after restart still leaves the lifecycle parents as `duplicate_task_run`, while descendant backend runs remain the active/resumable winners.

### Code path
- `workflow/workflow-engine.mjs`
  - `_detectInterruptedRuns()`
  - `resumeInterruptedRuns()`
  - `latestByTaskId` representative selection / duplicate-task retirement

### Current blocker hypothesis
The new unit-tested representative/promotion logic works on synthetic chains, but the real interrupted cohort still contains extra same-task descendants and/or summary ordering that lets a later backend descendant win `latestByTaskId` after the lifecycle parent was reintroduced. The remaining bug is inside live representative selection, not daemon startup ownership or missing cohort membership.

## Next start point
1. Inspect the exact `resumeInterruptedRuns()` candidate set for the `859141` / `859144` restart cohort, including the additional same-task run IDs that appeared beside the known sibling family (`3cdfcbf5-...`, `a98a1975-...`, `b5f1d813-...`, `5db634b3-...`).
2. Instrument or temporary-log representative selection for those task IDs inside `resumeInterruptedRuns()` to capture:
   - selected representative run id
   - selected startedAt
   - ancestor chain walked
   - task identity resolved at each hop
3. Re-restart the daemon after that targeted instrumentation/fix and verify the winners flip from backend descendants back to `980a...`, `35da...`, and `b4c0...`.

