## Bosun Local Ops Monitor - 2026-04-26 14:50:55

### Startup sequence
- Read latest three notes before touching runtime state:
  - `.bosun-monitor/session-20260426-141934.md`
  - `.bosun-monitor/session-20260426-134839.md`
  - `.bosun-monitor/session-20260426-125850.md`
- Stayed on source branch:
  - `bosun/codex-self-improvement-loop-commits`
- Rebuilt the open-blocker list from the newest note before touching daemon/live comparison paths.

### Rebuilt open-blocker list from the newest note
1. Finish the incomplete repo validation from the MCP loader fix.
2. Re-enter the next real blocked/live task in simulator-first mode and explain why `_active-runs.json` is empty while task rows still show live task execution.
3. If a new code seam appears, land the smallest durable source fix instead of more runtime cleanup.

### What changed this cycle
- Confirmed repo baseline before runtime work:
  - branch: `bosun/codex-self-improvement-loop-commits`
  - source head: `845407e6b8b9ae84f58029f59090d9146f830f70`
  - package version: `0.43.0`
  - local vs `origin/main` after fetch:
    - behind `0`
    - ahead `231`
- Attempted to finish the pending repo validation first:
  - `npm test && npm run build`
  - this failed before Vitest execution because source-root `node_modules\\vitest` was missing again
  - precise failure:
    - `Unable to locate vitest from C:\Users\jON\Documents\source\repos\virtengine-gh\bosun`
- Isolated the dependency state:
  - root `package.json` still declares:
    - `vitest`
  - root `package-lock.json` still contains:
    - root `devDependencies`
    - `node_modules/vitest`
  - effective npm config remained clean:
    - `omit = []`
    - `include = []`
    - `NODE_ENV = null`
- One repair step for this symptom family:
  - `npm install --include=dev`
  - immediate proof after that repair:
    - source-root `node_modules\\vitest\\vitest.mjs` present
    - `npm ls vitest --depth 0` showed `vitest@4.0.18`
- But the same source-root dev-dependency drift reappeared again in the same cycle:
  - later source-root check:
    - `node_modules\\vitest\\vitest.mjs` missing
    - `npm ls vitest --depth 0` empty again
  - important split proof:
    - source root:
      - `vitest` missing
    - active task worktree:
      - `vitest` present
      - worktree-local tests can still run
- While validation stayed partially blocked, inspected the live task/index seam from the newest note:
  - earlier task rows that had looked `inprogress` resolved to:
    - `45e8b42e-1038-41a6-818b-d8d25b8a4962`
      - `status = blocked`
      - `blockedReason = blocked_by_repo`
    - `d3033b4b-5a4e-4143-857f-b5745465404e`
      - `status = blocked`
      - `blockedReason = blocked_by_repo`
    - `1eca450a-9a99-4b86-9f0c-41ad5699b248`
      - `status = blocked`
      - `blockedReason = implementation_done_commit_blocked`
- Inspected real blocked task `1eca450a...` end to end:
  - task:
    - `[m] feat(task): attach planner provenance metadata to created backlog items`
  - previous blocked run:
    - `d10d1d34-6d7f-43cf-92a5-e19cb812c0da`
  - run evidence showed:
    - `run-agent-plan` completed
    - `run-agent-tests` completed
    - `run-agent-implement` completed
    - pre-PR validation completed
    - task blocked later at push / commit handoff
  - precise blocked reason in the run artifact:
    - `implementation_done_commit_blocked`
  - captured hook output proved the failure was not transport-level git push rejection:
    - Bosun pre-push hook ran the local quality gate in the task worktree
    - it escalated to the full local suite because of a large diff
    - one real test failed:
      - `tests/provider-auth-settings.test.mjs`
      - failing case:
        - `builds provider credential lifecycle state from the shared credential store`
- Re-entered that exact real task in simulator-first mode:
  - `bosun simulate task 1eca450a-9a99-4b86-9f0c-41ad5699b248 --json`
  - new live run:
    - `16d7ab24-0c81-4ed4-a499-6f177a5ee7f3`
  - task row moved back to:
    - `status = inprogress`
    - `blockedReason = null`
  - latest session now:
    - `1eca450a-...:agent:16d7ab24-0c81-4ed4-a499-6f177a5ee7f3:run-agent-plan:turn`
  - current live worktree diff footprint:
    - `workflow/workflow-nodes.mjs`
    - `tests/kanban-adapter.test.mjs`
    - `tests/workflow-engine.test.mjs`
- This fresh retry exposed the real active-run persistence seam directly:
  - task row said:
    - `inprogress`
  - latest run file in workspace mirror had:
    - `endedAt = null`
    - early lifecycle nodes completed
  - but both:
    - source `.bosun\\workflow-runs\\_active-runs.json`
    - mirror `.bosun\\workspaces\\virtengine-gh\\bosun\\.bosun\\workflow-runs\\_active-runs.json`
    - had `count = 0`
- Root cause identified in source:
  - `workflow/workflow-engine.mjs`
  - `restoreFromSnapshot()` set:
    - `this._activeRuns.set(...)`
  - but did **not** call:
    - `_persistActiveRunState(...)` when the restored run started
    - `_clearActiveRunState(...)` when the restored run completed/errored
  - effect:
    - restored live task runs existed in memory and task rows
    - but `_active-runs.json` never reflected them
- Landed the durable source fix:
  - `workflow/workflow-engine.mjs`
    - `restoreFromSnapshot()` now persists active-run state at start
    - `restoreFromSnapshot()` now clears active-run state on completion/error
- Added focused regression coverage:
  - `tests/workflow-engine.test.mjs`
    - new restore-from-snapshot active-run persistence/cleanup case
- Focused Vitest for that new regression could not be completed normally because source-root `vitest` disappeared again.
- So I validated the exact patched code path with a direct engine-level restore smoke:
  - created a temporary workflow with a blocking long-running node
  - wrote a synthetic snapshot under `runs/snapshots`
  - called `engine.restoreFromSnapshot(snapshotId)`
  - observed:
    - `_active-runs.json` contained the live restored run while the node was blocked
    - `_active-runs.json` returned to `[]` after completion

### Durable source fixes landed this cycle
- `workflow/workflow-engine.mjs`
  - restored runs now persist to `_active-runs.json` at start
  - restored runs now clear `_active-runs.json` on completion/error
- `tests/workflow-engine.test.mjs`
  - added restore-from-snapshot active-run persistence/cleanup regression
- Existing uncommitted fixes from prior cycles remain in the worktree:
  - `shell/mcp-client.mjs`
  - `tests/openai-native-adapter-tier1.test.mjs`
  - `task/task-claims.mjs`
  - `tests/task-claims.test.mjs`

### Retired blockers
1. **Restored Task Lifecycle runs silently missing from `_active-runs.json` now have a source fix.**
   - Symptom:
     - live restored simulator runs existed
     - active-run indexes stayed empty
   - Proof:
     - `1eca450a...` live retry `16d7ab24...`
     - run file `endedAt = null`
     - both active-run indexes had `count = 0`
   - Fix:
     - `restoreFromSnapshot()` now persists and clears active-run state correctly
2. **The earlier MCP CLI boot crash remains retired.**
   - `bosun` startup still works after the prior cycle’s lazy-load fix

### Current runtime state
- Simulator gate:
  - **already proven end to end from prior cycles**
  - **still open this cycle**
- Daemon:
  - allowed this cycle
  - not used as the main path
- Live simulator state now:
  - task:
    - `1eca450a-9a99-4b86-9f0c-41ad5699b248`
  - run:
    - `16d7ab24-0c81-4ed4-a499-6f177a5ee7f3`
  - task row:
    - `status = inprogress`
    - `blockedReason = null`
    - no PR yet
  - run detail:
    - `endedAt = null`
    - `run-agent-plan = completed`
    - `run-agent-tests = running`
    - later lifecycle nodes still pending
- Bosun refused a second simulator launch while that run was live:
  - `Another task simulator instance is already running (PID 14312)`

### Active sink paths
- Current restored live run `16d7ab24...` is active in the workspace mirror run detail file.
- Because the currently running simulator process started before the new `restoreFromSnapshot()` fix was loaded, both active-run indexes still show no Task Lifecycle entries for that live run.
- This means the new source fix is landed and manually validated, but not yet proven by a fresh Bosun-managed live retry after patch load.

### Real task path inspection
- Inspected in depth this cycle:
  - `1eca450a-9a99-4b86-9f0c-41ad5699b248`
- End-to-end checkpoints inspected:
  - blocked task row
  - previous blocked run `d10d1d34...`
  - hook/test failure evidence inside the pre-push gate
  - new live simulator retry `16d7ab24...`
  - current worktree diff footprint
- Earlier end-to-end merged reference path remains:
  - `0650780c-2417-455e-8e43-2f37d73e8225`
  - PR `#485`
  - merged

### Throughput
- Latest confirmed non-monitor merge remains:
  - PR `#485`
  - merged at `2026-04-26T03:55:52Z`
- That keeps the “one merged non-monitor task per hour” target **met** at note time.
- Live trend remains positive:
  - one blocked real task was already repaired/merged in the prior cycle
  - another real task (`1eca...`) is back to live execution now

### Remaining runtime oddities / blockers
1. **Repeated source-root dev-dependency disappearance is still unresolved and now qualifies as an incident family.**
   - Symptom:
     - source-root `vitest` disappears from `node_modules`
     - `npm test` / `tools/vitest-runner.mjs` then fail locally
   - Proof:
     - root `package.json` + `package-lock.json` still contain `vitest`
     - npm config shows `omit = []`, `include = []`
     - `npm install --include=dev` restores `vitest`
     - later source-root `node_modules\\vitest\\vitest.mjs` is missing again
   - Additional proof:
     - active task worktree still has `node_modules\\vitest\\vitest.mjs`
     - source root does not
   - Status:
     - unresolved
     - blocks normal root-repo Vitest validation
2. **Current live task `1eca...` may still fall back into the same pre-push test failure family.**
   - Previous concrete blocker:
     - pre-push hook full-suite run
     - one failing test:
       - `tests/provider-auth-settings.test.mjs`
       - `builds provider credential lifecycle state from the shared credential store`
   - Current status:
     - fresh retry `16d7ab24...` is still live in `run-agent-tests`
     - outcome not known yet

### Validation performed
- Source validation already completed from the prior cycle:
  - focused MCP loader fallback test passed
  - simulator resume for task `0650780c...` created PR `#485` and merged
- This cycle:
  - source checks:
    - repo baseline confirmed
    - source-root `vitest` install state confirmed multiple times
  - attempted full validation:
    - `npm test && npm run build`
    - blocked by missing source-root `vitest`
  - repair proof:
    - `npm install --include=dev`
    - immediately restored source-root `vitest`
  - direct workflow-engine smoke validation:
    - restored run appeared in `_active-runs.json` while live
    - active-run index cleared after completion
- Remaining validation gap:
  - normal source-root Vitest rerun for the new workflow-engine regression
  - `npm run build`

### Git / code status
- Source branch:
  - `bosun/codex-self-improvement-loop-commits`
- Source HEAD:
  - `845407e6b8b9ae84f58029f59090d9146f830f70`
- Current tracked modifications:
  - `shell/mcp-client.mjs`
  - `task/task-claims.mjs`
  - `tests/openai-native-adapter-tier1.test.mjs`
  - `tests/task-claims.test.mjs`
  - `tests/workflow-engine.test.mjs`
  - `workflow/workflow-engine.mjs`
  - pre-existing unrelated:
    - `workflow/workflow-nodes/agent.mjs`
- Untracked monitor notes remain present.

### Local source vs upstream
- `origin/main` divergence after fetch:
  - behind `0`
  - ahead `231`
- Local source still reflects the latest merged `origin/main`.

### Narrowest next starting point
1. Re-read the newest three notes again.
2. Check live task `1eca450a-9a99-4b86-9f0c-41ad5699b248` first:
   - run `16d7ab24-0c81-4ed4-a499-6f177a5ee7f3`
   - determine whether `run-agent-tests` completes cleanly or re-blocks on the same pre-push/provider-auth-settings failure family.
3. As soon as that simulator slot frees, start one fresh blocked-task retry under the patched `workflow-engine.mjs` and confirm `_active-runs.json` now records the restored live run.

incident
