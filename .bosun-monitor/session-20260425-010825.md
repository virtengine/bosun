incident

# Bosun Local Ops Monitor — 2026-04-25 01:08:25 local

## Startup / scope
- Read the latest three monitor notes before touching runtime state:
  - `session-20260425-004002.md`
  - `session-20260424-235237.md`
  - `session-20260424-230708.md`
- Rebuilt the blocker list from `session-20260425-004002.md`.
- Stayed simulator-first and kept the daemon locked/off.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Open-blocker list rebuilt this cycle
1. **Simulator gate still blocked** — no real non-monitor task has reached code change -> tests/build -> commit -> PR -> review/fix -> merge.
2. **Delegated code runs are no longer falling back to `codex-sdk`** — the last completed live rerun still proved the delegated path stayed on `openai-native` / `gpt-5.4`, so that blocker remains closed.
3. **New narrow blocker: tests-phase native run can stall before any delegate/tool transcript appears** — the fresh post-fix rerun never advanced past `run-agent-tests` slot acquisition, emitted no node output, and did not release claim/slot.
4. **Workspace-command policy fix is source-validated but not yet live-validated** — delegated non-voice sessions can now run non-safe workspace commands in source, but the current live rerun has not advanced far enough to exercise that path.
5. **Local broad test baseline still blocked** — full `npm test` remains unusable here because the existing `demo-defaults:sync` pretest hang is unchanged.

## Durable fix landed this cycle
1. **Allow writable workspace commands for delegated non-voice tool sessions**
   - Files:
     - `voice/voice-tools.mjs`
     - `tests/voice-tools.test.mjs`
   - Change:
     - `run_workspace_command()` now keeps direct voice sessions read-only, but allows non-safe workspace commands for non-voice delegated/tool contexts instead of hard-blocking everything beyond the safe read-only pattern set.
   - Why it mattered:
     - the last completed live rerun (`2808e5cd-25e7-4690-8c9a-2d54c0af9c59`) showed delegated `openai-native` children staying on the right runtime but self-reporting `run_workspace_command` denial and then recursively delegating again.

## Validation completed this cycle
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\voice-tools.test.mjs -t "run_workspace_command blocks non-safe commands|run_workspace_command allows non-safe commands for delegated non-voice sessions|run_workspace_command allows safe read-only commands"`
- `npm run build`
- `bosun simulate task restart --json --config-dir .bosun --repo-root .`
  - latest rerun: `b111d744-5b78-4dad-8e69-44d937c80894`
  - **not terminal yet**; see blocker below

## Live simulator evidence from this cycle

### 1. Previous rerun still confirmed the old runtime-selection blocker stayed fixed
- Prior completed rerun:
  - `2808e5cd-25e7-4690-8c9a-2d54c0af9c59`
- Persisted proof from that run:
  - `run-agent-tests.status = completed`
  - `run-agent-tests.success = false`
  - `run-agent-tests.blockedReason = blocked_by_env`
  - `run-agent-implement = skipped`
  - `release-claim = completed`
  - `release-slot = completed`
- Delegate proof from the same lineage:
  - `delegate-live-1777042259769-fa9d40.json`
  - `delegate-live-1777042282587-f2b966.json`
  - `delegate-live-1777042290201-a6a424.json`
  - `delegate-live-1777042307497-d6b14a.json`
  - all persisted:
    - `agent = openai-native`
    - `model = gpt-5.4`
    - `providerSelection = azure-openai-responses-1775278346327`
- This kept the `codex-sdk` fallback blocker closed.

### 2. The new source fix is real, but the fresh live rerun has not reached it yet
- Post-fix rerun:
  - `b111d744-5b78-4dad-8e69-44d937c80894`
- Current persisted state after repeated polling:
  - `.bosun\workflow-runs\b111d744-5b78-4dad-8e69-44d937c80894.json`
    - `endedAt = null`
    - `run-agent-tests = running`
    - `run-agent-implement = null`
    - `release-claim = null`
    - `release-slot = null`
    - latest workflow log line still equals:
      - `run-agent-tests: Agent acquired shared agent slot after 1s`
  - `.bosun\workflow-runs\_active-runs.json`
    - still contains run `b111d744-5b78-4dad-8e69-44d937c80894`
  - parent tests session:
    - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_b111d744-5b78-4dad-8e69-44d937c80894_run-agent-tests_turn.json`
    - `status = active`
    - `endedAt = null`
    - only persisted message is the initial workflow-start marker
    - no assistant/tool/delegate messages yet
- In contrast to `2808...`, this rerun has not yet emitted the delegate/tool history needed to prove the new workspace-command behavior live.

### 3. No new delegate chain from the current rerun has materialized yet
- Recent session-file rechecks after the post-fix rerun only showed:
  - the parent tests turn file above
  - the task root session file
- No new `delegate-live-*.json` files were created for the `b111...` rerun window while it remained stuck.
- That means the current live blocker moved one step earlier than the previous read-only recursive-delegate proof:
  - the tests-phase run is not even reaching the point where delegated child sessions are persisted.

## Runtime health
- Daemon remained **locked/off** as the main path.
- Explicit daemon check:
  - `node .\cli.mjs --daemon-status --config-dir .bosun --repo-root .`
  - result:
    - daemon mode is **not** running
    - 1 Bosun process is active (the current simulator/monitor path)
- Current active workflow path:
  - `.bosun\workflow-runs\_active-runs.json` still holds `b111d744-5b78-4dad-8e69-44d937c80894`

## End-to-end task inspection
- Real task inspected end to end again:
  - task: `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - title: `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`
  - PR: none
- Current terminal state this cycle:
  - source fix landed for delegated non-voice workspace commands
  - focused regression passed
  - build passed
  - live simulator gate is still blocked
  - no code change landed on the real task
  - no test/build completed on task code through a write-capable agent
  - no commit created
  - no PR created
  - no merge

## Throughput
- Throughput target still **not met**.
- Recent merged PR throughput remains historical only:
  - `gh pr list --state merged --limit 5 --json number,title,mergedAt,author,baseRefName,headRefName`
  - latest merged PRs are still Dependabot merges from `2026-04-20` and earlier
  - no new non-monitor completion landed through the simulator gate this cycle

## Git / source state
- Branch remained:
  - `bosun/codex-self-improvement-loop-commits`
- Local branch still contains `origin/main`.
- Current branch is still ahead-only vs tracked remote:
  - `ahead 3`
- Repository remains very dirty with unrelated user work; edits stayed surgical.

## Narrowest unresolved blocker now
**The current narrow blocker is no longer delegated runtime selection, and the new workspace-command allowance is landed in source. The live simulator is now blocked earlier: the fresh `run-agent-tests` native session can remain active after slot acquisition with no tool/delegate/assistant transcript, no node output, and no claim/slot release, so the cycle cannot yet prove whether the new writable-command path fixes the recursive delegate blocker live.**

### Symptom
- `run-agent-tests` launches `openai-native` and acquires a slot.
- After that, no more persisted workflow-node output appears.
- No delegate child sessions are created for the current rerun.
- Claim/slot do not release because the workflow run never leaves `run-agent-tests`.

### Proof
- Workflow run:
  - `.bosun\workflow-runs\b111d744-5b78-4dad-8e69-44d937c80894.json`
  - `endedAt = null`
  - `run-agent-tests = running`
  - last log line still:
    - `Agent acquired shared agent slot after 1s`
- Active-runs file:
  - `.bosun\workflow-runs\_active-runs.json`
  - still lists `b111d744-5b78-4dad-8e69-44d937c80894`
- Parent tests session:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_b111d744-5b78-4dad-8e69-44d937c80894_run-agent-tests_turn.json`
  - `status = active`
  - `endedAt = null`
  - only the workflow-start system message is present
- Absence proof:
  - no new `delegate-live-*.json` files appeared in the current rerun window

### Symptom -> proof -> code path -> fix or blocker
- **Fix landed**
  - Symptom:
    - delegated non-voice sessions reported `run_workspace_command` denial and could not execute build/test/git commands even though they were already inside a delegated agent workflow
  - Proof:
    - completed run `2808e5cd-25e7-4690-8c9a-2d54c0af9c59`
    - parent tests transcript preserved repeated `run_workspace_command` blocked summaries while nested delegates stayed on `openai-native`
  - Code path fixed:
    - `voice/voice-tools.mjs`
    - `tests/voice-tools.test.mjs`
  - Validation:
    - focused voice-tools regression above
    - build passed

- **Current blocker**
  - Symptom:
    - current tests-phase native run never advances beyond slot acquisition
  - Proof:
    - current live run `b111d744-5b78-4dad-8e69-44d937c80894`
    - workflow and session files above
  - Suspect code paths:
    1. tests-phase first-event / no-output handling before any delegate/tool transcript is emitted
       - `workflow/workflow-nodes/actions.mjs`
       - `agent/agent-launcher.mjs`
       - `agent/provider-session.mjs`
       - `shell/openai-native-adapter.mjs`
    2. managed external session lifecycle when a native run emits no meaningful early events
       - `agent/agent-launcher.mjs`
       - `infra/session-tracker.mjs`

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first; do **not** unlock the daemon.
3. Resume from the current active run first:
   - `b111d744-5b78-4dad-8e69-44d937c80894`
   - if it still has not drained, compare the active workflow-run JSON and parent tests session again before restarting.
4. Trace why `run-agent-tests` can remain parked after:
   - `Running agent (openai-native)`
   - `Agent acquired shared agent slot after 1s`
   with no further persisted events, node output, or delegate files.
5. Inspect the first-event / no-output path in:
   - `workflow/workflow-nodes/actions.mjs`
   - `agent/agent-launcher.mjs`
   - `agent/provider-session.mjs`
   - `shell/openai-native-adapter.mjs`
6. Once the tests node advances again, re-check whether the new `run_workspace_command` allowance eliminates the previous recursive read-only delegate blocker.
7. Keep the full daemon locked/off until a real task completes implementation -> push -> PR -> review/fix -> merge.

incident
