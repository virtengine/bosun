incident

# Bosun Local Ops Monitor — 2026-04-22 14:06 local

## Runtime State
- Daemon was restarted at the end of this cycle so the latest source fix is now loaded: `bosun --daemon-status --config-dir .bosun --repo-root .` reports PID 90984.
- Runtime remains pinned to the repo-local `.bosun` store via `bosun --where --config-dir .bosun --repo-root .`.
- Package version remains `0.43.0` from `package.json`.
- Branch remains `bosun/codex-self-improvement-loop-commits`; local source is `0 behind / 190 ahead` of `origin/main`, so the checkout still reflects the latest merged `main`.
- Working tree remains heavily dirty with unrelated preserved edits; this cycle only touched the kanban adapter/test slice plus monitor notes/log captures.

## Active Sink Paths
- `.bosun/logs/monitor.log` is advancing after the daemon restart and shows clean startup plus fresh scheduler activity at `2026-04-22T04:05:54Z` onward.
- `.bosun/logs/monitor-error.log` was already active earlier in the cycle and still carries residual warning families; it was not the controlling path for this incident.
- `.bosun/workflow-runs/_active-runs.json` is currently empty immediately after restart, which is consistent with the old in-flight Task Lifecycle run being cleared during daemon recycle.
- Post-restart workflow activity is already visible in run `abb6fa40-cb7d-4eed-8825-dd91d0414036` for Task Batch Processor.

## Throughput / Queue Health
- Throughput target is still missed: no non-monitor merge appeared on `origin/main` in the last 24 hours.
- Post-restart scheduler liveness is not enough to call the system healthy: Task Batch Processor resumed, but its first completed cycle was `0/0` and no fresh non-monitor task progressed to merge.
- Per monitor policy, repeated zero-throughput sessions keep the system in incident state even though the durable source defect is now patched and deployed.

## Real Task Paths Inspected
- `a6a0198e-a547-4eb7-872c-af2c83604b5f`
- `8ed214ec-7eac-4887-8abd-97aa09c61694`

## End-to-End Evidence Inspected In Depth
- Deep path inspected: `a6a0198e-a547-4eb7-872c-af2c83604b5f`.
- Live evidence proved the completed run `b92f9df9-fdf3-46fd-8e67-7cf5d13f53ec` really did reach `detect-commits` and return `hasCommits: false`.
- The immediate redispatch run `672f856f-3276-4847-b35b-8aa7bad1b286` then exposed the real loss point: scheduler-visible task snapshots lacked `consecutiveNoCommits` and `cooldownUntil` even though the no-commit path had already run.
- The next active run snapshot `d0e5dc4c-36c7-4ab9-85aa-3a1265e8542a` still showed the same flattened `trigger.task` shape before the daemon restart, confirming the loss was happening before the scheduler consumed the task row.
- `8ed214ec-7eac-4887-8abd-97aa09c61694` was also inspected earlier in the cycle as a neighboring live Task Lifecycle run to rule out a single-run artifact.

## Symptom -> Proof -> Code Path -> Fix Or Blocker
- Symptom: durable no-commit anti-thrash fields were being written in the workflow path but dropped before `trigger.task_available` could see them, so no-commit tasks were immediately eligible for redispatch.
- Proof:
  - `workflow/workflow-nodes/actions.mjs` already persisted `consecutiveNoCommits` and `cooldownUntil` via `kanban.updateTask`.
  - `task/task-store.mjs` already persisted both fields.
  - Live run snapshots for `a6a0198e...` (`672f...`, then pre-restart `d0e...`) showed `trigger.task` without those fields.
  - `workflow/workflow-nodes/triggers.mjs` explicitly reads `task.cooldownUntil`, so the omission was scheduler-relevant.
- Code path:
  - `kanban/kanban-adapter.mjs`
  - internal backend `_normalizeTask()`
- Durable fix applied this cycle:
  - preserve `consecutiveNoCommits` and `cooldownUntil` on normalized internal task objects
  - mirror those fields into normalized `meta` as well
- Regression coverage added:
  - `tests/kanban-adapter.test.mjs`
  - new regression verifies internal `getTask()` and `listTasks()` preserve persisted anti-thrash cooldown fields
- Exact remaining blocker:
  - live end-to-end proof is still pending because the daemon was only restarted after validation completed, and no post-restart Task Lifecycle run has yet reached `detect-commits` under the patched process.

## Recent Workflow Run Behavior
- Pre-restart behavior clarified the incident instead of broadening it:
  - stale completed run `b92...` was eventually removed from `_active-runs.json`
  - the same task was immediately redispatched into `672f...`, then later `d0e...`
  - those runs proved the anti-thrash state was disappearing at task normalization time, not in `detect-commits` itself
- Post-restart behavior is currently clean but shallow:
  - monitor startup completed
  - Task Batch Processor fired and completed successfully
  - no fresh Task Lifecycle run has yet advanced far enough to prove the cooldown fields live
- Residual warning family still present after restart:
  - execution ledger write failed: `Cannot create a string longer than 0x1fffffe8 characters`

## Recurring Blockers From Prior Sessions
- No-commit anti-thrash durability gap: fixed in source, validated locally, and deployed via daemon restart; still awaiting first post-restart live proof.
- Worktree reacquire loop incident from prior sessions: not reopened by this cycle's evidence.
- Packed CLI manifest incident: remains resolved.
- Execution-ledger oversized-string warning: still recurring and still unresolved, but it was not the controlling cause of this cycle's redispatch bug.
- Non-monitor throughput remains flat and therefore unresolved.

## Validation Status
- Focused regression passed:
  - `npx vitest run tests/kanban-adapter.test.mjs -t "preserves persisted anti-thrash cooldown fields in internal task listings"`
- Explicit build passed:
  - `npm run build` exited `0`
- Captured broad validation completed cleanly:
  - `prepush-out.txt` contains the completed `npm run prepush:check` footer with zero failures
  - that run includes the full `test:all` path, so the broad test layer is green for this source state
- Edited files reported no diagnostics after the patch.

## Open Blockers
- First post-restart live Task Lifecycle proof for the cooldown fields is still missing.
- Execution-ledger oversized-string warnings still recur at startup.
- Throughput target remains missed because there is still no recent non-monitor merge.

## Git / Validation Status
- Relevant touched files for this incident slice:
  - `kanban/kanban-adapter.mjs`
  - `tests/kanban-adapter.test.mjs`
- Earlier anti-thrash persistence fixes in `workflow/workflow-nodes/actions.mjs` and `tests/workflow-task-lifecycle.test.mjs` remain in the worktree and were part of the controlling context.
- Local code changes are validated but not pushed.
- Local source still reflects latest merged `main` (`0 behind origin/main`).

## Narrowest Next Starting Point
1. Watch the first post-restart Task Lifecycle run for `a6a0198e-a547-4eb7-872c-af2c83604b5f` or the next real no-commit candidate task.
2. When that run reaches `detect-commits` with no commits, confirm the task row and `trigger.task` snapshot now retain `consecutiveNoCommits` and `cooldownUntil`.
3. If the fields still vanish, compare the restarted run snapshot against the raw task-store row immediately, because that would prove a second normalization/drop path beyond `_normalizeTask()`.
4. If the cooldown proof lands cleanly but throughput is still flat, pivot next to the execution-ledger oversized-string warning as the highest-signal remaining incident family.
