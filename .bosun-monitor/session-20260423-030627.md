# Bosun monitor handoff — 2026-04-23 03:06:27 local

## Open blockers
- Throughput is still below target: no non-monitor merges in the last hour; task stats remain `draft=45 todo=4 inprogress=1 inreview=0 done=104 blocked=34 total=191`.
- Representative task `8ed214ec-7eac-4887-8abd-97aa09c61694` is recovered under fresh run `d50edee8-77f7-4953-8d5c-e122a90540fd`; latest ledger checkpoint is `run-agent-tests` -> `agent.started` at `2026-04-22T17:18:11.112Z`.
- Task Batch still reports `0/0` outside the single recovered representative task path; Bosun is not yet back to healthy merge throughput.

## Symptom -> proof -> code path -> fix
- Symptom family 1: workflow-owned openai-native agent runs could sit indefinitely after `agent.started`, leaving tasks stranded `inprogress` with no quick failure signal.
- Proof:
  - Historical representative run `4a5ff885-9e6f-4546-9844-5e1f59ed4a40` stalled at `run-agent-implement` -> `agent.started`.
  - The openai-native launcher path lacked a short first-event watchdog and inherited the full six-hour task timeout.
- Code path 1: `agent/agent-launcher.mjs` `launchOpenaiNativeThread()` -> `createProviderKernel()` -> `providerSession.runTurn(...)`.
- Fix 1:
  - Added a scoped abort controller plus `getFirstEventTimeoutMs()` watchdog to `launchOpenaiNativeThread()`.
  - Count native provider events locally and abort with reason `first_event_timeout` when no first event arrives inside the configured window.
  - Normalize that failure to `OpenAI Native execution error: first_event_timeout (no events received within ...ms)`.
  - Added focused regression coverage in `tests/agent-pool.test.mjs` for a native provider session that never emits events.
- Symptom family 2: even after the launcher aborts, `providerSession.runTurn()` could still wait forever if the native runner ignored the abort signal and never settled.
- Proof:
  - `agent/provider-session.mjs` awaited the runner promise directly.
  - A focused regression using a native adapter that never resolves only became safe once the session layer itself raced the runner against the abort signal.
- Code path 2: `agent/provider-session.mjs` `runTurn()` round loop.
- Fix 2:
  - Added an abort-aware promise race so each runner/adapter execution rejects with `AbortError` as soon as the caller aborts, even if the underlying native runner never resolves.
  - Added focused regression coverage in `tests/provider-kernel.test.mjs` for a never-settling native runner.
- Live nuance:
  - The earlier apparent post-fix stall at `3432c4df-ec5d-42b6-859c-644faa302032` `run-agent-plan` was a log-window artifact. Deeper log inspection later showed `run-agent-plan` completed at `2026-04-22T17:08:22.933Z` and `run-agent-tests` completed at `2026-04-22T17:11:22.040Z` before `run-agent-implement` started.
  - The second daemon restart interrupted that legitimately progressing run. Startup recovery then behaved as designed: after the configured `WORKFLOW_STARTUP_HISTORY_RECOVERY_DELAY_MS=120000`, the daemon skipped the older duplicate run `4a5ff...`, resumed `3432c4df...` via `retryRun(from_scratch)`, and created fresh run `d50edee8-77f7-4953-8d5c-e122a90540fd`.

## Validation
- Passed: `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/agent-pool.test.mjs -t "fails openai-native launches that never emit a first event"`
- Passed: `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/provider-kernel.test.mjs -t "aborts provider sessions even when the native runner never settles"`
- Passed: `npm run build`
- Not run: full `npm test` and full `npm run prepush:check` (user explicitly asked not to run the long full-suite/prepush loops in this continuation).
- Touched files had no editor-reported errors after the change.

## Runtime adoption
- Restarted daemon from PID `114692` to PID `126484` after the launcher fix, then from PID `126484` to PID `18304` after the provider-session fix, with:
  - `bosun --stop-daemon --config-dir .bosun --repo-root .`
  - `bosun --daemon --config-dir .bosun --repo-root .`
- Runtime proof:
  - after one explicit task requeue (`bosun task update 8ed214ec-7eac-4887-8abd-97aa09c61694 --status todo --config-dir .bosun --repo-root .`), the first restarted daemon resumed `4a5ff...` and started fresh lifecycle run `3432c4df-ec5d-42b6-859c-644faa302032`
  - deeper log inspection later proved `3432c4df...` advanced through `run-agent-plan`, `run-agent-tests`, and into `run-agent-implement`
  - the second restarted daemon detected interrupted runs `3432c4df...` and `4a5ff...`, waited the configured 120s history-recovery delay, skipped the older duplicate interrupted run, resumed `3432c4df...` via `retryRun(from_scratch)`, and created fresh lifecycle run `d50edee8-77f7-4953-8d5c-e122a90540fd`
  - `_active-runs.json` now contains `d50edee8...`, and task `8ed214ec...` now points at `latestRunId: d50edee8-77f7-4953-8d5c-e122a90540fd`

## Source / git state
- Local branch: `bosun/codex-self-improvement-loop-commits`
- `origin/main...HEAD = 0 190`
- No merged `origin/main` PRs observed in the last hour
- Worktree remains dirty with many unrelated pre-existing edits; this cycle added the launcher first-event timeout fix, the provider-session abort propagation fix, and their focused regression tests

## Narrowest next starting point
- Start with `logs/monitor.log` and `.bosun/workflow-runs/execution-ledger/d50edee8-77f7-4953-8d5c-e122a90540fd.json`.
- The exact current boundary is `run-agent-tests` -> `agent.started` at `2026-04-22T17:18:11.112Z` for task `8ed214ec-7eac-4887-8abd-97aa09c61694`.
- If `d50edee8...` clears tests and reaches implement/PR handoff, the next monitor cycle should stay on throughput: confirm this path lands in review/merge and then verify Bosun resumes at least one non-monitor merge per hour.
- If `d50edee8...` stalls again on an agent boundary, use the new launcher + provider-session abort guards as the first discriminant and inspect the exact live node/ledger transition rather than requeueing immediately.