# Bosun local ops monitor handoff — 2026-04-25 15:06:08

Status: incident

## Startup / notes read
- Read latest three monitor notes before runtime work:
  - `.bosun-monitor/session-20260425-144047.md`
  - `.bosun-monitor/session-20260425-141525.md`
  - `.bosun-monitor/session-20260425-133252.md`
- Re-read the session plan before touching runtime state.

## Open blocker list rebuilt from the newest note
1. **Provider/runtime auth selection had to be proven on a real restart** — prior run `82d2827b-92f1-41ae-939f-25a00bd6125a` was stuck on Azure auth selection.
2. **A lingering simulator claimant was blocking fresh validation** — the task claim had to be traced before another restart could run real work.
3. **Daemon must remain locked off** until a real simulator task merges end to end.

## What changed this cycle
### 1. Durable source fix landed and validated — auth-aware harness selector
Code path:
- `workflow/workflow-nodes/actions.mjs`
  - `selectHarnessExecutorDefault()`
- `tests/workflow-task-lifecycle.test.mjs`

Fix:
- `action.resolve_executor` now filters harness-native candidates through the provider registry and only selects `openai-native` providers whose auth state can actually run.
- Added a lifecycle regression proving an auth-dead harness primary is skipped in favor of a runnable secondary.

Validation:
- Focused lifecycle selector tests passed:
  - `node .\node_modules\vitest\vitest.mjs run tests\workflow-task-lifecycle.test.mjs -t "prefers the harness primary executor over legacy codex defaults when harness runtime is active|skips an auth-dead harness primary executor in favor of a runnable secondary"`

### 2. Resume-first was rechecked and exhausted again
- `bosun simulate task resume --mode replan_from_failed --json --config-dir .bosun --repo-root .`
  - run `57507ef5-ec8c-4d02-85c6-b958bc144419`
  - still replayed stale historical state
  - reused old `resolve-executor` / `run-agent-implement` outputs instead of reevaluating the live Azure-blocked run
- `_active-runs.json` was empty while orphaned run `82d2827b-92f1-41ae-939f-25a00bd6125a` still had `endedAt = null`, so a full restart was justified.

### 3. Narrow runtime cleanup proved and cleared the real claimant
Proof:
- persisted claim:
  - `.cache/bosun/task-claims.json`
  - instance `wf-0462bdff`
  - PID `35240`
  - command line:
    - `node.exe ... bosun/cli.mjs simulate task restart --json --config-dir .bosun --repo-root .`
- presence registry:
  - `.cache/bosun/presence.json`
  - same instance kept renewing the claim
- restart attempts failed with:
  - `already claimed, skipping`

Action:
- Stopped the specific stuck claimant:
  - `Stop-Process -Id 35240`
- Immediately restarted the real task:
  - `bosun simulate task restart --json --config-dir .bosun --repo-root .`

Result:
- shared-state force-claim succeeded after stale override
- new active run:
  - `a766f793-c6b5-42a3-8361-b7ad1ba2dbcd`

## Real task path inspection this cycle
Inspected task `f82209df-e236-4a63-9ce8-67122a1ca96b` through:
- claim
- worktree acquire
- `resolve-executor`
- `run-agent-plan`
- `run-agent-tests`
- release / cleanup path

Fresh live proof:
- `.bosun/workflow-runs/a766f793-c6b5-42a3-8361-b7ad1ba2dbcd.json`
- `resolve-executor` now selected the runnable provider:
  - `sdk = openai-native`
  - `provider = azure-openai-responses-1775278346327`
  - `endpoint = https://sweden-central-openloans-resourc.openai.azure.com/openai/v1`
  - `authMode = apiKey`
- This proves the workflow-side auth selector fix is working on the real task path.

## Current narrowest blocker (new)
### Symptom
- After the selector fix, the real task no longer blocks on Azure auth first.
- The next restart stops one node later:
  - `run-agent-tests = completed`
  - output status = `failed`
  - `blockedReason = implementation_done_commit_blocked`
- Implement, validation, push, PR reuse/create, review, and merge all remain unreached on this fresh run.

### Proof
- Tests session summary:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_a766f793-c6b5-42a3-8361-b7ad1ba2dbcd_run-agent-tests_turn.json`
  - says the implementation is present, a targeted regression passed, the working tree is clean, and the remaining failure is around finalizing commit metadata
- Cached tool outputs from that same session show verification/build command shaping problems:
  - `.cache/tool-logs/1777092959950019.json`
    - Vitest received a single comma-joined filter:
      - `tests/workspace-*.test.mjs, tests/*memory*.test.mjs`
    - repo proof:
      - `tests/workspace-*.test.mjs` files do exist locally
    - so this is not just “no matching tests”; the filter shape is wrong
  - `.cache/tool-logs/1777092959950020.json`
    - targeted `tests/fleet-coordinator.test.mjs` passed
  - `.cache/tool-logs/1777092959950022.json`
    - build execution resolved to missing:
      - `C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-f82209dfe236-666ab4d38c\build.mjs`
- PR / throughput state remains unchanged:
  - PR `#482` still `OPEN`, `MERGEABLE`, no reviews, no merge
  - recent merged PRs are still Dependabot-only

### Code path / blocker
Primary blocker:
- delegated workspace command execution / command normalization for workflow agent verification commands
- exact repo-local execution seam is still not isolated, but the proof now shows the failure is **after** auth selection and **inside** command/tool execution for the task worktree

Coupled secondary seam discovered during the same run:
- post-run cleanup left the managed worktree orphaned
- `release-worktree = completed`
- `sweep-task-wts` reported:
  - `EPERM` removing `task-f82209dfe236-666ab4d38c`
- remaining worktree `.git` file points to a missing gitdir:
  - `.bosun\worktrees\task-f82209dfe236-666ab4d38c\.git`
  - `gitdir: C:/Users/jON/Documents/source/repos/virtengine-gh/bosun/.git/worktrees/task-f82209dfe236-666ab4d38c`
  - that target directory does not exist
- this cleanup bug is likely to matter on the next restart path even though the fresh blocker is still the command-execution seam

## Daemon / throughput / health
- Simulator gate: blocked
- Daemon: intentionally still locked off
- Recent merged throughput: still bot-only
- No fresh non-monitor merge landed

## Exact next steps
1. Trace the delegated workspace command execution seam that produced:
   - comma-joined Vitest filters
   - `build.mjs` resolution instead of the repo’s real `npm run build` script path
2. Determine whether that seam is repo-local and patchable in source, or an external tool-bridge blocker that must be handed off as such.
3. Separately inspect why post-run worktree release/sweep leaves orphaned managed worktree directories with missing gitdirs.
4. After a durable fix, continue simulator validation from the same task:
   - prefer `bosun simulate task resume` if a failed run becomes resumable
   - otherwise restart only when recomputation is required
5. Keep daemon monitoring locked off until a real task completes implement -> push -> PR -> review/fix -> merge end to end.

incident
