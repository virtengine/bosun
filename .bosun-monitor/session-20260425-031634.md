incident

# Bosun Local Ops Monitor — 2026-04-25 03:16:34 local

## Startup / scope
- Read the latest three monitor notes before touching runtime state:
  - `session-20260425-025442.md`
  - `session-20260425-021846.md`
  - `session-20260425-015550.md`
- Rebuilt the blocker list from `session-20260425-025442.md`.
- Stayed simulator-first and kept the daemon locked/off.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Current open-blocker list
1. **Simulator gate still blocked** — no real non-monitor task has reached code change -> tests/build -> commit -> PR -> review/fix -> merge.
2. **Native write-tool schema gap is now fixed and live-validated** — the tests-phase session no longer dies immediately on `edit_file`/`write_file` contract failure.
3. **Current narrow blocker: workflow-agent tests sessions can be created and marked running without ever attaching a worker/thread or producing meaningful events**.
4. **Stale claim/run persistence remains coupled to the live blocker** — after the wedged owner PID is gone, `_active-runs.json` and `.cache\bosun\task-claims.json` still remain live/stale.
5. **Throughput remains incident-level** — no non-monitor merge landed and recent merged PR throughput is still historical only.

## Durable source fixes landed this cycle
1. **Advertise real JSON schemas for harness-native write/edit/subagent tools**
   - Files:
     - `agent/tool-builtin-catalog.mjs`
     - `tests/tool-orchestrator.test.mjs`
   - Change:
     - added explicit `parameters` schemas for:
       - `write_file`
       - `edit_file`
       - `spawn_subagent`
       - `wait_subagent`
       - `cancel_subagent`
       - `list_subagents`
   - Why it mattered:
     - `createToolOrchestrator().listTools()` previously exposed:
       - `edit_file.parameters = null`
       - `write_file.parameters = null`
     - `shell/openai-native-adapter.mjs` falls back to `{ type: "object", properties: {} }` when tool schemas are missing, so the model saw the tool names but not usable required fields.

2. **Require a meaningful agent event before clearing the workflow first-event guard**
   - Files:
     - `workflow/workflow-nodes/actions.mjs`
     - `tests/workflow-engine.test.mjs`
   - Change:
     - added `isMeaningfulWorkflowAgentEvent()` and stopped `action.run_agent` from clearing `firstEventSeen` on every callback.
     - non-meaningful lifecycle callbacks like `session.stream.start` / `session.turn.start` no longer disable the silent-launch timeout by themselves.
   - Why it mattered:
     - live wedged runs were showing a tests session that stayed `running` with zero messages/tools/thread attachment, which matched a workflow-level guard being cleared too early by a non-meaningful callback.

## Validation completed this cycle
- Focused tool/schema regression:
  - `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\tool-orchestrator.test.mjs`
- Focused workflow guard regressions:
  - `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\tool-orchestrator.test.mjs tests\workflow-engine.test.mjs -t "aborts silent agent launches when no events arrive after slot acquisition|does not clear the silent launch guard for non-meaningful lifecycle events|registers Bosun-native built-ins by default and exposes OpenClaude-style aliases"`
- Build:
  - `npm run build`
  - passed twice after the source changes above

## Live simulator evidence

### 1. The old write-tool invocation blocker is fixed live
- Previous blocker from the newest old note:
  - run `66d6c93b-f356-4f12-97b4-f1d6a940a6e7`
  - tests summary:
    - `blocked: tool invocation failure`
    - `edit_file is unusable here because the wrapper isn’t accepting the required structure`
- After the tool-schema fix, simulator rerun:
  - `94dab10d-d40d-4747-9c70-4d9bbd70064d`
- Live change:
  - `run-agent-plan` completed normally
  - `run-agent-tests` started instead of failing immediately on tool invocation
  - no new `edit_file`/`write_file` contract-failure summary appeared
- Interpretation:
  - the old write-tool schema gap is no longer the primary live blocker.

### 2. The current live blocker is a silent tests-session / worker-attachment stall
- Rerun after the tool-schema fix:
  - `94dab10d-d40d-4747-9c70-4d9bbd70064d`
- Rerun after the workflow guard patch:
  - `ef70ecac-3eaf-488d-a449-3b312b70dcda`
- Shared live symptom on both reruns:
  - `run-agent-plan = completed`
  - `run-agent-tests = running`
  - `tests-agent-ok = null`
  - `run-agent-implement = null`
  - no delegate child sessions created
  - tests session file contains only the start marker:
    - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_<run>_run-agent-tests_turn.json`
  - session file remains:
    - `status = active`
    - `messages = [start marker only]`
    - `toolCalls = 0`
    - `toolResults = 0`
  - but `lastActiveAt` advances later than `createdAt`, proving the runtime still touches the session without emitting transcript/tool work.

### 3. Snapshot-store proof narrows the blocker further
- File:
  - `logs/session-snapshots.json`
- Exact session:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b:agent:ef70ecac-3eaf-488d-a449-3b312b70dcda:run-agent-tests:turn`
- Relevant lifecycle entries:
  - `child_session_created`
  - `external_session_bound`
  - `execution_registered`
- Persisted state on the latest `execution_registered` snapshot:
  - `status = running`
  - `threadId = null`
  - `activeThreadId = null`
  - `threadIds = []`
  - `activeWorkerId = null`
  - `workerAttached = false`
  - `workerSwapped = false`
  - `messageCursor = 0`
  - `toolCallCount = 0`
  - `toolResultCount = 0`
  - `providerSelection = ""`
  - `adapterName = ""`
- Interpretation:
  - the workflow-agent tests session is being registered as running before a worker/thread is actually attached, then remains silent indefinitely.

### 4. Runtime cleanup did not clear the stale run/claim automatically
- Owner PID from fresh rerun claim:
  - `33372`
- Proof:
  - `Get-Process -Id 33372` returned nothing after stop
  - but state still persisted:
    - `_active-runs.json` still lists `ef70ecac-3eaf-488d-a449-3b312b70dcda`
    - `.cache\bosun\task-claims.json` still holds the claim for task `f82209df-e236-4a63-9ce8-67122a1ca96b`
    - claim metadata still references `pid = 33372`
- Interpretation:
  - the silent tests-session stall and stale-claim persistence are coupled in the current live failure family.

## Runtime health
- Daemon remained **locked/off** this cycle.
- No daemon mode was started.
- Simulator state at handoff:
  - `_active-runs.json` still shows `ef70ecac-3eaf-488d-a449-3b312b70dcda`
  - claim file still holds the same task for dead owner PID `33372`

## End-to-end task inspection
- Real task inspected end to end again:
  - task: `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - title: `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`
  - PR: none
- Current path state:
  - plan completes with a concrete implementation plan
  - tests phase no longer fails on write-tool contract mismatch
  - tests phase still does **not** reach real code edits, tests, commit, PR, review, or merge
  - implementation never opens on the latest reruns
  - no task code landed
  - no PR created
  - no merge

## Throughput
- Throughput target still **not met**.
- Recent merged PR throughput remains historical only:
  - latest merged PRs are still Dependabot merges from `2026-04-20`
  - no non-monitor merge landed this cycle

## Git / source state
- Branch remained:
  - `bosun/codex-self-improvement-loop-commits`
- Branch status remained:
  - `ahead 3`
- `package.json` version remained:
  - `0.43.0`
- Repository is still very dirty with unrelated user work.
- Local source does **not** reflect a new merged `main` change this cycle because no non-monitor merge landed.

## Narrowest unresolved blocker now
**The write-tool schema failure is fixed, but the simulator is still blocked because the tests-phase workflow-agent session is created and marked `running` without ever attaching a worker/thread or producing meaningful events. The run then stays live indefinitely, and if the owner PID dies the claim/run state remains stale.**

### Symptom
- `run-agent-tests` starts and acquires a shared slot.
- The tests session file records only the initial system message.
- Snapshot-store shows `execution_registered` but:
  - `workerAttached = false`
  - `activeWorkerId = null`
  - `threadId = null`
  - zero messages/tool counters
- Workflow run remains `running` forever with no downstream node changes.

### Proof
- Workflow runs:
  - `.bosun\workflow-runs\94dab10d-d40d-4747-9c70-4d9bbd70064d.json`
  - `.bosun\workflow-runs\ef70ecac-3eaf-488d-a449-3b312b70dcda.json`
- Session files:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_94dab10d-d40d-4747-9c70-4d9bbd70064d_run-agent-tests_turn.json`
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_ef70ecac-3eaf-488d-a449-3b312b70dcda_run-agent-tests_turn.json`
- Snapshot-store:
  - `logs/session-snapshots.json`
  - session id above
- Stale claim proof:
  - `.cache\bosun\task-claims.json`
  - dead PID `33372`

### Symptom -> proof -> code path -> fix or blocker
- **Fix landed and live-validated**
  - Symptom:
    - tests session blocked on `edit_file` / `write_file` contract mismatch
  - Proof:
    - run `66d6...`
  - Code path fixed:
    - `agent/tool-builtin-catalog.mjs`
    - `tests/tool-orchestrator.test.mjs`
  - New live proof:
    - rerun `94dab10d...` moved into `run-agent-tests` instead of failing immediately on tool invocation

- **Fix landed and source/test-validated, but not yet live-proven**
  - Symptom:
    - workflow first-event guard cleared too early on non-meaningful callbacks
  - Code path fixed:
    - `workflow/workflow-nodes/actions.mjs`
    - `tests/workflow-engine.test.mjs`
  - Validation:
    - focused workflow regression passed
  - Live status:
    - rerun `ef70...` still wedged, so there is at least one additional live gap beyond the guard fix

- **Current blocker**
  - Symptom:
    - workflow-agent tests session enters `execution_registered` with no attached worker/thread and no meaningful output
  - Likely code path:
    1. workflow-run agent session bootstrap / first-event handling:
       - `workflow/workflow-nodes/actions.mjs`
       - `beginWorkflowLinkedSessionExecution()`
       - `launchExtra.onEvent`
    2. worker attachment / lifecycle registration:
       - `agent/session-manager.mjs`
       - `beginExternalSession()`
       - `registerExecution()`
       - `attachExecutionWorker()`
    3. native launcher/provider startup callbacks:
       - `agent/agent-launcher.mjs`
       - `agent/provider-session.mjs`
       - any early `onEvent` emission path that leaves `adapterName` / `providerSelection` blank and never attaches a worker
    4. stale shared-state cleanup after dead owner:
       - task-claim release / active-run cleanup for aborted workflow-engine owners

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first; do **not** unlock the daemon.
3. Resume from the current narrow blocker, not the old write-tool contract path.
4. Inspect the `workflow-run-agent` session bootstrap for why `registerExecution()` can persist:
   - `workerAttached = false`
   - `activeWorkerId = null`
   - `threadId = null`
   - `adapterName = ""`
   - `providerSelection = ""`
5. Trace and fix the earliest live seam between:
   - `workflow/workflow-nodes/actions.mjs`
   - `agent/session-manager.mjs`
   - `agent/agent-launcher.mjs`
   - `agent/provider-session.mjs`
6. After that fix, rerun:
   - `bosun simulate task restart --json --config-dir .bosun --repo-root .`
   and confirm either:
   - the tests node blocks cleanly with a terminal reason and releases claim/slot, or
   - the task progresses into real code changes / PR flow.
7. Only unlock daemon monitoring after a real non-monitor task completes implementation -> push -> PR -> review/fix -> merge.

incident
