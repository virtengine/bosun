## Bosun Local Ops Monitor - 2026-04-26 00:42:14

### Startup sequence
- Read latest three notes before touching runtime state:
  - `.bosun-monitor/session-20260426-002934.md`
  - `.bosun-monitor/session-20260425-234734.md`
  - `.bosun-monitor/session-20260425-223137.md`
- Confirmed source branch remains `bosun/codex-self-improvement-loop-commits`.
- Confirmed package version `0.43.0`.
- Stayed simulator-first and kept the full daemon locked off.

### Rebuilt open-blocker list from newest note
1. Finish validation of the last code change before any new cleanup.
2. Verify whether retry resume still clones stale runtime state into the new run.
3. If resume is fixed, continue the same real task until the next blocker is precise.
4. Do not unlock daemon monitoring until one real non-monitor task reaches implementation -> push -> PR -> review/fix -> merge end to end.

### What changed this cycle
- Landed a local source fix in `workflow/workflow-engine.mjs`:
  - retry resume now strips stale workflow session/runtime fields from retry input data
  - task-lifecycle resumes with prior claim/worktree state now reset preservation from `claim-task` instead of replaying stale resource nodes
- Added focused regression coverage in `tests/workflow-engine.test.mjs`:
  - `restarts task-lifecycle retries from claim-task when prior runtime state carried task resources`
- Focused validation passed:
  - `node .\node_modules\vitest\vitest.mjs run tests\workflow-engine.test.mjs -t "replays only the satisfied branch when retrying from_failed|restarts task-lifecycle retries from claim-task when prior runtime state carried task resources|migrates template defaults"`

### Simulator-first validation
#### 1. Resume after the retry fix
- Ran:
  - `bosun simulate task resume --json --config-dir .bosun --repo-root .`
- Resulting run:
  - `c11375df-7e69-4242-bcd2-3e7133689213`
- Outcome:
  - the stale-resume symptom changed materially
  - resume no longer jumped straight into stale downstream node state
  - instead it stopped in the explicit worktree-failure branch
- Proof:
  - `acquire-worktree` no longer looked like a reused preserved success
  - run logs show:
    - `Discarding broken managed worktree (pre-reuse): ...task-f82209dfe236-666ab4d38c missing_gitdir, git_diff_failed`
  - the run then blocked on:
    - `unmanaged_attached_worktree`
    - path: `C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\test-task-branch`

#### 2. One direct cleanup to validate the next seam
- Inspected `.bosun\test-task-branch` and proved it was a real attached worktree for the task branch:
  - branch: `task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
  - clean status
  - `HEAD = f5518999...`
- Per monitor policy, used one cleanup action for this blocker family:
  - `git worktree remove '.bosun\test-task-branch' --force`
- Immediately re-ran:
  - `bosun simulate task resume --json --config-dir .bosun --repo-root .`
- Resulting run:
  - `339105c2-3cd1-4d58-8cb0-db451bf0c3c5`

### Current narrowest unresolved blocker
**Task-lifecycle worktree refresh conflicts route to an async repair workflow that only queues under simulator mode**

#### Symptom
- After the unmanaged attached worktree was removed, the next simulator run still did not reach implementation/push/PR.
- It failed immediately at `acquire-worktree` with:
  - `managed worktree was removed after stale refresh state: C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-f82209dfe236-666ab4d38c`
- The main task run ended with:
  - `set-blocked-wt-failed = completed`
  - `dispatch-wt-repair = completed`
  - `notify-wt-failed = completed`
- But the repair was only queued, not executed:
  - `dispatch-wt-repair.kind = harness-subagent`
  - `dispatch-wt-repair.success = true`
  - `dispatch-wt-repair.status = queued`
  - `dispatch-wt-repair.runId = null`
  - `dispatch-wt-repair.workflowId = template-task-repair-worktree`
- After the run:
  - `_active-runs.json = []`
  - claims remain empty
  - PR `#482` head stayed `22753c9fcfd94e20bb1d1cc1d9f7db6e3ca02179`

#### Proof
- `339105c2...` outputs:
  - `acquire-worktree.success = false`
  - `failureKind = branch_refresh_conflict`
  - `blockedReason = Managed worktree refresh conflict detected; task remains blocked until repair workflow succeeds.`
- The managed worktree path was removed by the failure path:
  - `C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-f82209dfe236-666ab4d38c`
  - current state after run: `WORKTREE_MISSING`
- Task-lifecycle template still defines repair as async dispatch:
  - `workflow-templates/task-lifecycle.mjs`
  - `dispatch-wt-repair`
  - `workflowId: "template-task-repair-worktree"`
  - `mode: "dispatch"`

#### Code path
- Retry fix landed in:
  - `workflow/workflow-engine.mjs`
- New blocker exposed in:
  - `workflow/workflow-nodes/actions.mjs`
    - `ensureWorktreeContainsBaseBranch(...)`
    - `syncReusableWorktreeToBaseBranch(...)`
    - `classifyAcquireWorktreeFailure(...)`
  - `workflow-templates/task-lifecycle.mjs`
    - `dispatch-wt-repair` uses `action.execute_workflow` with `mode: "dispatch"`

#### Why this is now the blocker
- Simulator-first mode proved the resume-state bug was real and the new engine fix corrected it.
- Once stale resume state stopped masking the problem, the task-lifecycle repair path became the actual frontier.
- In simulator mode, the repair workflow is only queued and not executed inline, so the task remains blocked and the main run ends before implementation/push/PR/review/merge can resume.

### Real task path inspection
- Real task inspected end to end this cycle:
  - task: `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - branch: `task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
  - PR: `#482`
  - current live frontier tested through:
    - retry resume
    - worktree validation / unmanaged attachment detection
    - managed worktree refresh conflict
    - repair workflow dispatch
- Still unreached:
  - `run-agent-implement`
  - `push-branch`
  - PR update
  - review
  - fix/re-review
  - merge

### Runtime / throughput
- Simulator gate:
  - blocked
- Daemon:
  - still locked off
- Active runs:
  - none
- Claims:
  - none
- Recent non-monitor throughput:
  - no merge landed
- Target of at least one non-monitor merge per hour:
  - not met

### Repo state
- Source `HEAD`:
  - `a7b0d603`
- Local source still does **not** reflect a new merged `main` beyond the existing branch state.
- This cycle added local monitor edits in:
  - `workflow/workflow-engine.mjs`
  - `tests/workflow-engine.test.mjs`
- No new git commit was created this cycle.

### Narrowest next starting point
1. Re-read the newest three notes again.
2. Start from the task-lifecycle repair seam, not the old stale-resume seam.
3. Decide whether `dispatch-wt-repair` must run synchronously during simulator mode (or otherwise wait for the child repair run) so simulator-first validation can continue past `branch_refresh_conflict`.
4. Re-run:
   - `bosun simulate task resume --json --config-dir .bosun --repo-root .`
5. Keep daemon monitoring locked off until a real non-monitor task completes implementation -> push -> PR -> review/fix -> merge end to end.

incident
