status: incident

# Bosun Local Ops Monitor — 2026-04-24 04:50:30

## Open blockers
- Non-monitor merged PR throughput remains `0` in the last hour.
- Target task family `97d41516-caca-4fa0-9175-dbe6ad4ce01d` is still `inprogress` with no PR and no fresh active run after restart.
- Fresh post-fix task execution is moving again, but the fix has not yet carried a real task all the way through validation and PR handoff.

## What changed this cycle
- Landed a surgical oversized-request guard in `shell/openai-native-adapter.mjs`.
  - Added `sanitizeHistoryEntriesForRequest(...)`.
  - Clamp oversized `user_message`, `assistant_message`, tool-call argument, and string tool-output history entries before OpenAI request assembly.
  - Apply the guard when history is stored and again right before building Responses API input.
- Added focused regression coverage in `tests/openai-native-adapter-tier1.test.mjs`.

## Symptom -> proof -> code path -> fix
- Symptom:
  - repeated `action.run_agent` / planner failures with `Invalid string length`
  - daemon log previously showed OpenAI `string_above_max_length` with `input[5].content[0].text` lengths around `17.5M`-`18.3M`
- Proof:
  - prior live task path `97d41516...` / lifecycle `1c0ce7e2...` failed at `run-agent-plan`
  - backend sibling runs failed at `write-tests` / `implement`
  - fresh latest failed examples still present in store before restart:
    - `3ef4cbcd-23cf-486c-b30a-1cb75a09aaae` — Task Lifecycle — `Failed at Agent Plan: Invalid string length`
    - `636fb058-08af-46f8-bb9e-e9fbb0c50e13` — Backend Agent — `Failed at Write Tests First: Invalid string length`
- Code path:
  - `shell/openai-native-adapter.mjs`
  - request assembly via `buildResponsesRequest(...)`
  - session history storage / reuse in the native adapter
- Fix:
  - hard-cap oversized history text before request serialization so a single carried-forward item cannot exceed OpenAI per-item text limits.

## Validation
- Passed: `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/openai-native-adapter-tier1.test.mjs`
- Passed: `npm run build`
- Passed direct probe:
  - sanitized a live-sized `18,356,850` char message down to `8,000,000` chars with truncation marker

## Runtime re-check after restart
- Restarted with:
  - `bosun --terminate --config-dir .bosun --repo-root . --no-update-check`
  - `bosun --daemon --config-dir .bosun --repo-root . --no-update-check`
- Daemon after restart:
  - PID `1236`
- Active sink paths advancing:
  - `logs/daemon.log`
  - `.bosun/workflow-runs/index.json`
  - `.bosun/workflow-runs/_active-runs.json`
  - `.bosun/.cache/kanban-state.json`
- Post-restart blocker scan:
  - `post_restart_blocker_hits=0` for:
    - `Invalid string length`
    - `string_above_max_length`
    - `node:FAILED run-agent-plan`
    - `node:FAILED write-tests`
    - `node:FAILED implement`

## End-to-end task inspection
- Historical blocked task path inspected in depth again:
  - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - task record still `inprogress`, `prUrl=null`, `latestRunId=47a0e0ce-...`
  - run-family tail still includes failed lifecycle `1c0ce7e2-...` and paused backend descendants
- Fresh live post-fix path inspected:
  - task `3be828a2-d3e5-4225-b1f0-5d28905a865e`
  - lifecycle `ce7cc34b-...` currently running past prompt build into `run-agent-plan`
  - backend `3d13ef22-...` currently running at `plan-work`
  - no post-restart oversize-string failures observed on that family yet

## Workflow-family spot checks
- `Task Lifecycle`: fresh run active
- `Backend Agent`: fresh run active
- `Bosun PR Watchdog`: recent run completed
- `Task Replenish (Scheduled)`: recent run active

## Throughput / health
- `bosun task stats --json --config-dir .bosun --repo-root .`
  - `draft=45`
  - `todo=5`
  - `inprogress=29`
  - `done=104`
  - `blocked=35`
- Last-hour merged non-monitor PR count: `0`

## Git / mainline state
- Branch: `bosun/codex-self-improvement-loop-commits`
- Local branch contains latest fetched `origin/main`

## Next start point
1. Continue from the fresh post-fix live family `3be828a2...` and verify it clears:
   - `run-agent-plan`
   - `write-tests`
   - validation
   - PR handoff
2. Then re-check whether the old stuck `97d41516...` family is superseded naturally or still needs a second durable fix in a different oversized field path.
3. Do not reopen the already-fixed heavy-runner Windows spawn bug unless it reappears live.

incident
