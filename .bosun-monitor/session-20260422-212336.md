# Bosun Monitor Session 20260422-212336

- Branch: `bosun/codex-self-improvement-loop-commits`
- Version: `0.43.0`
- Local source vs `origin/main`: `0` behind, `190` ahead
- Daemon: restarted from PID `30268` to PID `8164`
- Runtime paths: config dir `.bosun`, workspaces `.bosun/workspaces`

## Open blockers

- Throughput is still below target. `git log origin/main --merges --since='4 hours ago'` returned no merges.
- Two real task lifecycles are active again after restart and need end-to-end follow-through to PR/merge:
  - `abb28df1-1760-4903-aa28-5e0406445325` for task `8ed214ec-7eac-4887-8abd-97aa09c61694`
  - `8e4f8913-9315-4e71-b212-086e374b9eca` for task `a6a0198e-a547-4eb7-872c-af2c83604b5f`

## Symptom -> proof -> code path -> fix

- Symptom: Task Batch redispatched cooled-down `todo` tasks immediately after a no-commit cooldown bounce.
- Proof:
  - `monitor.log` at `2026-04-22T10:49:04Z` showed task `8ed214ec...` bounce `inprogress -> todo` with a 15 minute cooldown, then `Task batch completed: 1/1 succeeded` at `10:49:16Z`, then `todo -> inprogress` at `10:49:17Z`.
  - `monitor.log` at `2026-04-22T10:54:44Z` / `10:55:30Z` showed the same pattern for task `a6a0198e...`, including a 30 minute cooldown before redispatch.
- Code path: `workflow-templates/task-batch.mjs` uses `isTaskBatchDispatchEligible(task)` inside the Task Batch `query-tasks` inline script. That helper ignored persisted `task.cooldownUntil`, so the earlier trigger-only fix in `workflow/workflow-nodes/triggers.mjs` did not protect the actual batch selector.
- Fix applied:
  - Added a future-`cooldownUntil` guard in `workflow-templates/task-batch.mjs`.
  - Added a regression in `tests/workflow-templates.test.mjs` covering active vs expired persisted cooldowns.

## Validation

- Focused regression passed:
  - `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/workflow-templates.test.mjs -t "skips todo tasks whose persisted cooldownUntil is still active"`
- `npm test` passed (`.bosun-monitor/npm-test-task-batch-cooldown.exit.txt = 0`)
- `npm run build` passed (`.bosun-monitor/build-task-batch-cooldown.exit.txt = 0`)
- `npm run prepush:check` passed on clean rerun (`.bosun-monitor/prepush-task-batch-cooldown-rerun.exit.txt = 0`)
- Note: the first file-backed prepush launch produced a static log with no exit marker, so it was discarded as unusable evidence and rerun clean.

## Live runtime proof after restart

- After restarting onto PID `8164`, the live Task Batch Processor ran and selected no tasks:
  - `2026-04-22T11:14:03.836Z` -> `Task batch completed: 0/0 succeeded (0 failed)`
  - `2026-04-22T11:15:00.541Z` -> `Task batch completed: 0/0 succeeded (0 failed)`
  - `2026-04-22T11:16:09.619Z` -> `Task batch completed: 0/0 succeeded (0 failed)`
- `_active-runs.json` later repopulated with the two real `template-task-lifecycle` runs listed above, so real work resumed independently after startup. The important point is that the post-fix Task Batch cycles did not emit another `1/1` redispatch.

## Health assessment

- Cooldown redispatch root cause is fixed in source and validated.
- Real workflows are active again after restart.
- Bosun is still `incident` overall because recent non-monitor merge throughput is zero and the cycle has not yet produced a merge.

## Narrowest next start

- Start with the newest active lifecycle runs `abb28df1-1760-4903-aa28-5e0406445325` and `8e4f8913-9315-4e71-b212-086e374b9eca`.
- Confirm one of them advances through tests/implement/PR rather than bouncing back.
- If either task returns to `todo` before its cooldown expires, immediately recheck `monitor.log` for any new post-fix Task Batch `1/1` redispatch evidence. Otherwise stay on throughput and drive toward a non-monitor merge.