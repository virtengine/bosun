# Bosun Monitor Session 2026-04-22 17:00:39

## Outcome

- Status: incident
- Branch: `bosun/codex-self-improvement-loop-commits`
- Package version: `0.43.0`
- Main freshness: `git rev-list --left-right --count origin/main...HEAD` => `0 190` (`0` behind, `190` ahead)
- Recent merged throughput: `git log origin/main --merges --since="2 hours ago" --oneline` returned no merges

## Open Blockers

1. Fresh daemon startup recovery on PID `128712` detected interrupted task-lifecycle runs but the delayed `workflow-history-unstick` pass completed in `1ms` without recreating any active task-lifecycle runs.
2. Source fixes now restore top-level workflow/session fields in `bosun task get`, but live task rows still do not advance to the resumed run IDs or populate `worktreePath` after the restarted daemon’s no-op recovery pass.
3. `execution ledger write failed: Cannot create a string longer than 0x1fffffe8 characters` still appears on daemon startup.
4. Throughput target is still not met; no non-monitor merges landed in the last 2 hours.

## Symptom -> Proof -> Code Path -> Fix Or Blocker

### 1. Top-level task workflow/session fields were stale even though task topology was already correct

- Symptom:
  - `bosun task get 8ed... --json` and `bosun task get a6a... --json` returned `workflowName`, `latestRunId`, and `sessionId` as `null` while `.bosun/.cache/kanban-state.json` already held the correct values under `task.topology`.
- Proof:
  - Direct store inspection showed for both representative tasks:
    - top-level `latestRunId/workflowName/sessionId/worktreePath = null`
    - `task.topology.latestRunId`, `task.topology.workflowName`, and `task.topology.sessionId` were populated correctly.
  - `task/task-cli.mjs::taskGet()` simply returns `store.getTask()`, so there is no later read-time projection layer in the CLI.
- Code path:
  - `task/task-store.mjs`
    - `normalizeTaskTopology()`
    - `normalizeTaskStructure()`
    - `refreshTaskTopology()`
    - `linkTaskWorkflowRun()`
- Fix applied:
  - Added `syncLegacyTaskTopologyFields(task)` in `task/task-store.mjs` to hydrate the legacy top-level task fields from normalized `task.topology`.
  - Wired that helper into task normalization and task-topology refresh/update paths.
- Regression added:
  - `tests/task-store.test.mjs`
    - `hydrates legacy top-level workflow fields from persisted topology`
- Validation:
  - Focused regression passed.
  - Live CLI proof after the fix:
    - `bosun task get 8ed... --json` now returns `workflowId=template-task-lifecycle`, `workflowName=Task Lifecycle`, `latestRunId=2416976b-5875-4945-a4a3-cb87f72a8386`, `sessionId=8ed...:agent:2416976b...:run-agent-plan:turn`
    - `bosun task get a6a... --json` now returns `workflowName=Task Lifecycle`, `latestRunId=e2ef0b96-7c59-4c04-ad50-4ce7e51c5b3d`, `sessionId=a6a...:agent:e2ef0b96...:run-agent-plan:turn`

### 2. `action.run_agent` had a real worktree persistence gap, but live proof is blocked by a new recovery/projection failure family

- Symptom:
  - `worktreePath` stayed `null` in `bosun task get` even for task-backed workflow agent runs that clearly execute from a worktree-aware context.
- Proof:
  - Source inspection showed `workflow/workflow-nodes/actions.mjs::syncTaskDelegationTopology()` only patched `topology` in task-store.
  - `task/task-store.mjs::updateTask()` also ignored `worktreePath` because it was missing from `directPatchSetters`.
- Code path:
  - `workflow/workflow-nodes/actions.mjs`
    - `syncTaskDelegationTopology()`
    - task-backed `action.run_agent` call site
  - `task/task-store.mjs`
    - `updateTask()` direct patch setter list
- Fix applied:
  - `syncTaskDelegationTopology()` now splits `worktreePath` out of the topology patch and persists it as a top-level task field.
  - `task/task-store.mjs::updateTask()` now accepts `worktreePath` patches.
- Regression expanded:
  - `tests/workflow-task-lifecycle.test.mjs`
    - `persists workflow topology for task-backed action.run_agent executions`
    - now also asserts `storedTask.worktreePath === workingDir`
- Validation:
  - Focused regression passed.
- Remaining blocker:
  - Live daemon proof did not reach this new source path because the restarted daemon never recreated live task-lifecycle runs after the delayed recovery pass.

### 3. New runtime blocker after restart: startup detects interrupted runs, but delayed recovery no-ops and task rows remain stranded on retired runs

- Fresh daemon:
  - Stopped old PID `127964`
  - Started new PID `128712`
  - Restart timestamp: `2026-04-22T16:55:43.7764757+10:00`
- Startup proof:
  - `2026-04-22T06:55:52.595Z` and `06:55:52.841Z` logged `Detected 4 interrupted run(s): 2416976b..., e2ef0b96..., 3e50ac05..., 43774197...`
- Delayed recovery proof:
  - `2026-04-22T06:57:58.494Z` logged startup `workflow-history-unstick` attempt
  - `2026-04-22T06:57:58.496Z` logged success with `durationMs: 1`
  - There were **no** matching `Resuming ... interrupted run(s)...` lines for this restart window
  - `.bosun/workflow-runs/_active-runs.json` remained `[]`
- Representative task proof after the no-op delayed pass:
  - `bosun task get 8ed... --json`:
    - `status=inprogress`
    - `blockedReason="Repeated no-output agent starts detected; stopping retries"`
    - `cooldownUntil=2026-04-22T05:51:54.942Z`
    - `latestRunId=2416976b-5875-4945-a4a3-cb87f72a8386`
    - `worktreePath=null`
  - `bosun task get a6a... --json`:
    - `status=inprogress`
    - `blockedReason="Repeated no-output agent starts detected; stopping retries"`
    - `cooldownUntil=2026-04-22T05:48:31.396Z`
    - `latestRunId=e2ef0b96-7c59-4c04-ad50-4ce7e51c5b3d`
    - `worktreePath=null`
- Exact run-index state for the startup-detected runs:
  - `2416976b...` (`8ed...`) -> `paused`, `resumable=false`, `resumeResult=duplicate_task_run`
  - `e2ef0b96...` (`a6a...`) -> `paused`, `resumable=false`, `resumeResult=duplicate_task_run`
  - `3e50ac05...` (`8ed...`) -> `paused`, `resumable=false`, `resumeResult=resumed`
  - `43774197...` (`a6a...`) -> `paused`, `resumable=false`, `resumeResult=resumed`
- Newest indexed runs per representative task still show no recreated active run:
  - `8ed...` newest rows end at `2416976b... (duplicate_task_run)` then `3e50ac05... (resumed)`
  - `a6a...` newest rows end at `e2ef0b96... (duplicate_task_run)` then `43774197... (resumed)`
  - `_active-runs.json` is still empty at end of cycle
- Blocker interpretation:
  - The runtime now retires interrupted rows, but the restarted daemon does not leave behind a fresh active task-lifecycle run or advance the task projection to the resumed lineage.
  - This blocks live proof of the new `worktreePath` persistence fix, because no post-restart task-backed `action.run_agent` turn executed on the edited daemon.

## Validation Status

- Focused regressions passed:
  - `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/task-store.test.mjs -t "hydrates legacy top-level workflow fields from persisted topology"`
  - `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/workflow-task-lifecycle.test.mjs -t "persists workflow topology for task-backed action.run_agent executions"`
- Full suite passed:
  - `npm test` -> `__EXIT:0`
- Build passed:
  - `npm run build` -> `__EXIT:0`
- Pre-push validation:
  - `prepublish-check`, `syntax:check`, `prompt:lint`, and `smoke:packed-cli` passed inside `npm run prepush:check`
  - the command then entered the duplicate `test:all` tail after the already-passed standalone `npm test`, so the redundant rerun was terminated once equivalence was clear

## Runtime State At End Of Cycle

- Daemon running: PID `128712`
- Active sink paths advancing:
  - `.bosun/logs/monitor.log`
  - `.bosun/workflow-runs/index.json`
  - `.bosun/workflow-runs/_active-runs.json`
- Real workflow progress:
  - source fixes validated
  - live task-lifecycle recovery after restart did **not** recreate active runs for the representative tasks
- Throughput target:
  - not met

## Narrowest Next Starting Point

- Leave the task-store/CLI projection fixes in place; they are source-validated and live-proved for workflow/session fields.
- Start at the post-restart recovery handoff that converts a `resumeResult=resumed` interrupted row into a fresh active task-lifecycle run and a task projection update.
- Most likely next hop:
  1. inspect `workflow/workflow-engine.mjs` around the delayed `resumeInterruptedRuns()` startup pass for why PID `128712` logged success in `1ms` with no `Resuming ...` lines despite startup detection of 4 interrupted runs
  2. inspect how `retryRun()` updates task/workflow projection when interrupted rows are marked `resumed`
  3. prove why task rows still point to `2416976b...` / `e2ef0b96...` after `3e50ac05...` / `43774197...` were marked `resumed`
