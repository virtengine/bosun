incident

# Bosun Local Ops Monitor — 2026-04-24 23:07:08 local

## Startup / scope
- Read the latest three monitor notes before touching runtime state:
  - `session-20260424-215808.md`
  - `session-20260424-205542.md`
  - `session-20260424-195743.md`
- Rebuilt the blocker list from `session-20260424-215808.md`.
- Stayed simulator-first and kept the daemon locked/off.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Open-blocker list rebuilt this cycle
1. **Simulator gate still blocked** — no real non-monitor task has reached commit, PR creation, review/fix, merge, or local-source reflection.
2. **New narrow live blocker: delegated `openai-native` provider/deployment failure** — the parent workflow now blocks correctly, but delegated child sessions are failing below that layer with `OpenAI API error 404 / DeploymentNotFound`.
3. **Nested delegate-session cleanup still leaks** — after the parent workflow finishes and releases claim/slot, nested delegate session files can remain `status = active` with no terminal message.
4. **Local broad test baseline still blocked** — full `npm test` remains unusable here because the existing `demo-defaults:sync` pretest hang is unchanged.

## Durable fixes landed this cycle
1. **Workflow blocked-classifier hardening for the new live blocked summaries**
   - File: `workflow/workflow-nodes/actions.mjs`
   - Added coverage for newly observed persisted phrases:
     - `blocked from completing this task in the current session`
     - `workspace only permits read-only shell access`
     - `file-edit tools available here require exact replacement payloads`
     - `blocked: tool capability issue in current session`
     - `Still blocked: no usable write-capable tool path in this session`
     - `this session does not expose a working file-edit path`
     - `cannot truthfully claim implementation`
2. **Regression coverage for success-shaped blocked summaries**
   - File: `tests/workflow-engine.test.mjs`
   - Added focused regressions proving:
     - blocked stream text must still classify `blocked_by_env` even when the final summary looks like a harmless tool result
     - the new tool-capability summary family must not leak `success=true`

## Validation completed this cycle
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\workflow-engine.test.mjs -t "read-only shell access blocker streams|delegated session before-work-start failures|blocked write-capable tooling summaries|insufficient write-execute capability"`
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\workflow-engine.test.mjs -t "tool-capability blocked summaries|read-only shell access blocker streams|delegated session before-work-start failures|blocked write-capable tooling summaries"`
- `npm run build`
- `bosun simulate task restart --json --config-dir .bosun --repo-root .`

## Live simulator evidence from this cycle

### 1. Pre-fix rerun still leaked success on a blocked tests node
- Run: `21c02c75-949d-4926-8197-b08e044c07de`
- Persisted proof before the second classifier patch:
  - `run-agent-tests.status = completed`
  - `run-agent-tests.success = true`
  - `run-agent-tests.blockedReason = null`
  - `run-agent-tests.summary = "Still blocked: no usable write-capable tool path in this session..."`
  - `run-agent-implement.status = completed`
  - `run-agent-implement.success = true`
- Stream proof from the same node still contained:
  - `blocked: tool capability issue in current session`
  - `this session does not expose a working file-edit path`
  - `I cannot truthfully claim implementation`

### 2. Post-fix rerun now blocks correctly at tests phase
- Run: `dd137493-a5f5-4789-82c9-d0e7a55cef8d`
- Persisted proof:
  - `run-agent-tests.status = failed`
  - `run-agent-tests.success = false`
  - `run-agent-tests.blockedReason = blocked_by_env`
  - `run-agent-tests.summary = "Blocked. - I cannot run the required workspace commands in this session. - The delegated agent sessions have not returned actionable output..."`
  - `tests-agent-ok.value = false`
  - `set-blocked-agent-tests-failed.success = true`
  - `run-agent-implement` not opened
  - `detect-commits` not opened
  - `release-claim.success = true`
  - `release-slot.success = true`
  - `.bosun\workflow-runs\_active-runs.json = []`
- This is the first live proof in this sequence that the tests-node success leak is fixed for the current blocker family.

### 3. The new blocker is now below the workflow blocked classifier
- Latest delegate-session proof from the same rerun:
  - `delegate-live-1777035881868-e38d96`
    - `agent = openai-native`
    - `status = failed`
    - terminal message: `OpenAI API error 404 ... DeploymentNotFound`
  - `delegate-live-1777035883987-96a003`
    - `agent = openai-native`
    - `status = failed`
    - terminal message: `OpenAI API error 404 ... DeploymentNotFound`
- Additional nested delegate-session cleanup leak:
  - `delegate-live-1777035838795-db0049`
    - still `status = active`
    - last message only points to nested child `delegate-live-1777035892605-a6e445`
  - `delegate-live-1777035892605-a6e445`
    - still `status = active`
    - no terminal message persisted
- Re-check ~20 seconds after workflow completion still showed both nested delegate files as `active`.

## Runtime health
- Daemon remained **locked/off**:
  - `node .\cli.mjs --daemon-status --config-dir .bosun --repo-root .`
  - result: `bosun daemon is not running.`
- Latest real workflow run cleaned up correctly at the workflow level even though nested delegate sessions did not:
  - `release-claim.success = true`
  - `release-slot.success = true`
  - `_active-runs.json = []`

## End-to-end task inspection
- Real task inspected end to end again:
  - task: `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - title: `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`
  - PR: none
- Current terminal live state after rerun `dd137...`:
  - tests blocked correctly
  - implementation stayed closed
  - no file changes verified
  - no tests run/passed by a write-capable agent
  - no commit created
  - no PR created
  - no merge

## Throughput
- Recent merged PR throughput is still effectively **0** for this monitor window:
  - `gh pr list --state merged --limit 10 --json ...`
  - latest merged PRs returned were historical (`2026-04-20` and earlier), not new non-monitor completions from this cycle.

## Narrowest unresolved blocker now
**The current narrow blocker is no longer workflow success leakage. The parent workflow now blocks correctly, but delegated `openai-native` child sessions are failing with `DeploymentNotFound` and a nested delegate chain can remain marked active after the parent workflow has already finished.**

### Symptom
- Parent tests node blocks correctly and releases claim/slot.
- Delegated child sessions fail with provider/deployment errors before any actionable output lands.
- A nested delegate chain can remain `active` with no final message after the parent workflow is already done.

### Proof
- Workflow run:
  - `.bosun\workflow-runs\dd137493-a5f5-4789-82c9-d0e7a55cef8d.json`
- Parent tests session:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_dd137493-a5f5-4789-82c9-d0e7a55cef8d_run-agent-tests_turn.json`
  - `status = blocked_by_env`
- Failed delegates:
  - `logs/sessions/delegate-live-1777035881868-e38d96.json`
  - `logs/sessions/delegate-live-1777035883987-96a003.json`
  - both persist `OpenAI API error 404 ... DeploymentNotFound`
- Lingering active delegates:
  - `logs/sessions/delegate-live-1777035838795-db0049.json`
  - `logs/sessions/delegate-live-1777035892605-a6e445.json`

### Code-path suspect list
1. **Provider/deployment selection for delegated `openai-native` sessions**
   - likely in the bridge that maps delegated runtime config to provider deployment names
   - relevant files:
     - `voice/voice-tools.mjs`
     - `agent/provider-session.mjs`
     - `shell/openai-native-adapter.mjs`
     - config/provider selection plumbing used for delegated live sessions
2. **Delegate-session finalization after parent workflow exit**
   - relevant files:
     - `voice/voice-tools.mjs`
     - `infra/session-tracker.mjs`
     - delegate session completion / cancellation hooks in provider-session or launcher paths

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first; do **not** unlock the daemon.
3. Resume from the latest reproduced run:
   - `bosun simulate task restart --json --config-dir .bosun --repo-root .`
4. Follow the delegated provider/deployment failure itself:
   - prove which deployment/config path is producing `DeploymentNotFound` for nested `openai-native` delegates
5. Trace why nested delegate sessions remain `active` after the parent workflow has already failed tests and released claim/slot.
6. Only after a real non-monitor task reaches implementation -> PR -> review/fix -> merge should the daemon be restarted and monitored in parallel.

incident
