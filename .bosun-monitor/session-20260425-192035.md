# Bosun local ops monitor handoff — 2026-04-25 19:20:35

Status: incident

## Startup / notes read
- Read latest three monitor notes before runtime work:
  - `.bosun-monitor/session-20260425-184358.md`
  - `.bosun-monitor/session-20260425-182352.md`
  - `.bosun-monitor/session-20260425-175105.md`

## Open blocker list rebuilt from the newest note
1. **Keep simulator-first validation on the real task path** — still no end-to-end non-monitor merge, so daemon stays locked off.
2. **Validate the push seam with the smallest correct source fix** — the newest note said the next real question was why `push-branch` still failed after a green validation phase.
3. **Do not trust resume modes unless they actually re-run the failed seam** — restart remained justified only if resume/replan still cloned stale state.

## Repo / runtime state
- Source branch:
  - `bosun/codex-self-improvement-loop-commits`
- `package.json` version:
  - `0.43.0`
- Branch vs `origin/main`:
  - `0 197`
  - local source still reflects the latest merged `main`
- Branch vs `origin/bosun/codex-self-improvement-loop-commits`:
  - ahead `9`
- Dirty source tree remains large and pre-existing; unrelated edits were not reverted.
- Full daemon remained intentionally locked off.

## What happened this cycle
### 1. Isolated a new real source bug: Bosun validated the wrong quality gate before push
- New proof from the finished real restart `4b76bcfd-229e-4b0a-a51e-b14ab5f2c38f`:
  - `pre-pr-validation` succeeded with `npm run prepush:check`
  - `push-branch` still failed and its preserved output showed the real managed push path was running the local `.githooks/pre-push` hook instead
- Source path:
  - `workflow/project-detection.mjs`
  - `detectQualityGateCommand(...)`
- Root cause:
  - `qualityGate=auto` preferred package scripts such as `prepush:check` before `.githooks/pre-push`
  - on this repo, that made workflow validation go green on a different command than the one the actual managed git push enforces
- Durable fix landed:
  - `detectQualityGateCommand(...)` now prefers `.githooks/pre-push` when present, before falling back to package scripts
- Targeted regression added:
  - `tests/workflow-task-lifecycle.test.mjs`
  - new node-repo case proves a repo with both `package.json` `prepush:check` and `.githooks/pre-push` resolves `qualityGate` to `bash .githooks/pre-push`

### 2. Validation completed for the new source fix
- Focused validation passed:
  - `node .\node_modules\vitest\vitest.mjs run tests\workflow-task-lifecycle.test.mjs -t "prefers repo hooks for go quality gates instead of node-only defaults|prefers repo hooks for node quality gates even when prepush:check exists"`
- `npm run build` passed.
- Full `npm test` still has no final captured verdict this cycle.

### 3. Re-proved that both resume modes are still stale on the push seam
- New resume runs:
  - `fae11560-c811-4d72-929e-1ffdcfddc2f7`
  - `98c533b2-af31-464e-a542-a5fb89a6f069`
  - `412c6af3-c37f-4734-bd99-b5be1c0d2533` (`resume --mode replan_from_failed`)
- Proof:
  - all completed in under a second
  - all cloned completed `pre-pr-validation` and failed `push-branch` state
  - none re-ran validation or push on the real path
- Conclusion:
  - restart is still the only valid simulator path for consuming push-seam fixes on this task

### 4. Started a fresh real restart after the stale-resume proof
- Restart command:
  - `bosun simulate task restart --json --config-dir .bosun --repo-root .`
- New live run:
  - `59dd1316-8ec6-4039-9e8a-9dc60479d580`
- Runtime proof at handoff:
  - `_active-runs.json` still contains `59dd1316-8ec6-4039-9e8a-9dc60479d580`
  - `.cache/bosun/task-claims.json` shows live claim owner PID `51640`
  - run file `59dd1316-8ec6-4039-9e8a-9dc60479d580.json` has `endedAt = null`
  - completed nodes:
    - `claim-task`
    - `acquire-worktree`
  - current live node:
    - `run-agent-tests = running`
  - active child process:
    - `npm.cmd test -- tests/workspace-*.test.mjs tests/*memory*.test.mjs`
- This restart has **not** reproduced the earlier active-run drift so far:
  - `_active-runs.json` is still populated
  - live claim and run file state are consistent with the running process

## Real task path inspection
- Task:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
- PR:
  - `#482`
  - `OPEN`
  - `MERGEABLE`
  - not draft
  - no review decision
  - head ref still:
    - `22753c9fcfd94e20bb1d1cc1d9f7db6e3ca02179`
- Local task branch from source repo refs:
  - `task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
  - local head:
    - `5da54e11918304655c70369c976cacd25c96fcea`
  - remote head:
    - `22753c9fcfd94e20bb1d1cc1d9f7db6e3ca02179`
  - divergence:
    - `0 7`
- End-to-end state:
  - prior restarts reached implement -> validation -> auto-commit -> push and then blocked
  - review/fix/re-review/merge still unreached
  - simulator gate remains blocked

## Current narrowest blocker
Primary blocker is now:
- validate whether the new restart `59dd1316-8ec6-4039-9e8a-9dc60479d580` reaches `pre-pr-validation` and then `push-branch` with the corrected `qualityGate=auto` resolution
- if push still fails, capture the exact terminal cause from the live run and separate:
  - managed hook failure
  - git push rejection / lease failure
  - command timeout / termination

## Throughput / health
- Simulator gate:
  - blocked
- Daemon:
  - still locked off
- Recent non-monitor throughput:
  - no fresh non-monitor merge landed this cycle
- Target of at least one non-monitor merge per hour:
  - not met

## Validation performed
- Read latest three handoff notes first.
- Refreshed branch, git status, divergence, and `package.json` version.
- Re-checked current PR state for task `#482`.
- Re-ran `bosun simulate task resume` and `bosun simulate task resume --mode replan_from_failed`.
- Inspected preserved outputs from `4b76bcfd-229e-4b0a-a51e-b14ab5f2c38f`.
- Patched `workflow/project-detection.mjs`.
- Added targeted regression in `tests/workflow-task-lifecycle.test.mjs`.
- Ran the focused regression.
- Ran `npm run build`.
- Started and polled fresh restart `59dd1316-8ec6-4039-9e8a-9dc60479d580`.

## Next actions for the next cycle
1. Continue from live restart `59dd1316-8ec6-4039-9e8a-9dc60479d580` first.
2. Watch it through `run-agent-tests -> run-agent-implement -> pre-pr-validation -> push-branch`.
3. If it reaches `push-branch`, inspect whether the corrected quality-gate resolution removed the old false-green seam.
4. If it still fails at push, capture the exact final failure class with proof from the live run file before making another code change.
5. Keep daemon monitoring locked off until a real simulator task completes implementation -> push -> PR -> review/fix -> merge end to end.
