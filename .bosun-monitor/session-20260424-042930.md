status: incident

# Bosun Local Ops Monitor — 2026-04-24 04:29:30

## What changed this cycle
- Landed a durable Windows heavy-runner fix for validation leases.
  - `workflow/heavy-runner-pool.mjs`
    - local-process runner leases now resolve Windows `PATHEXT` command shims before spawn
    - `.cmd` / `.bat` commands are launched through `cmd.exe /d /s /c`
    - direct executables still stay on the non-shell `spawn()` path
  - `tests/workflow-heavy-runner-pool.test.mjs`
    - added a Windows regression proving `runCommandInHeavyRunnerLease({ command: "npm --version" })` succeeds for `local-process`

## Validation
- Passed: `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/workflow-heavy-runner-pool.test.mjs tests/workflow-heavy-runner-integration.test.mjs`
  - result: `2 test files`, `7 tests passed`
- Passed: `npm run build`
- Passed direct runtime probe:
  - `runCommandInHeavyRunnerLease({ command: "npm --version", runtime: "local-process" })`
  - result: `ok=true`, `exitCode=0`, `stdout=11.6.2`
- Full repo validation did **not** complete:
  - `npm test` never reached the suite
  - it stalled during `pretest` at `node tools/generate-demo-defaults.mjs`
  - no heavy-runner failure appeared on that path; this looks unrelated to the heavy-runner patch

## Runtime health / throughput re-check
- Clean-reset Bosun with:
  - `node cli.mjs --terminate --config-dir .bosun --repo-root . --no-update-check`
- Restarted the daemon onto patched source.
  - recent node processes started at `04:23`:
    - `16200`
    - `5004`
- `node cli.mjs task stats --json --config-dir .bosun --repo-root .`
  - `draft=45`
  - `todo=8`
  - `inprogress=26`
  - `inreview=0`
  - `done=104`
  - `blocked=35`
- Recent non-monitor throughput is still incident-level.
  - `gh pr list --state merged --search "base:main merged:>=<last hour>" ...`
  - result: `[]`
  - last-hour merged non-monitor PR count: `0`

## Required end-to-end real task path inspection
- Inspected the full live task path for:
  - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - title: `[s] feat(observability): emit structured logs and metrics for planner-to-task handoff`
- Current task record:
  - status `inprogress`
  - branch `task/97d41516caca-s-feat-observability-emit-structured-logs-and-me`
  - `prUrl = null`
  - `latestRunId = 1c0ce7e2-59ad-4bdd-bb23-c2be2804cce6`
- Current authoritative run-family tail:
  - `86d60fb4-...` — Backend Agent — `paused` — summary `Workflow is ready to continue.`
  - `1c0ce7e2-59ad-4bdd-bb23-c2be2804cce6` — Task Lifecycle — `failed`
    - summary: `Failed at Agent Plan: Invalid string length`
  - `6ecf00dd-d439-4248-ae34-8af69a916289` — Backend Agent — `paused`
- Run detail proof:
  - `1c0ce7e2-...`
    - `run-agent-plan.status = "failed"`
    - `run-agent-plan.attempts = 2`
    - `run-agent-plan.lastError = "Invalid string length"`
    - failed node summary also records `Invalid string length`
  - `6ecf00dd-...`
    - `write-tests.status = "waiting"`
    - `implement.status = "pending"`

## What the runtime proved
- The Windows heavy-runner `spawn npm ENOENT` path is fixed in source and validated directly.
- But the fresh runtime did **not** reach the old validation gate this cycle.
- A newer upstream blocker now fails recovered task families earlier, during agent planning / scheduling:
  - `Invalid string length`
  - `string_above_max_length`
  - then heap pressure / V8 OOM

## Narrowest unresolved blocker
### Symptom
Fresh recovered workflow families are failing in `action.run_agent` planning/scheduling with `Invalid string length`, and the daemon can fall over with heap exhaustion before the run ever reaches build/test validation or PR handoff.

### Proof
- Real task path inspected: `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
- Latest lifecycle run:
  - `1c0ce7e2-59ad-4bdd-bb23-c2be2804cce6`
  - summary: `Failed at Agent Plan: Invalid string length`
- Run detail shows:
  - `run-agent-plan.status = "failed"`
  - `attempts = 2`
  - `lastError = "Invalid string length"`
- Child backend run:
  - `6ecf00dd-d439-4248-ae34-8af69a916289`
  - left paused with `write-tests = waiting`
- `logs/daemon.log` shows broader correlated failures:
  - `schedule-run failed workflow=...: Invalid string length`
  - `node:FAILED auto-fix (action.run_agent) ...: Invalid string length`
  - `node:FAILED run-agent-plan (action.run_agent) ...: Invalid string length`
  - preceding code marker: `"code": "string_above_max_length"`
  - followed by:
    - `FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory`

### Code path
- `workflow/workflow-nodes/actions.mjs`
  - `action.run_agent`
  - `finalPrompt` assembly
  - `runSinglePass(...)`
  - `harnessAgentService.runTask(...)` / `launchEphemeralThread(...)`
- `agent/harness-agent-service.mjs`
  - `frameInteractivePrompt(...)`
  - pool handoff via `execWithRetry` / `launchOrResumeThread` / `launchEphemeralThread`
- Adjacent session/serialization frontier still to pin down:
  - `workflow/harness-session-node.mjs`
  - session-manager / tracing / scheduler persistence around workflow scheduling and run-state recording

## Next start point
1. Start from failed lifecycle run `1c0ce7e2-...`, not the older validation run `40da7eec-...`.
2. Trace where `Invalid string length` is thrown during `action.run_agent` for planner/write-tests flows:
   - prompt assembly
   - session/tracing metadata recording
   - scheduler/run-detail serialization
3. Instrument or log string/object sizes immediately before:
   - `runSinglePass(finalPrompt, ...)`
   - `harnessAgentService.runTask(...)`
   - workflow/session finalization paths
4. Keep the heavy-runner Windows fix in place; only re-test the validation gate after this planning/scheduling OOM blocker is cleared.

incident
