# Bosun Monitor Session 2026-04-23 04:47:54

- Status: incident
- Branch: `bosun/codex-self-improvement-loop-commits`
- Local source vs `origin/main`: current (`git rev-list --left-right --count HEAD...origin/main` => `190 0`)
- Recent non-monitor merge throughput: no `origin/main` merge commits in the last hour

## Open blockers

1. Throughput is still below target: no non-monitor merges landed during this or the prior cycle, so Bosun remains in incident state even though the stale task-row defect is now fixed.
2. Representative replacement run `33f3641d-2fa4-433e-9c64-bacb4286c236` is now the live Task Lifecycle attempt for task `8ed214ec-7eac-4887-8abd-97aa09c61694`, but its latest checkpoint is still `run-agent-tests -> agent.started` at `2026-04-22T18:42:02.589Z`; it needs another cycle to prove forward progress vs. another startup-only stall.
3. `_active-runs.json` read back as `[]` immediately after restart while the workflow engine reported interrupted runs and the task row pointed at `33f3641d-...`; if this repeats, inspect whether the active-run index is lagging or being aggressively cleaned during startup.

## Symptom -> proof -> code path -> fix

### 1. Workflow-owned startup-stalled runs were not reclaimed soon enough

- Symptom: representative stale run `c74d2ebc-1726-4c2f-93bc-ce2116d9536f` stayed pinned at `run-agent-tests -> agent.started` and kept task `8ed214ec-...` in `inprogress`.
- Proof:
  - turn-scoped session `8ed214ec-...:agent:c74d2ebc-...:run-agent-tests:turn` had a single startup event, no edits, no commits, and elapsed well beyond the startup-stall window.
  - execution ledger `c74d2ebc-...` was frozen at `agent.started` with zero tool/spill counters.
- Code path: `task/task-executor.mjs` workflow-owned recovery ranking in `_rankWorkflowEvidenceCandidate(...)`.
- Fix: added startup-stall-specific liveness discrimination in `task/task-executor.mjs` so stale workflow evidence is ignored when the latest checkpoint is still `agent.started`, counters remain zero, and the turn-scoped session never progressed beyond startup.

### 2. Startup cleanup could clear workflow evidence without reconciling the task row immediately

- Symptom: after the first controlled daemon restart, `_active-runs.json` cleared but task `8ed214ec-...` remained `inprogress` on the stale run until later runtime activity.
- Proof:
  - post-restart task row still showed `status=inprogress`, `latestRunId=c74d2ebc-...`.
  - monitor startup logs showed workflow recovery activity while the runtime also logged `task-executor lifecycle delegation enabled — finalization/recovery handled by workflow replacement`.
- Code path: `infra/monitor.mjs` startup recovery ordering around `workflow-history-unstick` and `stale-dispatch-task-poll-unstick`.
- Fix: added `runWorkflowOwnedTaskRecoveryPass(...)` in `infra/monitor.mjs` and invoked it immediately after startup workflow cleanup paths that mutate interrupted-run evidence. This forces a workflow-owned in-progress recovery pass after startup unstick operations.

## Validation

- Focused tests passed:
  - `tests/task-executor.test.mjs`
    - `keeps workflow-owned tasks in progress when latest run detail shows a recent running DAG`
    - `resets workflow-owned tasks when a run is stuck at agent.started with no turn progress`
  - `tests/monitor-workflow-startup-guards.test.mjs`
    - `resumes interrupted workflow runs after monitor services are wired`
    - `kicks schedule-driven workflow polling immediately when workflow lifecycle owns dispatch`
    - `runs task recovery immediately after startup workflow cleanup when lifecycle is delegated`
- `npm run build` passed after the monitor patch.
- Editor diagnostics: no errors in touched files.

## Runtime proof after reload

- Daemon restarted twice in this cycle to load validated fixes:
  - `62396 -> 15980`
  - `15980 -> 57360`
- Latest startup logs after PID `57360`:
  - workflow engine detected interrupted runs `33f3641d-...` and `c74d2ebc-...`
  - `task-executor` logged: `in-progress recovery: resumed 1, reset 0 stale task(s) to todo`
- Representative task row now points to a replacement run instead of the stale original:
  - task `8ed214ec-...`
  - `status: inprogress`
  - `latestRunId: 33f3641d-2fa4-433e-9c64-bacb4286c236`
  - `updatedAt: 2026-04-22 18:42:01`
- Replacement run ledger `33f3641d-...`:
  - `status: running`
  - `parentRunId: c74d2ebc-...`
  - latest checkpoint `run-agent-tests -> agent.started`
  - `updatedAt: 2026-04-22T18:42:02.589Z`

## Git / worktree state

- Current branch still contains latest `origin/main`, but the working tree is dirty and ahead of `origin/bosun/codex-self-improvement-loop-commits` by 2 commits.
- Many tracked modifications pre-existed in the repo; this cycle added monitor/runtime recovery changes in `infra/monitor.mjs`, `task/task-executor.mjs`, and targeted tests.

## Narrowest next starting point

1. Re-check representative run `33f3641d-2fa4-433e-9c64-bacb4286c236` first.
2. If it advances beyond `run-agent-tests -> agent.started`, follow it through to dispatch/PR throughput and measure whether Bosun is back to real non-monitor progress.
3. If it is still pinned after the startup-stall window, inspect why the replacement run has no turn-scoped session progress after the restart even though startup recovery resumed it.