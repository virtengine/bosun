# Bosun Monitor Session 2026-04-23 20:38:30

- Status: incident
- Branch: `bosun/codex-self-improvement-loop-commits`
- Local source vs `origin/main`: current (`ahead=191 behind=0`)
- Recent non-monitor merge throughput: no `origin/main` merge commits in the last hour

## Open blockers

1. Throughput is still below target: the live backlog is effectively stalled with `todo=4` (all diagnostics), `blocked=35`, `inprogress=0`, and Task Batch Processor still reporting `0/0 succeeded`.
2. Representative real task `8ed214ec-7eac-4887-8abd-97aa09c61694` is still blocked on pre-fix evidence (`latestRunId=28150d83-0420-41b3-839d-f6fcd530bb00`, `updatedAt=2026-04-22T20:28:24.654Z`), so the new source fix has not yet been exercised by a fresh Task Lifecycle retry.
3. The daemon still logs `Codex config drift detected (runtime is read-only; run node cli.mjs --setup to apply)` on startup, so the global runtime-mutation/config issue remains unresolved even after the tool-bridge fix.
4. Full validation is still blocked by an unrelated repo failure: `tests/provider-auth-settings.test.mjs` expects credential lifecycle `status: "authenticated"` but the current code reports `status: "configured"` for credential-store API keys.

## Symptom -> proof -> code path -> fix or blocker

### 1. Workflow/openai-native task sessions were inheriting voice-only tool approvals

- Symptom: the representative task's later blocked run no longer reported only Codex drift; it showed workflow agent summaries like:
  - `run_workspace_command is denied`
  - `delegate_to_agent requires operator approval`
  - `No direct write/edit path is available`
- Proof:
  - `.bosun/workflow-runs/28150d83-0420-41b3-839d-f6fcd530bb00.json` records `provider: "azure-openai-responses"` and `Executor harness default: sdk=openai-native, provider=azure-openai-responses, model=gpt-5.4`.
  - The same run records workflow-agent summaries under `run-agent-tests` and `run-agent-implement` complaining about `delegate_to_agent` approval and blocked workspace commands.
  - `agent/tool-builtin-catalog.mjs` routed voice-backed tool names through `voice-tools.executeToolCall(...)`.
  - `voice/voice-tools.mjs` rebuilt a **voice** tool registry inside `executeToolCall(...)`, including `VOICE_APPROVAL_REQUIRED_TOOLS`, and defaulted missing context to `surface=sessionType=voice`.
- Code path:
  - `agent/tool-builtin-catalog.mjs` `callVoiceTool(...)`
  - `voice/voice-tools.mjs` `executeToolCall(...)`, `buildVoiceToolRuntimeContext(...)`, and `buildVoiceToolRegistryDefinitions(...)`
- Fix:
  - Patched `voice/voice-tools.mjs` so non-voice callers preserve a non-voice surface/session type and skip voice-only approval defaults when the context is not voice-like.
  - Patched `agent/tool-builtin-catalog.mjs` so built-in bridged tool calls default to a non-voice tool context (`surface=bosun-builtin`, `sessionType=tool-bridge`) instead of silently falling back to voice semantics.
  - Added regressions in `tests/voice-tools.test.mjs` covering:
    - direct `workflow-agent` `delegate_to_agent` calls without manual approval
    - built-in bridged `delegate_to_agent` calls defaulting to non-voice context

### 2. Runtime is still incident-level because there is no fresh real task attempt on the patched path

- Symptom: after reloading the daemon with the fix, PR Watchdog and Task Batch resumed, but Task Batch still reports `0/0` and no real Task Lifecycle run started.
- Proof:
  - Controlled daemon restart changed PID `67408 -> 76568`.
  - Post-restart monitor log shows:
    - startup workflow wiring succeeded
    - Task Batch cycles at `2026-04-23T10:36:37Z` and `2026-04-23T10:37:40Z` still logged `Task batch completed: 0/0 succeeded (0 failed)`
  - `.bosun/workflow-runs/_active-runs.json` after the next poll only showed the PR Watchdog run, not a task run.
  - `bosun task stats --json` now reports `todo=4`, `blocked=35`, `inprogress=0`, `done=104`.
- Fix or blocker: not fixed this cycle. The source path is patched and reloaded, but there is no current non-diagnostic `todo` task to prove end-to-end recovery, and the representative real task remains blocked on old evidence.

## Validation

- Focused regression passed:
  - `npx vitest run tests\\voice-tools.test.mjs --reporter verbose`
- `npm run build` passed.
- `npm test` still fails on an unrelated existing repo issue:
  - `tests/provider-auth-settings.test.mjs`
  - expected credential lifecycle `status: "authenticated"`
  - actual `status: "configured"`
- `npm run prepush:check` was started but did not complete within the monitor cycle window; no final result was captured here.

## Runtime proof and workflow behavior

- Active sink paths are still advancing:
  - `.bosun/logs/monitor.log`
  - `.bosun/workflow-runs/_active-runs.json`
  - `.bosun/workflow-runs/index.json`
- Inspected one real task path end to end:
  - task row `8ed214ec-7eac-4887-8abd-97aa09c61694`
  - prior run `6c69ca5f-2d4f-4ceb-b46a-ef33a3003507`
  - latest blocked run `28150d83-0420-41b3-839d-f6fcd530bb00`
  - monitor log drift/no-throughput evidence
- Recent workflow runs behaved mechanically but not productively:
  - PR Watchdog keeps cycling normally
  - Task Batch keeps polling and completing `0/0`
  - no fresh Task Lifecycle run has exercised the patched tool bridge

## Git / worktree state

- Local source now contains the latest fetched `origin/main`.
- The branch remains ahead of `origin/bosun/codex-self-improvement-loop-commits`.
- Working tree remains broadly dirty from substantial pre-existing edits; this cycle changed:
  - `voice/voice-tools.mjs`
  - `agent/tool-builtin-catalog.mjs`
  - `tests/voice-tools.test.mjs`

## Narrowest next starting point

1. Re-check the first fresh **real** Task Lifecycle attempt after this daemon reload, not just scheduler liveness.
2. If no real task is retried automatically, inspect why the backlog only exposes diagnostics in `todo` while 35 real tasks remain blocked.
3. When a fresh openai-native task run appears, inspect its `run-agent-*` output first and confirm `delegate_to_agent` is no longer approval-gated before doing any more queue cleanup.
