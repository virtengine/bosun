incident

# Bosun Local Ops Monitor — 2026-04-25 07:21:06 local

## Startup / scope
- Read the latest three monitor notes before touching simulator state:
  - `.bosun-monitor/session-20260425-063312.md`
  - `.bosun-monitor/session-20260425-055646.md`
  - `.bosun-monitor/session-20260425-050238.md`
- Rebuilt the blocker list from `session-20260425-063312.md`.
- Kept the daemon locked/off and stayed simulator-first.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Current open-blocker list
1. **Simulator gate still blocked** — no real non-monitor task has reached PR creation -> review/fix -> merge.
2. **Simulator branch targeting is now fixed and live-proven** — task-lifecycle runs now materialize `defaultTargetBranch = bosun/codex-self-improvement-loop-commits`.
3. **Explicit task payload override is now fixed and live-proven** — simulator direct-task input now rewrites stale default-shaped `task.baseBranch = origin/main` to the simulator target branch.
4. **Worktree reuse now rebases onto local base branches** — reused task worktrees no longer skip local-branch base sync just because the base is not `origin/*`.
5. **New narrowest blocker: reused task branch contamination** — the current task branch still carries old unrelated history, including `shell/codex-sdk-import.mjs`, so validation keeps failing even after the base-branch path was corrected.
6. **Throughput remains incident-level** — still no non-monitor merge landed.

## Durable source fixes landed this cycle
1. **Simulator task lifecycle now defaults to the current local source branch when config is still on the `origin/main` fallback**
   - File:
     - `task/task-simulate-cli.mjs`
   - Change:
     - added `resolveTaskSimulationDefaultTargetBranch(...)`
     - simulator runtime now overrides `branchRouting.defaultBranch` / `defaultTargetBranch`
     - installed simulator task-lifecycle workflow variables are updated to the resolved simulator branch
   - Proof:
     - fresh run data recorded:
       - `defaultTargetBranch = "bosun/codex-self-improvement-loop-commits"`

2. **Simulator explicit-task input now overrides stale default-shaped `task.baseBranch`**
   - File:
     - `task/task-simulate-cli.mjs`
   - Change:
     - direct task input now rewrites `baseBranch` / `base_branch` from `origin/main` to the simulator target branch when the simulator is intentionally targeting the current local branch
   - Why:
     - the trigger copied `task.baseBranch` into `ctx.data.baseBranch`, which previously overrode the new simulator default and kept routing worktrees back to `origin/main`

3. **`action.acquire_worktree` now syncs reused worktrees onto local base branches, not just `origin/*`**
   - File:
     - `workflow/workflow-nodes/actions.mjs`
   - Change:
     - extracted reusable-worktree base sync into a local helper
     - existing worktree reuse and attached-branch reuse now attempt `git rebase <baseBranch>` when the requested base branch is a local branch ref
   - Why:
     - the old reuse path only did `pull --rebase origin ...` for `origin/*` bases and skipped local-base branch migration entirely

## Validation completed this cycle
- `npx vitest run tests\task-simulate-cli.test.mjs` passed.
- `npx vitest run tests\workflow-task-lifecycle.test.mjs -t "rebases a reused task worktree onto a local base branch override"` passed.
- `npm run build` passed after both source fixes.

## Fresh simulator evidence
- First post-fix rerun:
  - `e4d80cf1-b1b7-4c24-ae2e-33476ed0924c`
  - proved:
    - `defaultTargetBranch = "bosun/codex-self-improvement-loop-commits"`
    - but `task.baseBranch = "origin/main"` still won until the explicit-task override was patched
- Second rerun after task payload override:
  - `dc0d6378-1178-4de1-ab8e-8ddc67e2b752`
  - proved:
    - `defaultTargetBranch = "bosun/codex-self-improvement-loop-commits"`
    - `baseBranch = "bosun/codex-self-improvement-loop-commits"`
    - but reused worktree sync still skipped local-base rebasing before the `action.acquire_worktree` patch
- Current rerun after the worktree reuse patch:
  - `3e8f18d6-9a05-4924-b88c-9c453a732840`
  - final state at handoff:
    - ended cleanly with `errorCount = 0`
    - `_active-runs.json = []`
    - retry validation failed again
    - `notify-validation-blocked` degraded gracefully with `Telegram service not available`
    - persisted task status is back to `blocked`

## What the simulator proved this cycle
### 1. The base-branch policy bug is fixed
- Run `3e8f18d6-9a05-4924-b88c-9c453a732840` currently records:
  - `defaultTargetBranch = "bosun/codex-self-improvement-loop-commits"`
  - `baseBranch = "bosun/codex-self-improvement-loop-commits"`
- `acquire-worktree` logs confirm the corrected path:
  - `Using provided task context for f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `Reusing worktree: C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-f82209dfe236-666ab4d38c`

### 2. The remaining blocker is branch contamination inside the reused task branch
- The current base branch ref already contains the compatibility fix:
  - `git show bosun/codex-self-improvement-loop-commits:shell/codex-sdk-import.mjs`
  - still shows the dynamic-import version
- The task worktree `HEAD` still shows:
  - `export { Codex } from "@openai/codex-sdk";`
- Task-branch file history proof:
  - `git log --oneline -- shell/codex-sdk-import.mjs`
  - top relevant commit:
    - `9c9a03e3 fix: restore removed bosun features and fix tests`
- Task-vs-base proof:
  - `git diff bosun/codex-self-improvement-loop-commits..HEAD -- shell/codex-sdk-import.mjs`
  - still inverts the dynamic-import file back to:
    - `export { Codex } from "@openai/codex-sdk";`
- Branch contamination scope:
  - `git diff --name-only bosun/codex-self-improvement-loop-commits..HEAD`
  - count:
    - `687`
  - sample includes many unrelated files outside the current task scope

### 3. Real task path still inspected end to end up to the current blocker
- Task:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
- Current parent run:
  - `3e8f18d6-9a05-4924-b88c-9c453a732840`
- Worktree path:
  - `C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-f82209dfe236-666ab4d38c`
- Observed path this cycle:
  - tests completed
  - implementation completed
  - pre-PR validation failed
  - retry validation failed
  - second local autofix pass completed
  - task blocked cleanly again
  - no PR created yet
  - no review/fix/re-review
  - no merge

## Runtime state
- Simulator gate: **blocked**
- Daemon allowed this cycle: **no**
- Active run at handoff:
  - none
- Latest completed run:
  - `3e8f18d6-9a05-4924-b88c-9c453a732840`
- Active-task branch at handoff:
  - `task/f82209dfe236-m-fix-workspace-restore-per-agent-rate-limiting-f`
- Worktree upstream still recorded as:
  - `origin/main`
  - but the run context base branch is now correctly `bosun/codex-self-improvement-loop-commits`
- Persisted task metadata still shows stale linkage:
  - `status = "blocked"`
  - `baseBranch = "origin/main"`
  - `branchName = "task/TASKDIAGLOCA-diagnostics-export"`

## Narrowest unresolved blocker now
### Symptom
- Simulator now sends the correct local base branch into the run and worktree node.
- Validation still fails on `shell\codex-sdk-import.mjs` because the reused task branch itself contains an old unrelated commit that restores the stale named-export file.

### Proof
- Live run:
  - `.bosun\workflow-runs\3e8f18d6-9a05-4924-b88c-9c453a732840.json`
- Corrected run context:
  - `defaultTargetBranch = "bosun/codex-self-improvement-loop-commits"`
  - run data reached `baseBranch = "bosun/codex-self-improvement-loop-commits"` during execution
- Base ref proof:
  - `git show bosun/codex-self-improvement-loop-commits:shell/codex-sdk-import.mjs`
  - dynamic-import version present
- Contaminated task branch proof:
  - `git show HEAD:shell/codex-sdk-import.mjs`
  - stale `export { Codex } ...`
  - `git log bosun/codex-self-improvement-loop-commits..HEAD -- shell/codex-sdk-import.mjs`
  - task branch still carries file history above current base
  - `git diff --name-only bosun/codex-self-improvement-loop-commits..HEAD`
  - `687` changed files

### Code path / blocker
- Source fixes landed this cycle resolved:
  - simulator default target branch selection
  - explicit task payload base-branch override
  - reused-worktree local-base rebasing
- Remaining blocker is likely in task-branch hygiene / lifecycle reuse policy:
  - current task branch reuse preserves a heavily contaminated historical branch
  - persisted task metadata also still points at stale branch/base linkage
  - simulator validation is therefore fighting inherited branch drift, not the intended current task delta

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first and keep the daemon locked/off.
3. Resume from:
   - `.bosun\workflow-runs\3e8f18d6-9a05-4924-b88c-9c453a732840.json`
4. Start from the ended run proof:
   - retry validation failed again on `shell\codex-sdk-import.mjs`
   - the run ended cleanly and re-blocked the task
5. Trace the narrowest durable hygiene fix:
   - determine when reused task branches should be discarded/recreated from the corrected base branch
   - or when old unrelated commits (like `9c9a03e3`) should be detected and stripped from a task branch before validation
6. Do **not** unlock daemon-first monitoring until a real task completes validation -> PR -> review/fix -> merge end to end.

incident
