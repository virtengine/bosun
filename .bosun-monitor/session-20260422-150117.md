incident

## Runtime state
- Branch: `bosun/codex-self-improvement-loop-commits`
- Package version: `0.43.0`
- After `git fetch origin main`, local source is current with refreshed `origin/main` (`behind 0`, `ahead 190`)
- Patched daemon restarted successfully; `bosun --daemon-status --config-dir .bosun --repo-root .` now reports PID `7228`
- Active sinks advancing under the restarted daemon:
  - `.bosun/logs/monitor.log`
  - `.bosun/logs/monitor-error.log`
  - `.bosun/workflow-runs/_active-runs.json`
  - `.bosun/workflow-runs/index.json`
  - `.bosun/workflow-runs/35d0ade9-4682-4905-8cd9-7c4ebf967756.json`
  - `.bosun/workflow-runs/c99024cc-5cbd-42a8-b20a-cd4c81522fc2.json`
- Current active runs after restart:
  - `Bosun PR Watchdog` run `c99024cc-5cbd-42a8-b20a-cd4c81522fc2`
  - `Task Lifecycle` run `35d0ade9-4682-4905-8cd9-7c4ebf967756` for task `8ed214ec-7eac-4887-8abd-97aa09c61694`

## Throughput
- Target still not met
- `git log origin/main --merges --since="2 hours ago" --oneline` still produced no output, so no recent non-monitor merge throughput was visible on refreshed `origin/main`
- Real workflows are progressing again after restart, but not yet to merge throughput:
  - `8ed214ec-7eac-4887-8abd-97aa09c61694` is no longer orphaned and now has an active `Task Lifecycle` run
  - `Bosun PR Watchdog` is active under run `c99024cc-5cbd-42a8-b20a-cd4c81522fc2`

## End-to-end task path inspected
- Representative task: `8ed214ec-7eac-4887-8abd-97aa09c61694`
- Pre-restart proof:
  - task detail was `status=inprogress`
  - `blockedReason="Repeated no-output agent starts detected; stopping retries"`
  - `cooldownUntil=2026-04-22T04:05:22.365Z`
  - top-level `latestRunId`, `workflowName`, and `sessionId` were blank
  - `_active-runs.json` had no active run for `8ed...`
  - latest indexed task-lifecycle run `6e9f46d1-02bd-4f9e-a40e-c2c409371864` was `paused`, `resumable=true`, blank `resumeResult`
- Post-restart proof:
  - `_active-runs.json` now includes task-lifecycle run `35d0ade9-4682-4905-8cd9-7c4ebf967756` for `8ed...`
  - run index now shows `35d0...` as `running`
  - older run `6e9f46d1-02bd-4f9e-a40e-c2c409371864` still remains `paused`, `resumable=true`, blank `resumeResult`
  - `monitor.log` shows `Task 8ed... status: inprogress -> todo (source: task-executor-recovery-stale-workflow-claim)` followed by `schedule-run completed workflow=fcd7047d-ba1e-410a-adaa-23455441c3b7 runId=2956ea6e-9b4e-459e-bf94-7e44a04a1d59`
  - live run artifact `35d0...json` carries persisted anti-thrash fields: `consecutiveNoCommits: 0` and `cooldownUntil: 2026-04-22T04:05:22.365Z`
- Remaining inconsistency on this path:
  - `bosun task get 8ed... --json` still reports blank top-level `latestRunId`, `workflowName`, and `sessionId` even while `_active-runs.json` and run index show the live run

## Symptom -> proof -> code path -> fix
- Symptom: startup interrupted-run recovery starved older paused resumable runs whenever `_startupInterruptedRunIds` existed, so unrelated paused task runs such as `6e9f46d1-02bd-4f9e-a40e-c2c409371864` were excluded from the recovery pass
- Proof:
  - pre-fix monitor cycles repeatedly logged `Resuming 2 interrupted run(s)...` while only newer interrupted task families were retried
  - `6e9...` stayed `paused`, `resumable=true`, blank `resumeResult` across restarts
  - focused regression reproduced the startup-cohort-plus-older-paused case and showed the older paused run was never resumed
- Code path:
  - `workflow/workflow-engine.mjs`
  - `resumeInterruptedRuns()` selection of `runs` when `_startupInterruptedRunIds` was populated
- Fix applied:
  - changed `resumeInterruptedRuns()` to keep the current startup cohort first but append the remaining paused resumable runs in the same pass instead of excluding them
  - updated `tests/workflow-engine.test.mjs` regression to `prioritizes the current interrupted-run scan without starving older paused runs`
- Live result after restart:
  - the patched daemon did execute startup recovery on live data: `Resuming 2 interrupted run(s)...`, `Skipped 2 duplicate interrupted run(s) (kept latest per taskId)`, `Resuming run fa2b0999-deee-4a28-b948-4c73cbf0af80 via retryRun(from_scratch) [task_claim_missing_on_resume]...`
  - `8ed...` itself was repaired into an active run by the stale-claim recovery path before the startup duplicate-skip branch finished, so the user-visible orphaned-task symptom is gone even though the old paused row remains in the index

## Validation
- Focused regression: `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/workflow-engine.test.mjs -t "prioritizes the current interrupted-run scan without starving older paused runs"` -> pass
- File diagnostics: no errors in `workflow/workflow-engine.mjs` or `tests/workflow-engine.test.mjs`
- Full suite: `npm test` -> pass
- Build: `npm run build` -> pass
- Pre-push gate: `npm run prepush:check` -> pass
- Live validation on patched source:
  - daemon restart -> pass
  - `bosun --daemon-status --config-dir .bosun --repo-root .` -> running PID `7228`
  - `_active-runs.json` now contains live task `8ed...`
  - run index confirms new running task-lifecycle run `35d0...`
  - `monitor.log` tail confirms both stale-claim recovery and startup interrupted-run recovery executed on this daemon session

## Remaining blocker
- Symptom: duplicate paused interrupted runs are skipped ephemerally but not retired durably
- Proof:
  - current startup logged `Skipped 2 duplicate interrupted run(s) (kept latest per taskId)`
  - old task-lifecycle run `6e9f46d1-02bd-4f9e-a40e-c2c409371864` still remains `paused`, `resumable=true`, blank `resumeResult` in `.bosun/workflow-runs/index.json`
  - this means future restarts can keep rediscovering the same stale duplicate interrupted rows instead of converging the index state
- Related open gap:
  - task detail summary projection for `8ed...` is still stale/blank for `latestRunId`, `workflowName`, and `sessionId` despite the active run being present in `_active-runs.json`

## Narrowest next starting point
- Start in `workflow/workflow-engine.mjs` at the duplicate interrupted-run filtering path immediately after the `latest per taskId` selection.
- Make skipped duplicate paused runs durable by marking them non-resumable with an explicit terminal `resumeResult` (for example a duplicate/superseded result) instead of only logging and skipping them in-memory.
- Add a focused regression that proves duplicate skipped rows stop reappearing across restart scans, then restart the daemon and re-check `8ed...` / `6e9...` lineage plus the task summary projection gap.