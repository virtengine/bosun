# Bosun local ops monitor handoff — 2026-04-25 14:40:47

Status: incident

## Startup / notes read
- Read latest three monitor notes before runtime work:
  - `.bosun-monitor/session-20260425-121008.md`
  - `.bosun-monitor/session-20260425-133252.md`
  - `.bosun-monitor/session-20260425-141525.md`
- Re-read the session plan before touching runtime state.

## Open blocker list rebuilt from the newest note
1. **Push-path simulator gate still unproven** — `resume` and `replan_from_failed` were still replaying the already-blocked push branch.
2. **Need a durable source fix or sharper proof for the push/output seam** before spending more simulator cycles.
3. **Daemon must remain locked off** until a real task reaches merge end to end.

## What changed this cycle
### 1. Durable source fix — preserve push-style node outputs in run detail
Symptom:
- The blocked push replay evidence only persisted:
  - `nodeOutputs["push-branch"] = { branch, branchName }`
- That made it impossible to distinguish:
  - true push failure
  - reduced persisted output
  - resume state that had already lost `pushed/error`

Code path:
- `workflow/workflow-engine.mjs`
  - persisted run-detail compaction
  - `isTaskLikePersistedValue(...)`

Fix:
- Tightened task-like persisted-value detection so generic node outputs are no longer collapsed just because they contain `branch` / `branchName`.
- Push-style outputs now keep fields like:
  - `pushed`
  - `success`
  - `error`
  - `remote`

Validation:
- Added regression in:
  - `tests/workflow-engine.test.mjs`
    - `preserves push action flags when completed run detail compaction summarizes node outputs`
- Focused workflow-engine regressions passed.

### 2. Durable source fix — provider auth lifecycle expectation
Symptom:
- Source checkout still expected:
  - `status: "authenticated"`
- Runtime currently returns:
  - `status: "configured"`

Code path:
- `tests/provider-auth-settings.test.mjs`

Fix:
- Updated the source expectation from `authenticated` to `configured`.

Validation:
- Focused provider auth regression passed in source.
- The same focused test also passed in the task worktree copy used by the real simulator task.

## Resume-first validation this cycle
### `bosun simulate task resume`
- Command:
  - `bosun simulate task resume --json --config-dir .bosun --repo-root .`
- New run:
  - `79417157-9a24-44f7-8828-037d5d386d6a`
- Result:
  - still completed immediately on the blocked branch
  - `push-branch = completed`
  - `push-ok = completed`
  - `set-blocked-push-failed = completed`
  - `create-pr = skipped`
  - `handoff-pr-progressor = skipped`
- Persisted `push-branch` output was still only `{ branch, branchName }` because this resume path reused already-compacted prior state.

### `bosun simulate task resume --mode replan_from_failed`
- Command:
  - `bosun simulate task resume --mode replan_from_failed --json --config-dir .bosun --repo-root .`
- New run:
  - `d00e8b01-132b-4fe5-a872-1dd7961d70b6`
- Result:
  - same no-op replay of the blocked push branch
  - did not reopen `create-pr` / `handoff-pr-progressor`

Conclusion:
- Resume-first was exhausted again.
- A full restart was required to re-execute meaningful work against the fixed source state.

## Fresh simulator restart (real task path rechecked)
### Restart command
- `bosun simulate task restart --json --config-dir .bosun --repo-root .`

### New real run
- `82d2827b-92f1-41ae-939f-25a00bd6125a`

### What the restart now proves
- It is a real fresh lifecycle run, not a blocked resume replay:
  - present in `.bosun/workflow-runs/_active-runs.json`
  - task id `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - worktree acquired fresh:
    - `created = true`
    - `worktreePath = C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-f82209dfe236-666ab4d38c`
  - new task branch head recorded:
    - `record-head = 64ddb3165aaeec40360d45f82bdf909d8b32ecd9`
- Real task path inspected through:
  - claim
  - slot allocation
  - worktree acquire
  - record-head
  - prompt build
  - `run-agent-plan`
  - `run-agent-tests`
  - `tests-agent-ok`
  - `run-agent-implement` startup

### Test phase behavior
- `run-agent-tests` completed and `tests-agent-ok` completed.
- The persisted output shows a success-shaped harness session and a delegated/live-session path rather than a direct inline test transcript.
- Even so, this fresh restart did materially advance beyond the stale blocked push replay.

## Current narrowest blocker (new)
### Symptom
- The fresh restart is now blocked at:
  - `run-agent-implement`
- Implement session log shows:
  - `Missing environment variable: AZURE_OPENAI_API_KEY.`
  - `Turn failed: Missing environment variable: AZURE_OPENAI_API_KEY.`
- The workflow currently shows:
  - `run-agent-implement = waiting`

### Proof
- Session log:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_82d2827b-92f1-41ae-939f-25a00bd6125a_run-agent-implement_turn.json`
- Workflow run evidence:
  - `.bosun/workflow-runs/82d2827b-92f1-41ae-939f-25a00bd6125a.json`
- Persisted executor selection for implement:
  - `sdk = openai-native`
  - `provider = azure-openai-responses`
  - `endpoint = https://open-ai-foundry-us-resource.openai.azure.com/openai/v1`
  - `authMode = apiKey`
- Direct provider-runtime probe in source showed no usable Azure credential on the default runtime path:
  - `apiKeyPresent = false`
  - `oauthTokenPresent = false`

### Code path / likely blocker
- `agent/provider-kernel.mjs`
  - runtime/provider selection for `resolve-executor`
- `agent/providers/_shared.mjs`
  - session config credential propagation
- `shell/auth-resolver.mjs`
  - Azure/OpenAI credential resolution for native adapters
- `shell/openai-native-adapter.mjs`
  - Azure request setup / auth use

Current read:
- The live task is no longer blocked on push replay first.
- It is now blocked earlier on provider runtime auth for implement.
- Need to determine whether this is:
  1. a remaining source bug in runtime credential propagation / fallback, or
  2. a precise external environment/config gap (Azure key absent for the selected executor).

## PR / throughput / daemon state
- PR `#482` remains:
  - `OPEN`
  - `MERGEABLE`
  - `CLEAN`
  - no reviews
  - no merge
- Recent merged PR throughput is still bot-only:
  - latest merged PRs are Dependabot
  - no fresh non-monitor merge landed this cycle
- Daemon remains intentionally locked off.

## Real task path inspected end to end this cycle
Inspected task `f82209df-e236-4a63-9ce8-67122a1ca96b` across both historical and fresh live evidence:
- historical path already proven from prior cycles:
  - implement
  - validation/autofix
  - push
  - PR creation
  - in-review handoff
- fresh live restart this cycle:
  - claim
  - worktree acquire
  - plan
  - tests
  - implement startup
  - provider-auth failure

Still **not reached** this cycle:
- fresh validation
- fresh push
- fresh PR reuse / creation
- fresh review/fix
- merge

## Git / source state
- Source branch:
  - `bosun/codex-self-improvement-loop-commits`
- `package.json` version:
  - `0.43.0`
- Dirty source tree remains large and pre-existing; only these source files were changed this cycle:
  - `workflow/workflow-engine.mjs`
  - `tests/workflow-engine.test.mjs`
  - `tests/provider-auth-settings.test.mjs`

## Exact next steps
1. Continue from the new narrowest blocker:
   - provider/runtime auth selection for `run-agent-implement`
2. Prove whether the selected Azure executor should already have a bound credential:
   - inspect `agent/provider-kernel.mjs`
   - inspect `agent/providers/_shared.mjs`
   - inspect `shell/auth-resolver.mjs`
3. If there is a source bug, land the smallest runtime/auth fix and validate it with focused provider/native-shell tests.
4. Then continue simulator validation:
   - use `bosun simulate task resume` first if the run becomes resumable
   - otherwise restart only if the full pipeline must rerun
5. Keep daemon monitoring locked off until a real task reaches merge end to end.

incident
