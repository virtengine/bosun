status: incident

# Bosun Local Ops Monitor — 2026-04-24 05:42:10

## Open blockers
- Non-monitor merged PR throughput remains `0` in the last hour.
- Fresh overflow-class failures returned in live runtime before this cycle's patch, so the earlier OpenAI native request-size fix was not sufficient on its own.
- A new launcher-side recovery fix is now landed, but it is not yet fully proven end to end on a real task through validation / PR handoff.

## What changed this cycle
- Narrowed the live blocker from “stalled historical resume family” to a more precise runtime failure:
  - backend retry run `bfbfaa72-23d5-4485-8ca6-a3469cb0b3b4` for task `97d41516-caca-4fa0-9175-dbe6ad4ce01d` did not merely freeze
  - it failed at `write-tests` with fresh `Invalid string length`
- Landed a small durable source fix in `agent/agent-launcher.mjs` so overflow-class agent failures invalidate the persistent thread before retrying.

## Symptom -> proof -> code path -> fix
- Symptom:
  - fresh runtime produced:
    - OpenAI 400 `Invalid 'input[5].content[0].text': string too long`
    - `code: "string_above_max_length"`
    - multiple `schedule-run failed ...: Invalid string length`
    - monitor uncaught `RangeError: Invalid string length`
- Proof:
  - fresh daemon evidence around the failure window:
    - `input[5].content[0].text` length `18356850`
    - `workflow=fcd7047d-ba1e-410a-adaa-23455441c3b7` and sibling workflows failed in the same burst
  - real task path inspected end to end:
    - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
    - latest run `bfbfaa72-23d5-4485-8ca6-a3469cb0b3b4`
    - status `failed`
    - summary `Failed at Write Tests First: Invalid string length`
    - node input prompt for the failing `write-tests` step was small, so the oversized item was not coming from the current prompt text
  - this points to retry/session carry-forward state rather than the per-node prompt template
- Code path:
  - `agent/agent-launcher.mjs`
    - `isContextOverflowError(...)`
    - `execWithRetry(...)`
    - persistent thread reuse through `launchOrResumeThread(...)`
- Fix:
  - `isContextOverflowError(...)` now treats `Invalid string length` and `string too long` as overflow-class failures
  - `execWithRetry(...)` now calls `forceNewThread(taskKey, ...)` before retrying an overflow failure so the next attempt starts with a fresh persistent thread instead of resuming poisoned history

## Files changed
- `agent/agent-launcher.mjs`
- `tests/agent-launcher-overflow.test.mjs`

## Validation
- Passed:
  - `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/agent-launcher-overflow.test.mjs tests/agent-harness-foundation.test.mjs tests/agent-pool-node-warning-suppression.node.test.mjs tests/agent-pool-monitor-thread-refresh-clamp.node.test.mjs`
  - `npm run build`
- Attempted:
  - `npm test`
  - still stalled in `pretest` at `node tools/generate-demo-defaults.mjs`

## Runtime re-check after restart onto the patch
- Restarted daemon successfully:
  - previous PIDs terminated: `17576`, `29976`
  - new daemon PID: `23476`
- Active sink paths advancing:
  - `logs/daemon.log` -> `2026-04-24 05:40:42`
  - `.bosun/workflow-runs/index.json` -> `2026-04-24 05:40:37`
  - `.bosun/workflow-runs/_active-runs.json` -> `2026-04-24 05:40:37`
  - `.bosun/.cache/kanban-state.json` -> `2026-04-24 05:40:38`
- Immediate newest daemon tail after restart showed:
  - fresh `run-agent-plan` starts
  - fresh `write-tests` starts
  - no new `Invalid string length`
  - no new `string_above_max_length`
  - no new `force-invalidating thread` line yet in the immediate window

## End-to-end task inspection
- Historical failing path re-checked:
  - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - status `inprogress`
  - `latestRunId = bfbfaa72-23d5-4485-8ca6-a3469cb0b3b4`
  - no PR
  - latest run still points at the pre-patch failure at `write-tests`
- Fresh comparison paths after restart:
  - task `aab1550f-1980-4c00-be2a-ae047b0d457c`
    - status `inprogress`
    - `latestRunId = 1f1286a9-21eb-4e96-9245-44f6adf7d961`
  - task `3be828a2-d3e5-4225-b1f0-5d28905a865e`
    - status `inprogress`
    - `latestRunId = 16f98fa2-adf5-4348-8e76-0333bda11b9e`
- Interpretation:
  - fresh task families are moving again on the patched daemon
  - the previously failing `97d...` family has not yet produced a new post-patch descendant run, so the launcher fix is not yet fully proven on that exact path

## Throughput / health
- `bosun task stats --json --config-dir .bosun --repo-root .`
  - before restart in this cycle: `draft=45 todo=8 inprogress=33 done=104 blocked=35`
- last-hour merged non-monitor PR count: `0`

## Git / working tree
- Working tree is dirty beyond this cycle; do not disturb unrelated edits.
- This cycle specifically added:
  - `tests/agent-launcher-overflow.test.mjs`
  - overflow-recovery edits in `agent/agent-launcher.mjs`

## Narrowest next handoff
1. Stay on the patched daemon and watch for the first fresh overflow-class retry.
2. Prove whether the new `forceNewThread(...)` path fires and clears the retry on a real task path.
3. Re-check task `97d41516-...` for a fresh post-patch descendant run beyond `bfbfaa72-...`.
4. If overflow still recurs without a fresh-thread retry, inspect whether the remaining failing path bypasses `execWithRetry(...)` or uses `continueSession(...)` directly.

incident
