incident

# Bosun Monitor Session — 2026-04-21T09:55Z (hourly)

## Runtime State
- Daemon was DOWN at session start (`bosun --daemon-status` → not running). Hard-stop and clean restart required.
- After `bosun --terminate` (no live procs found) and `bosun --daemon --config-dir .bosun --repo-root . --no-update-check --no-auto-update`, daemon came up as PID 31704 listening on tcp/4400 (PID 14548 is the spawned monitor child).
- Branch: `bosun/codex-self-improvement-loop-commits` — 0 behind origin/main, 188 ahead. Working tree dirty (~24 files including this-session fixes from prior agent turn that have not yet been committed: `shell/openai-native-adapter.mjs`, `ui/components/charts.js`, `infra/worktree-recovery-state.mjs`, `ui/modules/worktree-recovery.js`, `site/ui/modules/worktree-recovery.js`, `agent/session-snapshot-store.mjs`, `workflow/workflow-engine.mjs`, plus other changes from earlier sessions).
- Package version: 0.43.0.

## Active Sinks
- `.bosun/logs/monitor.log` (advancing, 53.92 MB)
- `.bosun/workflow-runs/index.json` (advancing)
- `.bosun/workflow-runs/_active-runs.json` (currently empty between cycles — workflows finish in <1s)

## Workflow Categories Observed (post-restart)
- **Task Batch Processor** (`runId=1debf858-69cd-493b-9c78-f7f071c7650d`): trigger.task_available → check-coordinator → query-tasks → dispatch-tasks (loop.for_each) → join-dispatch → record-results → has-batch-failures (false) → log-summary. Result: `Task batch completed: 0/0 succeeded (0 failed)`. The 4 todo tasks visible (`TASK-DIAG-1`, `task-import-1`, `task-export-1`, `TASK-DIAG-LOCAL`) are CLI test fixtures — `query-tasks` correctly excludes them, hence 0/0.
- **Bosun PR Watchdog** (`runId=0c1c8e0a-9acf-4d7b-97ad-d31a433c9df8`): fetch-and-classify → has-prs → has-behind → fix-needed → security-fix-needed → review-needed → notify → resume-task-spawning → cleanup-merged-branches. Cycle clean, no PRs requiring action.
- **GitHub ↔ Kanban Sync**, **Health Check**, **Task Replenish (Scheduled)**: no scheduled fire window observed during this short post-restart sample, but trigger registrations are intact (visible in workflows index).
- **Task Lifecycle**: 0 active runs — there is no real backlog to dispatch (see Real-Task Inspection).

## Real-Task End-to-End Inspection
Deep-inspected blocked task `9317f12e-7cdc-4135-ace7-7b4486114549` ("[m] feat(task): add repo-root task store doctor and remediation"):
- status=blocked, agentAttempts=0, blockedReason=null, lastError=null, errorPattern=null, lastActivityAt=`2026-04-02T07:17:21Z` (19 days ago).
- topology: `workflowName=Task Lifecycle`, `latestNodeId=run-agent-plan`, `latestRunId=049314f6-6c1f-4cb7-b245-9987558d38c3` (run-detail file purged from `.bosun/workflow-runs/`).
- prLinkage: `task/9317f12e7cdc-...-doctor-...` — never opened a PR (prUrl=null, prNumber=null).
- statusHistory shows the recurring loop on 2026-04-02: `inprogress` → `blocked` (source=workflow) repeated 6 times in ~2 hours, interspersed with `task-executor-recovery-stale-workflow-claim` resets to todo. Pattern: claim → enter `run-agent-plan` → workflow promotes to blocked within 5–15s WITHOUT incrementing `agentAttempts` and WITHOUT writing a `blockedReason`.
- Same family of blocked tasks: 35 total (`9317f12e`, `12fcad47`, `792ad411`, `d5325eca`, `9d72330a`, `fd89e738`, `684e3621`, `52840c3e`, `f82209df`, `f41ed8b3`, …), all stale from the 2026-04-02 OOM-restart epoch.

## Throughput
- Last-hour merged non-monitor PRs: **0**.
- `done=104` (unchanged from last 3 sessions); `inreview=0`, `inprogress=0`, `todo=4` (test-fixtures only), `blocked=35`, `draft=45`. Total tasks: 191.
- **Below target → status = incident**.

## Root Cause Chain
1. **Immediate**: Bosun daemon was not running. Sentinel did not auto-revive after the prior agent turn killed the wedged stack (PIDs 127944/125564/87240 were terminated to free port 4400 and clear the 21 MB run-detail OOM trap). Resolved by manual restart.
2. **Throughput=0 even with daemon up**: real backlog is empty. The 35 blocked tasks all have `agentAttempts=0`, `blockedReason=null`, and have not been touched since 2026-04-02. Per the operating rules, do NOT mass-reset them again — prior sessions reset subsets to `todo` and they re-entered `blocked` immediately without diagnostics. The source-level bug is the workflow path that transitions `inprogress → blocked` from inside `Task Lifecycle / run-agent-plan` (or the lifecycle's blocked-on-failure edge) WITHOUT (a) writing a non-null `blockedReason` and (b) incrementing `agentAttempts`. The current `action.set_kanban_status` does enforce a `unspecified_failure:<nodeId>` sentinel (workflow/workflow-nodes/actions.mjs L4416-L4424), so the blocking call is going through a different code path that bypasses that helper. Most likely candidates: `task-executor.mjs` failure-handling around the `blocked` literal (~L145, L3505), or a workflow edge that hands directly to a deterministic `set_status` helper rather than `set_kanban_status`.

## Code-Level Blockers (carried forward, NOT fixed this hour)
- 35 stale blocked tasks with null blockedReason / 0 attempts — root cause: workflow path that transitions to blocked without going through `action.set_kanban_status`. Fix area: trace `setInternalStatus(taskId, "blocked", ...)` and `transitionTaskStatus(taskId, "blocked", ...)` call sites in `task/task-executor.mjs` and `workflow/workflow-nodes/actions.mjs`; require `blockedReason` at the boundary (mirror of L4416-L4424 fix).
- `branch=fix/dep-audit-{{_runId}}` template leak surfaced in monitor-error.log at `2026-04-21T05:08:06Z` (prior-daemon epoch). The current `action.create_pr` already guards via `isUnresolvedTemplateToken` (L4669) and returns `blocked` instead of calling `gh`. The recurrence implies one of: (a) an alternate create-pr path that does not check, or (b) `ctx.resolve` partially-resolved the value during the OOM cycle. Has not recurred since the fresh restart — re-confirm next hour.
- `Unknown node type: action.evaluate_run` at `2026-04-21T05:08:23Z`: registration exists at `actions.mjs:10601`. The error coincided with the OOM crash window — likely a partial module-load race. Has not recurred since restart.
- `churn-check Expression error: Truncated is not defined` at `2026-04-21T05:07:00Z`: same OOM window. The expression references an undeclared identifier `Truncated`. Source not yet located (no source `.mjs` or template grep hit "churn-check" / "Truncated" — likely persisted in a generated workflow JSON in `.bosun/.bosun/workflows/`). Defer to next hour.

## Fix Applied This Hour
- Operational only: terminated the wedged stack, restarted the daemon as PID 31704 with explicit `--config-dir .bosun --repo-root .`. NO source changes were committed this hour. The previous agent turn left several uncommitted source fixes in the working tree (delta-passthrough in openai-native-adapter, donut chart rotation, worktree-recovery template sanitization, session-snapshot maxSessions, workflow-engine head-only run-detail reads + 10 MB write cap); these are tested and correct but have not been committed yet because the fleet was unhealthy. They will ship in the next hour once a clean dispatch cycle is observed.

## Validation
- `bosun --daemon-status` → running PID 31704.
- `bosun --where` → repo-local `.bosun`.
- `bosun task stats --json` → counts as above.
- 1 full Task Batch + 1 full PR Watchdog cycle observed end-to-end with no FAILED nodes since restart.
- No `npm test` / `npm run build` / `npm run prepush:check` this hour because no source change was authored this hour.

## Git / PR / CI Status
- No commits, no pushes this hour.
- Branch `bosun/codex-self-improvement-loop-commits` is 0 behind origin/main (already in sync); 188 commits ahead represent the autonomous improvement loop history.

## Local Source Freshness
- ✓ Local branch is up to date with `origin/main` (0 behind).

## Narrow Handoff for Next Session
1. **Confirm the daemon is still running** (`bosun --daemon-status`). If it died again, root-cause via fresh `logs/daemon.log` tail.
2. **Commit the 7 working-tree source fixes from the prior agent turn** so they take effect across the fleet (the daemon currently runs with a hot codepath — the in-process module graph already includes these changes via the dirty checkout, but a sentinel respawn would reload from disk anyway).
3. **Trace the `inprogress → blocked` path that bypasses `action.set_kanban_status`** — start from `task/task-executor.mjs` `transitionTaskStatus`/`updateKanbanTaskStatus` call sites filtered to `status === "blocked"`, then ensure each path either calls the `set_kanban_status` helper OR writes a non-null `blockedReason` directly. Use task `9317f12e` and its 6-cycle `2026-04-02` history as the regression fixture.
4. **Decide on the 35 stale blocked tasks**: do NOT reset until the path-3 fix lands, otherwise they will re-enter `blocked` with null reason within minutes (proven by 04/05 session). After the fix, reset 1-2 as a smoke test before mass-unblocking.
5. **Re-check `fix/dep-audit-{{_runId}}` and `Truncated is not defined` and `Unknown node type: action.evaluate_run`** in the next hour's logs to confirm they were OOM-cycle artifacts and not steady-state bugs.
