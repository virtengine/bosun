# Bosun monitor handoff - 2026-04-23 22:50:27

Status: incident

## Summary
- No new source patch landed this cycle. I re-ran the required startup checks, finished another validation attempt, and traced the narrowest live blocker forward from the newest note instead of repeating cleanup.
- The system is more alive than the earlier flatline:
  - sink paths are advancing
  - `Task Replenish`, `Task Planner`, `Task Batch Processor`, and `Bosun PR Watchdog` are completing again
  - fresh `Task Lifecycle` runs are happening for real tasks
- But Bosun is still incident-level because there is still no non-monitor merge throughput, active Backend Agent task execution is not reaching PR creation, and the newest live failure mode has shifted into paused/no-output delegated sessions.

## Open blockers
1. **Throughput target still missed** — no confirmed non-monitor merge this cycle, and active real tasks are not reaching PR handoff.
2. **Current freshest task path degrades inside Backend Agent** — task `71f1bbbc-6b44-431b-ad58-5fab70d8184c` is still `inprogress`, but its Backend Agent run `cb954779-fb18-4013-9ab5-7cfa2647d1b0` surfaces a background session `voice-live-1776947741295-cbb409` with `no_output`, and the workflow index now marks that run `paused`.
3. **Codex fallback is still unusable**, but the failure is now proven to be credential/config related rather than a stdin transport bug.
4. **Older representative blocked task remains stale** — `8ed214ec-7eac-4887-8abd-97aa09c61694` is still `blocked` with `repeated_no_commit_runs`.

## Symptom -> proof -> code path -> fix or blocker

### 1. Codex fallback is not failing because Bosun cannot pass stdin
- Symptom:
  - Monitor logs still report `Codex Exec exited with code 1: Reading prompt from stdin...`
- Proof:
  - Direct probe with stdin transport succeeded in delivering the prompt:
    - `Get-Content ... | codex exec --skip-git-repo-check --sandbox workspace-write --full-auto -`
  - The real failures were:
    1. default Codex config path:
       - provider = `azure`
       - error = `Missing environment variable: AZURE_OPENAI_API_KEY`
    2. `--ignore-user-config` path:
       - provider = `openai`
       - error = `401 Incorrect API key provided`
    3. `--ignore-user-config` with ambient OpenAI/Azure env vars stripped:
       - same `401 Incorrect API key provided`
       - proves the credential coming from `~/.codex/auth.json` is itself unusable for OpenAI
- Code path:
  - runtime lane: generic `codex` fallback
  - ambient config: `C:\Users\jON\.codex\config.toml`
  - auth source: `C:\Users\jON\.codex\auth.json`
- Blocker:
  - This is not a stdin transport bug in Bosun.
  - Current local Codex credential/config state is externally invalid for the fallback path.

### 2. Azure-native is no longer globally dead, but live workflow behavior is still inconsistent
- Proof:
  - Direct native health probe from current source:
    - `launchEphemeralThread('Reply with exactly OK', ..., { sdk: 'openai-native', disableFallback: true })`
    - result: `success=true`, `output="OK"`
  - Recent monitor log still shows live workflow inconsistency:
    - `primary SDK "codex" failed ...`
    - `fallback SDK "openai-native" failed (Error: OpenAI API error 404: {`
- Interpretation:
  - The old “Azure path is completely dead” incident is no longer fully true.
  - The remaining provider/runtime problem is now narrower: native probes can succeed, but live workflow runs can still hit model/deployment/runtime mismatches.

### 3. Freshest live task path now fails as a paused/no-output delegated session
- Real task path inspected end to end:
  - task `71f1bbbc-6b44-431b-ad58-5fab70d8184c`
  - title: `[s] feat(validation): strict planner payload linting before task creation side effects`
  - status: `inprogress`
  - base branch: `origin/main`
  - no PR
  - latest lifecycle run for this path in active ledger: `d567cd8f-8154-4cd2-ad3d-1cd7ca9bd010`
  - latest Backend Agent child run: `cb954779-fb18-4013-9ab5-7cfa2647d1b0`
- Proof:
  - workflow index reports the latest Backend Agent run as:
    - `status = "paused"`
  - run artifact `cb954779-fb18-4013-9ab5-7cfa2647d1b0.json` shows:
    - `plan-work.summary` returned a structured JSON task plan
    - `write-tests.summary` = `session.turn.complete: Still running. Background session: - \`voice-live-1776947741295-cbb409\` Status: - \`no_output\` so far ...`
  - monitor log shows:
    - `Detected 7 interrupted run(s): ... cb954779-fb18-4013-9ab5-7cfa2647d1b0 ...`
- Code path:
  - workflow handoff path:
    - `workflow/workflow-nodes/actions.mjs` (`runSinglePass`, direct-agent handoff / session finalization)
    - `agent/harness-agent-service.mjs`
  - background session id origin:
    - `voice/voice-tools.mjs` creates `voice-live-*`
    - `voice/voice-action-dispatcher.mjs` also creates `voice-live-*`
- Current blocker:
  - A non-voice Backend Agent execution path is surfacing a voice-style background session id and then stalling on `no_output`.
  - This is now the narrowest code-level symptom family worth tracing next.

### 4. Scheduler/workflow starvation is improved, but not healed enough to downgrade health
- Proof:
  - Current task stats:
    - `draft=45`
    - `todo=8`
    - `inprogress=3`
    - `blocked=35`
  - Current sink paths are advancing:
    - `.bosun\logs\monitor.log`
    - `.bosun\workflow-runs\index.json`
    - `.bosun\workflow-runs\_active-runs.json`
  - Latest major workflow family runs:
    - `Task Replenish (Scheduled)` -> completed
    - `Task Batch Processor` -> completed
    - `Task Planner` -> completed
    - `Bosun PR Watchdog` -> completed
    - `Task Lifecycle` -> completed for fresh task `78b17682-3cdc-4303-bdb7-3f564ede9bd1`
    - `Backend Agent` -> latest run still `paused`
- Interpretation:
  - Bosun is no longer flat or feeder-starved.
  - But it still does not meet the health bar because the active task execution path is not finishing end to end.

## Recurring blockers from prior sessions
- **Resolved / improved**
  - startup workflow starvation: improved
  - auth-dead primary native selection: fixed earlier
  - provider credential propagation into native runtime: fixed earlier
  - Codex auth.json schema detection: fixed earlier this session family
  - native provider path completely dead: no longer true; direct health probe now works
- **Still unresolved**
  - no non-monitor merge throughput
  - older blocked task `8ed214ec-7eac-4887-8abd-97aa09c61694`
  - Codex fallback still unusable from current credential/config state
  - freshest Backend Agent path pauses behind a `voice-live-*` / `no_output` handoff

## Validation
- Current cycle probes:
  - direct `codex exec` stdin transport probe
  - direct `codex exec --ignore-user-config` probe
  - sanitized-env `codex exec --ignore-user-config` probe
  - direct `openai-native` health probe from current source -> `OK`
- Existing validation still relevant:
  - focused `tests\provider-auth-settings.test.mjs` Codex auth.json regression passed in the prior cycle
  - `npm run build` passed in the prior cycle
- Full-suite validation:
  - `npm test` was re-run again this cycle but still did not finish cleanly in the shell wrapper within the allowed wait window

## Git / source state
- Branch: `bosun/codex-self-improvement-loop-commits`
- Package version: `0.43.0`
- Against `origin/main`: behind `0`, ahead `191` (`git rev-list --left-right --count origin/main...HEAD` -> `0 191`)
- Local source reflects the latest merged `main` seen by this checkout.
- No source files were changed this cycle.

## Narrowest next starting point
1. Start from Backend Agent run `cb954779-fb18-4013-9ab5-7cfa2647d1b0`, not the older Azure-only incident.
2. Trace why a non-voice `write-tests` node is surfacing a `voice-live-*` background session and stalling on `no_output`.
3. Inspect the handoff bridge between:
   - `workflow/workflow-nodes/actions.mjs`
   - `agent/harness-agent-service.mjs`
   - `voice/voice-tools.mjs` / `voice/voice-action-dispatcher.mjs`
4. Only revisit provider credentials after that if the paused/no-output path turns out to be a downstream effect rather than the root cause.
