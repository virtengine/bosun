incident

# Hourly Bosun Monitor Session — 2026-04-21 20:27 local

## Runtime State
- Daemon: restarted this session (PID 31704 → 14548 [supervisor auto-restart] → 134820 [post-fix manual restart]).
- Repo-local `.bosun` confirmed via `bosun --where`.
- Branch: `bosun/codex-self-improvement-loop-commits`. 0 behind / 189 ahead of `origin/main` after this session's commit.
- Working tree: 32 modified files / ~1568 insertions / ~140 deletions still uncommitted (third-party in-progress work, intentionally preserved).

## Task Statistics
- total=191, draft=45, todo=4 (CLI test fixtures only), inprogress=0, inreview=0, done=104 (unchanged 6+ sessions), blocked=35.
- Throughput last hour: **0 non-monitor tasks merged** → still violates the ≥1/hr SLO.

## Active Sinks Inspected
- `.bosun/logs/monitor.log` (~54 MB) — full lifecycle trace for `f41ba544-9879-4af5-9212-03bba13dda2c` captured 10:14:55 → 10:15:40 UTC.
- `.bosun/logs/monitor-error.log` — no new template-leak entries this session.
- `.bosun/workflow-runs/` — short-lived failure runs are not persisting run-detail JSON (write skipped or trimmed).

## Recurring Symptom — Disposition
- Family: `blocked-without-reason` — **observed 4+ consecutive sessions**.
- Per the user's escalation rule (≥2 sessions ⇒ code-level incident until disproven), this session was treated as an **incident**.
- Prior session's hypothesis (lifecycle bypasses the `unspecified_failure:` enforcement in `action.update_task_status`) was **DISPROVEN**: the lifecycle DOES route through `set-blocked-agent-plan-failed (action.update_task_status)`. The real bug was elsewhere.

## Real-Task Lifecycle Proof
Live trace of `f41ba544` after `bosun task update --status todo` reset:
1. 10:14:55 trigger `task.task_available` → Task Lifecycle.
2. 10:14:56 `claim-task` → `set-inprogress` → status: todo → inprogress.
3. 10:14:58 `acquire-worktree` succeeded.
4. 10:14:59 `run-agent-plan` started.
5. 10:15:23 codex SDK fail (`Codex Exec exited with code 1: Reading prompt from stdin...`).
6. 10:15:23 copilot fallback fail (`Failed to list models: 400`) → SDK auto-disabled.
7. 10:15:38 `execWithRetry` circuit breaker tripped: "Repeated no-output agent starts detected; stopping retries".
8. 10:15:39 `plan-agent-ok=false` → all downstream nodes SKIPPED.
9. 10:15:39.783 `set-blocked-agent-plan-failed (action.update_task_status)` started.
10. 10:15:39.792 status: inprogress → blocked (source: workflow). `blockedReason=''`. **Bug confirmed.**

## Root Cause Chain
1. (External, throughput-zero driver) Codex SDK transient OpenAI failure + Copilot SDK 400 on `models.list` (token scope/expiry). Both SDKs offline → every `run-agent-plan` exhausts retries → circuit breaker → lifecycle blocks the task.
2. (Source-level diagnosability bug — fixed this session) `kanbanService` adapter built in `infra/monitor.mjs:ensureWorkflowAutomationEngine()` exposed only `createTask`/`updateTaskStatus`/`listTasks`/`getTask`. `action.update_task_status` (workflow/workflow-nodes/actions.mjs:4427) gates patch persistence on `typeof kanban.updateTask === "function"`, so the call was silently skipped and `blockedReason` (along with `branchName`/`prNumber`/`prUrl` on inreview/inprogress) never landed in the task store.

## Fix Applied (durable, source-level)
- File: `infra/monitor.mjs` (lines 894–913).
- Added `updateTask: async (taskId, patch) => updateKanbanTask(...)` wired through the existing `kanban-adapter.updateTask` abstraction.
- Diff: +10/-0.
- Commit: `6c707b18` on local branch `bosun/codex-self-improvement-loop-commits` (NOT pushed, per user instruction).
- Other in-progress unstaged hunks in `infra/monitor.mjs` (workspace-sync benign-error widening, watcher fallback to `process.argv[1]`) preserved on top of the commit.

## Live Verification
After daemon restart loaded the new code:
1. Reset `f41ba544` → todo at 10:22:53 UTC.
2. Lifecycle re-ran identically: codex fail → copilot fail → circuit breaker → `set-blocked-agent-plan-failed`.
3. Final task state at 10:24:14 UTC:
   - `status=blocked`
   - `blockedReason="Repeated no-output agent starts detected; stopping retries"` ← **persists end-to-end**
   - `lastStatusEntry={status:"blocked", source:"workflow"}`

## Validation
- `node --check infra/monitor.mjs` → exit 0.
- `tools/vitest-runner.mjs run tests/workflow-task-lifecycle.test.mjs tests/kanban-adapter.test.mjs` → 245 passed, 2 skipped, 0 failed (47.0s).
- `npm test` and `npm run build` not yet run this hour (next hour's monitor should run them).

## Throughput Blocker — Not Fixed This Hour
The actual reason throughput is 0/hr is the **Codex + Copilot SDK simultaneous failure**, not the missing-blockedReason bug. The fix this hour makes the failure observable; it does not restore agent execution. Next hour's monitor must:
- Verify `.bosun/.env` `COPILOT_TOKEN` scope/expiry (the `models.list 400` is auth-shaped).
- Investigate whether codex SDK has a stdin-pipe regression on long built prompts (always failing with `Reading prompt from stdin...` + exit 1).
- Consider re-enabling a previously disabled fallback SDK (claude / gemini / openai-native) so lifecycle can complete a plan when codex+copilot are both down.

## Why a Runtime Cleanup Was Not Sufficient
This session deliberately avoided runtime queue-janitoring of the 35 stale blocked tasks because:
1. Cleanup without source fix re-creates the same state within minutes.
2. The smoke-test reset of `f41ba544` proved the bug regenerates on the very next dispatch.
3. The committed fix is the durable cure for the diagnosability symptom; cleanup of historical stale entries is now safe to do next hour with confidence the new entries will carry reasons.

## Local Source vs Upstream
- `git fetch` not performed (offline-friendly per instruction; local branch is already 0 behind upstream HEAD).
- Local fix is HEAD on `bosun/codex-self-improvement-loop-commits`. Not pushed.

## Narrow Handoff to Next Hourly Run
1. Re-snapshot stats; confirm any new blocked-state entries since this hour carry non-empty `blockedReason` (regression check on this commit).
2. Address Codex SDK stdin-pipe failure: open `shell/codex-sdk.mjs` (or equivalent) and inspect prompt-piping logic; check if recent `build-prompt` output exceeds an undocumented size limit.
3. Investigate Copilot `models.list 400`: check token storage (`.bosun/.env`, OAuth refresh path in `github/github-oauth-portal.mjs`), regenerate if needed.
4. Once at least one SDK is healthy, run a real-task throughput test: pick one of the 4 todo CLI fixtures (or a real backlog task) and watch it progress through inprogress → inreview → done.
5. Bulk-recover the 35 stale blocked tasks ONLY after confirming throughput is restored, otherwise they will block again immediately.
