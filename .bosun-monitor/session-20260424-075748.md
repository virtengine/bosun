status: incident

# Bosun Local Ops Monitor — 2026-04-24 07:57:48

## Open blockers
- Non-monitor merged PR throughput remains `0` in the last hour.
- Task `97d41516-caca-4fa0-9175-dbe6ad4ce01d` still has no PR, no branch divergence, and no completed validation handoff.
- The task-executor stale-evidence fixes are now live in the daemon, but `97d...` still does not stay reset.
- The narrowest unresolved blocker is now workflow resume arbitration: an older paused Task Lifecycle run is reclaiming `97d...` and forcing it back to `inprogress`.

## What changed this cycle
- Re-read the latest three monitor notes before touching runtime:
  - `session-20260424-073749.md`
  - `session-20260424-071012.md`
  - `session-20260424-065541.md`
- Rebuilt blockers from the newest note and stayed on the `97d...` family.
- Re-checked daemon health, sink mtimes, task stats, `97d...` projection/topology, workflow runs, session activity, worktree state, and merged non-monitor throughput.
- Restarted the daemon once to get the first task-executor stale-evidence fix live.
- Waited through the startup-stall recovery window and proved the first fix alone was insufficient in runtime.
- Landed a second narrow source fix in:
  - `task/task-executor.mjs`
  - `tests/task-executor.test.mjs`
- Restarted the daemon again to get the second fix live and re-checked `97d...` end to end.

## Validation
- Passed:
  - `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\task-executor.test.mjs tests\task-store.test.mjs`
  - `npm run build`

## Runtime health
- `bosun --daemon-status --config-dir .bosun --repo-root .` -> healthy, PID `23760`
- Active sink paths are advancing:
  - `logs\daemon.log` -> `2026-04-24T07:57:10`
  - `.bosun\workflow-runs\_active-runs.json` -> `2026-04-24T07:57:16`
  - `.bosun\.cache\kanban-state.json` -> `2026-04-24T07:57:10`
  - `.cache\task-executor-runtime.json` -> `2026-04-24T07:54:07`
- Current task stats:
  - `draft = 45`
  - `todo = 32`
  - `inprogress = 16`
  - `done = 104`
  - `blocked = 35`
- Runtime is active, but delivery is still unhealthy: `done` stayed `104`, and last-hour merged non-monitor PR count stayed `0`.

## End-to-end task inspection
- Real task path inspected end to end again: `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - task status remains `inprogress`
  - `prUrl = null`
  - worktree `task-97d41516caca-4c7810c1ec` is still clean and aligned with `origin/main`
  - latest projected run is now `6aa64692-9220-4bed-bab3-a65a05e9282a`
  - `latestNodeId = null`
- Root workflow/session state after the second restart:
  - `task_topology` now also points at `6aa64692-...` with blank `latest_node_id`
  - `_active-runs.json` has no `97d...` entry
  - `session_activity` still shows:
    - `6aa...:write-tests:turn` -> `stalled|error`
    - `f428...:write-tests:turn` -> `no_output`
    - `9820...:write-tests:turn` -> `no_output`
- Despite that stale descendant state, daemon logs immediately show:
  - `Added task 97d...`
  - `Shared state synced for 97d...`
  - `Task 97d... status: todo → inprogress (source: workflow)`

## Symptom -> proof -> code path -> blocker
- Symptom:
  - `97d...` no longer stays pinned to old `f320...`, but it still gets forced back to `inprogress` even when the latest backend retry is stale and `_active-runs.json` is empty.
- Proof:
  - both task projection and `task_topology` now point to `6aa64692-...` with `latestNodeId = null`
  - `session_activity` still marks the latest delegate session `97d...:agent:6aa...:write-tests:turn` as `stalled|error`
  - `_active-runs.json` still has no `97d...` entry
  - after the second daemon restart, logs show workflow resume immediately re-added the task and moved it `todo -> inprogress (source: workflow)`
  - workflow resume trace logged:
    - winner task `97d...` -> older paused Task Lifecycle run `980a5f32-6d4b-4617-a02f-20faab589d19`
    - backend ancestors such as `1962517d-...`, `32cf191a-...`, and `c775f774-...` remained in the duplicate chain
- Code path:
  - `workflow/workflow-engine.mjs`
    - interrupted resume repair set includes `duplicate_task_run` / `covered_by_root_interrupted_run` around lines `8682-8700`
    - promotable paused representative logic around lines `8843-8853`
    - resume winner logging and duplicate-run retirement around lines `9051-9081`
- Blocker:
  - task-executor stale-evidence recovery is now live, but workflow-engine interrupted resume arbitration reclaims the task first from an older paused root run
  - the next durable fix belongs in workflow-engine representative selection / duplicate-run retirement, not in task-executor

## Workflow-run behavior
- The stale backend descendant family remains:
  - `9cf...` -> `write-tests` bootstrap-only / `no_output`
  - `f320...` -> later completed session artifact but no downstream task work
  - `9820...` -> `no_output`
  - `f428...` -> `no_output`
  - `6aa...` -> `stalled|error`
- Parent Task Lifecycle behavior is the new interference point:
  - after the fresh daemon restart, workflow resume selected older paused root run `980a5f32-...` as the representative winner for `97d...`
  - that path reasserted workflow ownership and re-marked the task `inprogress`

## Throughput
- Last-hour merged non-monitor PR count for `virtengine/bosun`: `0`
- Throughput target not met.

## Git / source state
- This cycle changed:
  - `task/task-executor.mjs`
  - `tests/task-executor.test.mjs`
- No PR or CI was created in this cycle.
- The repository is otherwise dirty; do not disturb unrelated edits.

## Narrowest next handoff
1. Stay on daemon PID `23760`.
2. Trace `workflow/workflow-engine.mjs` interrupted resume winner selection for task `97d...`, starting at:
   - `resumableRepairResults`
   - `promotableResumeResults`
   - representative selection feeding `latestByTaskId`
   - `_markRunUnresumable(runId, "duplicate_task_run")`
3. Prove why paused root run `980a5f32-...` beats the stale backend descendants and reclaims the task after startup recovery.
4. Only after that workflow-engine fix lands, restart the daemon again and verify `97d...` finally stays `todo` instead of flipping back to `inprogress`.
5. Keep watching last-hour merged non-monitor PR throughput; it remains `0`.

incident
