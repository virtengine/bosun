# Bosun monitor handoff - 2026-04-23 22:24:32

Status: warning

## Summary
- Landed a narrow source fix in `agent/provider-auth-state.mjs` so `resolveCodexSubscriptionAuth()` now recognizes the current `~/.codex/auth.json` API-key schema (`OPENAI_API_KEY`) instead of only older OAuth-style token fields.
- Added a regression in `tests/provider-auth-settings.test.mjs` proving `openai-codex-subscription` becomes `authenticated=true` / `canRun=true` from that auth file shape.
- Live queue state is no longer flat: `todo=8`, `inprogress=3`, `blocked=35`, and real Backend Agent runs are active again.

## Open blockers
1. Harness runtime still prefers the native Azure path, and active Backend Agent runs still surface the same Azure-side agent failure summary (`Missing environment variable: AZURE_OPENAI_API_KEY`) instead of producing real implementation progress.
2. The new Codex auth detection fix does not yet restore a usable fallback, because direct `codex` execution still fails locally with `Codex Exec exited with code 1: Reading prompt from stdin...`.
3. Representative older blocked task `8ed214ec-7eac-4887-8abd-97aa09c61694` is still unchanged (`blocked`, `repeated_no_commit_runs`, no fresh retry).
4. Fresh non-monitor success is not proven yet: there are active tasks and workflow movement, but no confirmed new code/PR outcome from the recovered queue.

## Symptom -> proof -> code path -> fix or blocker

### 1. Codex subscription auth was present on disk but invisible to provider auth normalization
- Symptom:
  - `~\.codex\auth.json` existed, but live provider auth still reported `openai-codex-subscription-1775278524001` as `unauthenticated`.
- Proof:
  - `resolveCodexSubscriptionAuth(process.env)` returned `authenticated: false` even though:
    - `C:\Users\jON\.codex\auth.json` exists
    - the file schema is just `{ "OPENAI_API_KEY": "..." }`
  - Current source-level provider registry now reports:
    - `openai-codex-subscription-1775278524001`
    - `authenticated: true`
    - `canRun: true`
    - `authSource: "auth.json"`
- Code path:
  - `agent/provider-auth-state.mjs` `resolveCodexSubscriptionAuth()`
- Fix:
  - Added API-key schema paths (`OPENAI_API_KEY`, `CODEX_API_KEY`) to the auth.json token resolution list.
  - Added regression coverage in `tests/provider-auth-settings.test.mjs`.

### 2. The source fix improves provider visibility, but it does not change the live harness route yet
- Proof:
  - Current source-level provider registry shows both:
    - `azure-openai-responses-1775278346327` -> `authenticated=true`, `canRun=true`
    - `openai-codex-subscription-1775278524001` -> `authenticated=true`, `canRun=true`
  - `resolvePoolSdkName()` still chooses `openai-native` whenever harness finds a runnable native executor.
  - `resolveHarnessNativeSelectionId()` only considers providers with `adapterId === "openai-native"`.
  - `openai-codex-subscription` remains a `codex-sdk` provider, so it is not considered by the native Azure selector.
- Blocker:
  - Even after the Codex auth fix, Bosun still prefers the Azure-native path while that path appears runnable.

### 3. Direct Codex fallback is still not healthy enough to replace Azure
- Proof:
  - Direct probe:
    - `launchEphemeralThread('Reply with exactly OK', ..., { sdk: 'codex', disableFallback: true })`
    - result: `Codex Exec exited with code 1: Reading prompt from stdin...`
- Blocker:
  - Codex auth discovery is fixed, but the local Codex execution path is still failing for a separate reason.

### 4. Real task flow is active again, but agent execution is still degraded
- Queue / workflow proof:
  - `bosun task stats --json --config-dir .bosun --repo-root .`
    - `todo=8`
    - `inprogress=3`
    - `blocked=35`
  - Current real in-progress tasks:
    - `396cae99-d369-488d-84ff-8decbcaaf873`
    - `90d52986-5790-4571-88dc-0b4180b67089`
    - `3464de03-6e6c-468f-a470-f705b2b1252c`
  - Recent Backend Agent runs:
    - completed: `52fa6c97-cc39-4093-852b-aea664b5a2d0`, `d37c892f-0b31-47b8-b316-2a294d7baf59`, `9c67207f-010f-45fb-898f-c25f12176b8a`
    - running: `16be47b4-1618-4089-b4f9-6d078d56fc5d`, `0eb7c34f-59a0-4d23-9622-27d370d16427`, `63532ecd-0e82-41fe-9cff-a4914870abad`
  - Monitor log now shows live non-monitor workflow activity such as:
    - `wf=Backend Agent`
    - `build-retry`
    - `test-retry`
    - `lint-retry`
- Real task path inspected end to end:
  - task `396cae99-d369-488d-84ff-8decbcaaf873`
  - current status: `inprogress`
  - delegated Backend Agent child run `7fcd23cf-b479-4b17-a6bb-c07feae9d815` completed
  - later Backend Agent run `52fa6c97-cc39-4093-852b-aea664b5a2d0` completed
  - current follow-up Backend Agent run `16be47b4-1618-4089-b4f9-6d078d56fc5d` is still running
  - the completed Backend Agent run still records:
    - `plan-work.summary = "Agent error: Missing environment variable: \`AZURE_OPENAI_API_KEY\`."`
    - `write-tests.summary = "Agent error: Missing environment variable: \`AZURE_OPENAI_API_KEY\`."`
    - `implement.summary = "Agent error: Missing environment variable: \`AZURE_OPENAI_API_KEY\`."`
- Current interpretation:
  - the queue is no longer starved, but active task execution is still degraded at the agent/provider layer.

### 5. Replenish is no longer the immediate incident edge
- Proof:
  - latest `Task Replenish (Scheduled)` run: `17900a99-9915-4dcc-84d3-5d7c38c2195c`
  - `check-backlog.triggered = false`
  - `todoCount = 8`
  - `threshold = 5`
- Interpretation:
  - current replenish behavior is consistent with having enough todo inventory; the narrower blocker has moved to the active task execution path rather than backlog creation.

## Validation
- Passed:
  - `npx vitest run tests\provider-auth-settings.test.mjs -t "Codex auth.json" --reporter verbose`
  - `npm run build`
- Inconclusive / still running in shell wrapper:
  - `npm test`

## Git / worktree state
- This cycle changed:
  - `agent/provider-auth-state.mjs`
  - `tests/provider-auth-settings.test.mjs`
- Pre-existing modified files from earlier monitor fixes remain present:
  - `agent/agent-launcher.mjs`
  - `agent/providers/_shared.mjs`
  - `infra/monitor.mjs`
  - `tests/agent-pool.test.mjs`
  - `tests/monitor-workflow-startup-guards.test.mjs`
  - `tests/provider-kernel.test.mjs`
  - `RELEASE_DRAFT.md`

## Narrowest next starting point
1. Start from the current running Backend Agent task path (`16be47b4-1618-4089-b4f9-6d078d56fc5d`, `0eb7c34f-59a0-4d23-9622-27d370d16427`, `63532ecd-0e82-41fe-9cff-a4914870abad`) instead of the older replenish-only failure.
2. Prove whether the active agent-node `AZURE_OPENAI_API_KEY` summaries are still masking the same Azure 401/runtime mismatch or a newer routing/env bug.
3. If Azure remains bad, decide between:
   - changing harness routing so a failing native Azure executor does not outrank an authenticated Codex subscription executor, or
   - fixing the separate local Codex execution failure (`Reading prompt from stdin...`) so Codex becomes a real fallback.
4. Re-check whether any of the three active `inprogress` tasks produce actual code/PR output before downgrading the incident further.
