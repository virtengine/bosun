incident

# Bosun Local Ops Monitor — 2026-04-25 01:29:39 local

## Startup / scope
- Read the latest three monitor notes before touching runtime state:
  - `session-20260425-010825.md`
  - `session-20260425-004002.md`
  - `session-20260424-235237.md`
- Rebuilt the blocker list from `session-20260425-010825.md`.
- Stayed simulator-first and kept the daemon locked/off.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Open-blocker list rebuilt this cycle
1. **Simulator gate still blocked** — no real non-monitor task has reached code change -> tests/build -> commit -> PR -> review/fix -> merge.
2. **Windows `npm` bridge bug is now fixed in source** — delegated workspace commands no longer die at `spawnSync npm ENOENT`; the live path moved past that seam.
3. **New narrow blocker: recursive-delegation / tool-side-file-modification summaries still leak success live** — the current tests-phase run records blocked summaries but still persists `success = true`, so implementation opens incorrectly.
4. **Live proof for the new workflow matcher patch is still pending** — the active rerun started before the classifier fix landed and already leaked into implementation.
5. **Local broad test baseline still blocked** — full `npm test` remains unusable here because the existing `demo-defaults:sync` pretest hang is unchanged.

## Durable fixes landed this cycle
1. **Resolve `npm` correctly in delegated workspace commands on Windows**
   - Files:
     - `voice/voice-tools.mjs`
     - `tests/voice-tools.test.mjs`
   - Change:
     - `run_workspace_command()` now resolves `npm` to `npm.cmd` on Windows before calling `spawnSync(..., { shell: false })`.
   - Why it mattered:
     - the live delegated path had been reporting `spawnSync npm ENOENT`, which blocked required test/build verification inside delegated non-voice sessions.

2. **Classify recursive delegation / tool-side file-modification summaries as env blocks**
   - Files:
     - `workflow/workflow-nodes/actions.mjs`
     - `tests/workflow-engine.test.mjs`
   - Change:
     - added workflow env-block matchers for:
       - `delegation did not progress normally`
       - `repeatedly spawning nested sessions instead of producing code/output`
       - `blocked on tool-side file modification`
   - Why it mattered:
     - the current live tests-phase payload now carries those exact blocked summaries while still persisting `success = true`.

## Validation completed this cycle
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\voice-tools.test.mjs -t "run_workspace_command blocks non-safe commands|run_workspace_command allows non-safe commands for delegated non-voice sessions|run_workspace_command allows safe read-only commands without approval|run_workspace_command resolves npm.cmd on Windows"`
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\workflow-engine.test.mjs -t "summary-only|digest-only|delegated inspection stall|read-only workspace session summaries|recursive delegation stall summaries|environment-constraint fallback|failing delegated executor path"`
- `npm run build`
- `bosun simulate task restart --json --config-dir .bosun --repo-root .`
  - current rerun: `d4cf3cf9-8780-4d42-a949-9cdc59014f92`
  - still active at handoff time

## Live simulator evidence from this cycle

### 1. The Windows `npm` seam moved forward in the real task path
- Active rerun:
  - `d4cf3cf9-8780-4d42-a949-9cdc59014f92`
- Live proof from workflow logs:
  - `run-agent-tests` emitted:
    - `Tool call: run_workspace_command`
    - `Tool result: run_workspace_command`
- This replaces the earlier blocker family where nested delegates reported:
  - `spawnSync npm ENOENT`
- The real task path has now advanced beyond the direct `npm` command-resolution failure.

### 2. The current tests-phase run still leaks success off blocked summaries
- Same active rerun:
  - `d4cf3cf9-8780-4d42-a949-9cdc59014f92`
- Persisted node output now shows:
  - `run-agent-tests.status = completed`
  - `run-agent-tests.success = true`
  - `run-agent-tests.summary = "session.turn.complete: Delegation did not progress normally: ... repeatedly spawning nested sessions ..."`
  - `tests-agent-ok` evaluated `true`
  - `run-agent-implement.status = waiting`
- Strongest workflow-log proof from the same run:
  - `session.turn.complete: I’m blocked on tool-side file modification, not on code understanding ...`
  - `session.turn.complete: Delegation did not progress normally: the live delegate session appears to be repeatedly spawning nested sessions instead of producing code/output ...`
  - `Agent completed: success=true streamEvents=54`
- Parent tests session proof:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_d4cf3cf9-8780-4d42-a949-9cdc59014f92_run-agent-tests_turn.json`
  - final assistant message collapsed to `Agent run completed.`
  - but the workflow node payload retained the blocked summary above

### 3. The success leak opened implementation incorrectly
- Current downstream state in the same rerun:
  - `run-agent-implement.status = waiting`
  - latest workflow log:
    - `Agent waiting for shared agent slot (10/10 active, queue=15)`
- Implementation session file exists:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_d4cf3cf9-8780-4d42-a949-9cdc59014f92_run-agent-implement_turn.json`
- No commits, PRs, reviews, or merges exist for the task.

### 4. Nested delegation is still the observable live failure family
- Current tests delegate root:
  - `logs/sessions/delegate-live-1777044090049-40d7e1.json`
- Persisted proof:
  - recursive `delegate-live-*` child launches
  - assistant summary says the live coding session was merely started and suggests polling a later child
  - no verified code/test/build/commit evidence was returned from that delegate chain

## Runtime health
- Daemon remained **locked/off** as the main path.
- Explicit daemon check:
  - `node .\cli.mjs --daemon-status --config-dir .bosun --repo-root .`
  - result:
    - daemon mode is **not** running
    - 1 Bosun process is active (simulator/monitor path)
- Active workflow path at handoff:
  - `.bosun\workflow-runs\_active-runs.json`
  - still lists `d4cf3cf9-8780-4d42-a949-9cdc59014f92`

## End-to-end task inspection
- Real task inspected end to end again:
  - task: `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - title: `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`
  - PR: none
- Current terminal state this cycle:
  - delegated workspace commands advanced past the old `npm ENOENT` seam
  - tests-phase live run now exposes blocked recursion / file-modification summaries directly
  - because the current rerun started before the new classifier patch, `run-agent-tests` still leaked `success = true`
  - implementation opened and is waiting for a shared agent slot
  - no task code landed
  - no tests/build completed for task code
  - no commit created
  - no PR created
  - no merge

## Throughput
- Throughput target still **not met**.
- Recent merged PR throughput remains historical only:
  - latest merged PRs are still Dependabot merges from `2026-04-20` and earlier
  - no new non-monitor completion landed through the simulator gate this cycle

## Git / source state
- Branch remained:
  - `bosun/codex-self-improvement-loop-commits`
- Current branch is ahead-only vs tracked remote:
  - `ahead 3`
- Repository remains very dirty with unrelated user work; edits stayed surgical.

## Narrowest unresolved blocker now
**The current narrow blocker is no longer Windows `npm` resolution. The live simulator now gets past `run_workspace_command`, but the tests-phase agent still persists success-shaped completion when the actual summary says delegated execution is recursively stalling and blocked on tool-side file modification. That leak opens implementation incorrectly and keeps the simulator gate from proving a clean end-to-end task flow.**

### Symptom
- `run-agent-tests` records blocked recursive-delegation / tool-side-file-modification summaries.
- The same node still persists `success = true`.
- `tests-agent-ok` evaluates true and opens implementation.

### Proof
- Workflow run:
  - `.bosun\workflow-runs\d4cf3cf9-8780-4d42-a949-9cdc59014f92.json`
- Parent tests session:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_d4cf3cf9-8780-4d42-a949-9cdc59014f92_run-agent-tests_turn.json`
- Delegate root:
  - `logs/sessions/delegate-live-1777044090049-40d7e1.json`
- Downstream implementation wait:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_d4cf3cf9-8780-4d42-a949-9cdc59014f92_run-agent-implement_turn.json`

### Symptom -> proof -> code path -> fix or blocker
- **Fix landed**
  - Symptom:
    - delegated workspace commands on Windows failed at `spawnSync npm ENOENT`
  - Proof:
    - older live delegate summaries in the previous cycle explicitly reported missing `npm`
  - Code path fixed:
    - `voice/voice-tools.mjs`
    - `tests/voice-tools.test.mjs`
  - New live proof:
    - current rerun `d4cf...` now shows `Tool call: run_workspace_command` and `Tool result: run_workspace_command` before the next blocker

- **Fix landed in source, live proof pending**
  - Symptom:
    - recursive delegation / tool-side-file-modification summaries still persisted `success = true`
  - Proof:
    - current rerun `d4cf...` payload above
  - Code path fixed:
    - `workflow/workflow-nodes/actions.mjs`
    - `tests/workflow-engine.test.mjs`
  - Why live proof is pending:
    - the active rerun started before this classifier patch landed and already leaked into implementation

- **Current blocker**
  - Symptom:
    - active run must drain before a fresh rerun can prove the new classifier behavior live
  - Proof:
    - `_active-runs.json` still lists `d4cf...`
    - `run-agent-implement` is waiting on a shared slot
  - Suspect code paths if the next rerun still leaks:
    1. workflow blocked-result normalization for success-shaped harness sessions
       - `workflow/workflow-nodes/actions.mjs`
    2. harness session result assembly / terminal summary selection
       - `agent/agent-launcher.mjs`
       - `agent/internal-harness-control-plane.mjs`
       - `infra/session-tracker.mjs`

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first; do **not** unlock the daemon.
3. Let the current active rerun drain first:
   - `d4cf3cf9-8780-4d42-a949-9cdc59014f92`
4. Once it is no longer active, rerun:
   - `bosun simulate task restart --json --config-dir .bosun --repo-root .`
5. Confirm the new live expectations after the classifier patch:
   - `run-agent-tests.success = false`
   - `run-agent-tests.blockedReason = blocked_by_env`
   - `set-blocked-agent-tests-failed.success = true`
   - `run-agent-implement` stays closed
6. If the next rerun still leaks success, trace terminal summary selection between:
   - `agent/internal-harness-control-plane.mjs`
   - `agent/agent-launcher.mjs`
   - `infra/session-tracker.mjs`
   - `workflow/workflow-nodes/actions.mjs`
7. Keep the full daemon locked/off until a real task completes implementation -> push -> PR -> review/fix -> merge.

incident
