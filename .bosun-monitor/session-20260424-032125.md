status: incident

# Bosun Local Ops Monitor — 2026-04-24 03:21:25

## What changed this cycle
- Landed a durable projection-reconcile fix across the task store and workflow startup recovery path.
  - `task/task-store.mjs`
    - same-run workflow links now merge by richness instead of blindly replacing the older entry
    - this stops sparse delegated node-scoped updates from clobbering fuller completed run summaries for the same `runId`
  - `workflow/workflow-engine.mjs`
    - `_detectInterruptedRuns()` now replays authoritative run summaries back into task projections for paused duplicate/resumed task families, not only task IDs seen in the current interrupted cohort
  - `tests/task-store.test.mjs`
    - added regression proving a sparse same-run delegated update cannot overwrite a richer completed workflow link
  - `tests/workflow-engine.test.mjs`
    - added regression proving paused duplicate task families still reconcile task projection even when there is no active interrupted entry left

## Validation
- Passed: `npx vitest run tests\task-store.test.mjs`
- Passed via task runner agent:
  - `npx vitest run tests\workflow-engine.test.mjs -t "(syncs persisted completed runs into task topology|does not let older run status sync clobber the latest task run linkage|reconciles interrupted task families into task topology without clobbering a newer completed run|reconciles paused duplicate task families into task topology even without an active interrupted entry)"`
  - result: 4/4 requested tests passed
- Passed: `npm run build`
- Interactive PowerShell shells running `tests\workflow-engine.test.mjs` directly were wrapper-noisy and had to be stopped, so the clean validation source of truth for that targeted set is the task runner result above.

## Runtime proof
- Reloaded Bosun onto the new patch set; final daemon PID is `3488`.
- The old `0de8...` task projection drift is fixed live.
  - raw repo task store and `bosun task get --json` now both show:
    - `0de863b2-94b7-4cd0-b71b-7d137cb5ba4c`
    - `latestRunId = fb1c05df-a04e-4cde-85cd-3c3ea1559d4c`
    - old family members `b4c04dfe-...`, `3264d1fc-...`, and `22ecfd67-...` replayed as `paused`
    - latest backend run `fb1c05df-...` preserved as `completed` with summary `Workflow completed successfully.`
- The startup recovery owner fix is now live again for the sibling family after the extra recovery interval.
  - daemon log showed:
    - `Resuming 45 interrupted run(s)...`
    - `Resuming run 980a5f32-...`
    - `Resuming run 35dacfce-...`
  - active run index then showed fresh live families:
    - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
      - Task Lifecycle `6215f600-6024-41ac-a08a-50604a47d647`
      - Backend Agent `86d60fb4-cc8d-4b11-ae74-0a3044e1af2d`
    - task `7f707e6d-5a61-480d-8346-59991af8d1bf`
      - Task Lifecycle `51b657b3-4774-4346-b726-40480ea7d722`
      - Backend Agent `b719bc11-efff-4241-a71a-41c31dd9e391`

## Required end-to-end task path inspection
- Inspected the live `97d41516-caca-4fa0-9175-dbe6ad4ce01d` path end to end.
  - task record:
    - status `inprogress`
    - branch `task/97d41516caca-s-feat-observability-emit-structured-logs-and-me`
    - `prUrl = null`
    - workflow run tail now shows:
      - previous paused pair `59c5c966-...` / `4294c02f-...`
      - fresh running pair `6215f600-...` / `86d60fb4-...`
  - fresh Task Lifecycle detail `6215f600-...`:
    - nodes through `build-prompt` completed
    - issue advisor: `Resume from Agent Plan.`
  - fresh Backend Agent detail `86d60fb4-...`:
    - issue advisor: `Resume from Write Tests First.`
    - `nodeStatuses = { trigger: completed, plan-work: running }`
    - only planner log present: `Running planner for 5 tasks`

## Throughput / health re-check
- `bosun task stats --json --config-dir .bosun --repo-root .`
  - `draft=45`
  - `todo=4`
  - `inprogress=23`
  - `inreview=0`
  - `done=104`
  - `blocked=35`
- Recent non-monitor throughput remains below target.
  - `gh pr list --state merged --search "base:main merged:>=2026-04-23T16:09:13Z"` returned `[]`
  - last-hour merged non-monitor PR count: `0`
- `Task Batch Processor` is still not turning the available backlog into throughput in this epoch:
  - fresh log line: `Task batch completed: 0/0 succeeded (0 failed)`

## Narrowest unresolved blocker
### Symptom
The recovery/projection bug is fixed, but fresh resumed Backend Agent runs are stalling in planner execution, so throughput is still incident-level.

### Proof
- fresh live backend runs:
  - `86d60fb4-cc8d-4b11-ae74-0a3044e1af2d` for task `97d...`
  - `b719bc11-efff-4241-a71a-41c31dd9e391` for task `7f70...`
- both details show:
  - `issueAdvisor.summary = "Resume from Write Tests First."`
  - `nodeStatuses = { trigger: "completed", plan-work: "running" }`
  - only planner log line present: `Running planner for 5 tasks`
- after the extra recovery interval there is still no PR output (`prUrl = null`) and merged non-monitor PR throughput in the last hour is `0`.

### Code path
- `workflow/workflow-nodes/agent.mjs`
  - `agent.run_planner`
- next hop:
  - `agent/harness-agent-service.mjs`
    - `launchEphemeralThread(...)`
- adjacent runtime handoff:
  - `agent/agent-launcher.mjs`

## Next start point
1. Start from the fresh live backend runs `86d60fb4-...` and `b719bc11-...`, not the already-fixed `0de8...` projection drift.
2. Inspect why `agent.run_planner` emits only the initial planner log with no heartbeat/completion for these live runs.
3. Determine whether the stall is inside planner event streaming / heartbeat flush, harness launch finalization, or a native runtime handoff gap.
4. Do not reopen task projection recovery first unless the repo task store regresses and starts showing old `running` states for the `0de8...` family again.

incident
