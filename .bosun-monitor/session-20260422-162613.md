# Bosun Monitor Session 2026-04-22 16:26:13

## Outcome

- Status: warning
- Branch: `bosun/codex-self-improvement-loop-commits`
- Package version: `0.43.0`
- Main freshness: `git rev-list --left-right --count origin/main...HEAD` => `0 190` (`0` behind, `190` ahead)
- Recent merged throughput: `git log origin/main --merges --since="2 hours ago" --oneline` returned no merges

## Open Blockers

1. User-facing task projection fields are still blank for real in-progress tasks even after live recovery (`latestRunId`, `workflowName`, `sessionId`, `worktreePath`).
2. `execution ledger write failed: Cannot create a string longer than 0x1fffffe8 characters` still appears on daemon startup.
3. Throughput target is still not met; no non-monitor merges landed in the last 2 hours.

## Symptom -> Proof -> Code Path -> Fix Or Blocker

### 1. Startup interrupted-run recovery was missing older paused resumable rows from the startup cohort

- Symptom:
  - Prior daemon startups logged only the newest interrupted task family and left the older `8ed214ec-7eac-4887-8abd-97aa09c61694` paused run stranded.
  - Representative failure before this fix: `2026-04-22T06:00:12.741Z` logged `Detected 1 interrupted run(s): 9fe8ae03-3f04-4ee7-ab52-5df8bd7f02a6`, and the delayed pass at `06:02:16` resumed only that one run.
- Proof:
  - Source read showed `workflow/workflow-engine.mjs::_detectInterruptedRuns()` only seeded startup recovery from:
    - persisted `_active-runs.json`
    - index rows still marked `RUNNING`
    - orphan running detail files
  - It did **not** add already-paused resumable index rows to `_startupInterruptedRunIds`, which matched the live log shape where `ca08ddd9-bc4d-4eb9-b485-69374996ccf2` existed in the run index but was absent from startup detection.
- Code path:
  - `workflow/workflow-engine.mjs`
    - `_detectInterruptedRuns()` startup cohort seeding
    - `resumeInterruptedRuns()` startup cohort ordering
- Fix applied:
  - `_detectInterruptedRuns()` now adds existing `paused + resumable` index rows into the startup interrupted cohort.
  - `resumeInterruptedRuns()` now preserves startup cohort insertion order when expanding the resume list, so freshly interrupted runs still stay ahead of historical paused siblings.
- Focused regression coverage added:
  - `tests/workflow-engine.test.mjs`
    - `includes existing paused resumable runs in the startup interrupted cohort`
  - Existing adjacent regression kept green:
    - `prioritizes the current interrupted-run scan without starving older paused runs`

### 2. Live proof on fresh daemon startup succeeded

- Fresh daemon:
  - Stopped old PID `112576`
  - Started new PID `127964`
- Startup proof from fresh log:
  - `2026-04-22T06:23:00.808Z` `Detected 4 interrupted run(s): 3a73494e-3d06-4b22-b077-f795f3beb35e, 1ae70882-fe0e-4635-bdf3-0b7f4145e311, ca08ddd9-bc4d-4eb9-b485-69374996ccf2, 0fc59217-88c4-43ee-aca5-1cabef9a3fe8`
  - `2026-04-22T06:25:02.560Z` `Resuming 4 interrupted run(s)...`
  - `2026-04-22T06:25:04.451Z` `Skipped 2 duplicate interrupted run(s) (kept latest per taskId)`
  - `2026-04-22T06:25:04.460Z` resumed `3a73494e-3d06-4b22-b077-f795f3beb35e` for task `8ed...`
  - `2026-04-22T06:25:07.772Z` resumed `1ae70882-fe0e-4635-bdf3-0b7f4145e311` for task `a6a...`
- Active run proof after delayed recovery:
  - `_active-runs.json` now contains:
    - `0068026c-541b-4e5f-ab36-4ca53d360c45` for task `8ed214ec-7eac-4887-8abd-97aa09c61694`
    - `0ff70eeb-c2e4-49aa-9a04-3f37f7eb6b4b` for task `a6a0198e-a547-4eb7-872c-af2c83604b5f`
- End-to-end representative path inspected in depth:
  - Task `8ed...`
  - historical paused run `ca08...`
  - fresh interrupted run `3a73494e...`
  - new active run `0068026c...`
  - plus the parallel `a6a...` lineage through `0fc59217...`, `1ae70882...`, and `0ff70eeb...`

### 3. Remaining user-visible blocker is task projection staleness, not startup recovery

- Symptom:
  - Even after the fresh daemon startup and successful delayed recovery, `bosun task get` still reports blank:
    - `latestRunId`
    - `workflowName`
    - `sessionId`
    - `worktreePath`
  - for both representative tasks.
- Proof:
  - After recovery:
    - `bosun task get 8ed... --json` -> `status=inprogress`, but all topology fields above remain `null`
    - `bosun task get a6a... --json` -> same
  - This is now inconsistent with `_active-runs.json`, which clearly holds fresh active lifecycle runs for both tasks.
- Likely code path:
  - `workflow/workflow-nodes/actions.mjs`
  - `task/task-store.mjs`
  - possibly the merge/projection path that populates task summary topology from run history / active runs
- Blocker:
  - Startup interrupted-run recovery is no longer the controlling issue. The next narrow bug is the stale task projection layer.

## Validation Status

- Focused regressions passed:
  - `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/workflow-engine.test.mjs -t "includes existing paused resumable runs in the startup interrupted cohort|prioritizes the current interrupted-run scan without starving older paused runs"`
- Full suite passed:
  - `npm test` -> `__EXIT:0`
- Build passed:
  - `npm run build` -> `__EXIT:0`
- Pre-push unique stages passed:
  - `node tools/prepublish-check.mjs`
  - `npm run syntax:check`
  - `npm run prompt:lint`
  - `npm run smoke:packed-cli`
- Pre-push duplicate tail:
  - `npm run prepush:check` advanced into `npm run test:all`
  - the rerun was terminated once it entered the duplicate full-test tail because `npm test` had already passed from the same source and the prepush-only delta had already cleared

## Runtime State At End Of Cycle

- Daemon running: PID `127964`
- Active sink paths advancing:
  - `.bosun/logs/monitor.log`
  - `.bosun/workflow-runs/index.json`
  - `.bosun/workflow-runs/_active-runs.json`
- Real workflow progress:
  - both representative task families resumed into fresh active lifecycle runs
- Throughput target:
  - still not met

## Narrowest Next Starting Point

- Leave `workflow/workflow-engine.mjs` unless new evidence reopens it.
- Start at the task projection merge path and prove why `bosun task get` is still blank after `_active-runs.json` and run history clearly show fresh active lifecycle runs.
- Most likely next hop:
  1. inspect the task projection update path in `workflow/workflow-nodes/actions.mjs`
  2. inspect summary/topology merge behavior in `task/task-store.mjs`
  3. add a focused regression proving that a fresh active lifecycle run updates the task’s top-level `latestRunId` / `workflowName` / `sessionId` / `worktreePath`
