status: warning

# Bosun Local Ops Monitor — 2026-04-24 07:37:49

## Open blockers
- Non-monitor merged PR throughput remains `0` in the last hour.
- The recovered `97d41516-caca-4fa0-9175-dbe6ad4ce01d` family still has no validation completion, branch divergence, or PR handoff.
- A durable source fix is now landed for stale checkpointless workflow evidence, but the running daemon has not picked it up yet.
- The live `97d...` family is still retrying while `_active-runs.json` and task topology remain pinned to older retry `f32079a6-9456-425c-bbed-b0b05c0f5235`.

## What changed this cycle
- Continued from the previously rebuilt blocker list and stayed on the narrowest unresolved blocker around `97d...`.
- Opened the exact `index.json` entries for `f320...` and `9820...` and confirmed the run index already contains newer retry identity data even when task projection lags.
- Landed a durable source fix in `task/task-executor.mjs`:
  - non-`active-runs` workflow evidence with no checkpoint progress now stops counting as live after the existing startup-stall window
  - this prevents stale topology-only run detail from keeping ownerless tasks in `inprogress`
- Added a focused regression in `tests/task-executor.test.mjs` covering stale topology-only run detail with no checkpoint progress.
- Re-checked live runtime state for `97d...` after the code change.

## Validation
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\task-executor.test.mjs tests\task-store.test.mjs`
- `npm run build`

## Runtime health
- The daemon is still advancing work; sink files and ledger rows are moving.
- `97d...` remains `inprogress` in task projection.
- `kanban-state.json` for `97d...` is still pinned to:
  - `latestRunId = f32079a6-9456-425c-bbed-b0b05c0f5235`
  - `latestNodeId = null`
  - `lastActivityAt = 2026-04-23T21:03:43.433Z`
- `_active-runs.json` still exposes only the same older `f320...` family entry for `97d...`.

## End-to-end task inspection
- Real task path inspected end to end again: `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - task status remains `inprogress`
  - `prUrl = null`
  - worktree `task-97d41516caca-4c7810c1ec` is still clean and aligned with `origin/main`
- Root ledger state has moved ahead of projection again:
  - `f32079a6-9456-425c-bbed-b0b05c0f5235` still exists as the projected latest run
  - newer `97d...` retries now also exist in `workflow_runs`:
    - `f4286b37-e167-4eb2-b8ff-f46d1000c857`
    - `6aa64692-9220-4bed-bab3-a65a05e9282a`
  - both newer retry run JSON files also exist on disk
  - task projection and `_active-runs.json` did not promote either newer retry

## Symptom -> proof -> code path -> blocker
- Symptom:
  - `97d...` keeps retrying in the root ledger, but recovery and task projection stay anchored to older retry `f320...`.
- Proof:
  - `.bosun\.cache\kanban-state.json` still reports `latestRunId = f320...` and `latestNodeId = null`
  - `_active-runs.json` still returns only the `f320...` family entry for `97d...`
  - `.bosun\.cache\state-ledger.sqlite` now shows newer `97d...` workflow rows for `f428...` and `6aa...`
  - raw run detail files exist for `f428...` and `6aa...`, proving those retries were persisted even though projection did not catch up
- Code path:
  - `task/task-executor.mjs` was over-trusting stale checkpointless topology-only run detail when no authoritative active-run corroboration existed
  - the new guard now treats that evidence as stale after `WORKFLOW_RUN_STARTUP_STALL_RECOVERY_MS`
  - this is the smallest durable fix for the stuck-ownerless-task side of the loop
- Blocker:
  - the running daemon is still executing old code, so the new recovery guard is not yet proven live in runtime
  - until the daemon reloads, `97d...` can keep looping on stale projected evidence even though the repo fix is ready

## Workflow-run behavior
- The `97d...` family has advanced past the earlier `9820...` retry.
- Current proven sequence:
  - `9cf...` reached `write-tests` and ended bootstrap-only `no_output`
  - `f320...` retried and became the projected latest run
  - `9820...` retried from `f320...`
  - newer ledger retries `f428...` and `6aa...` were then persisted without projection catching up
- This is now a split-brain runtime symptom:
  - ledger/run-detail truth is moving
  - `_active-runs.json` and task topology are lagging

## Throughput
- Last-hour merged non-monitor PR count for `virtengine/bosun`: `0`

## Git / source state
- This cycle changed:
  - `task/task-executor.mjs`
  - `tests/task-executor.test.mjs`
- The repository is otherwise dirty; do not disturb unrelated edits.

## Narrowest next handoff
1. Restart or reload the current daemon so the new `task/task-executor.mjs` recovery guard is live.
2. Re-check `97d...` from root workflow evidence first:
   - `workflow_runs`
   - `_active-runs.json`
   - task topology / `kanban-state.json`
3. Confirm whether the task now resets to `todo` once stale checkpointless evidence ages out, instead of staying `inprogress`.
4. If `97d...` still loops after the daemon reload, isolate the remaining drift as a workflow persistence bug using the newest retry after `6aa...`.
5. Keep watching last-hour merged non-monitor PR throughput; it remains `0`.

warning
