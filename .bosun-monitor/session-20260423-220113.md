# Bosun monitor handoff - 2026-04-23 22:01:13

Status: incident

## Summary
- Landed a second durable source fix in `agent/providers/_shared.mjs` so provider session configs now preserve resolved bound credentials (`apiKey`, `oauthToken`, `subscriptionToken`) instead of only surfacing boolean credential metadata.
- Added a regression in `tests/provider-kernel.test.mjs` proving a non-primary Azure harness executor with `authBindings.apiKeyEnv` reaches runtime with `providerConfig.apiKey`.
- Reloaded Bosun with `bosun --stop-daemon --config-dir .bosun --repo-root .` then `bosun --daemon --config-dir .bosun --repo-root . --no-update-check`; new daemon PID is `95608`.

## Open blockers
1. Real throughput is still stalled: `draft=45`, `todo=4`, `blocked=35`, `inprogress=0`, no non-monitor merges in the last hour, and Task Batch still completes `0/0`.
2. The selected secondary Azure executor now receives a bound credential in runtime config, but Azure still rejects live requests with `401 invalid subscription key or wrong API endpoint`.
3. Representative real task `8ed214ec-7eac-4887-8abd-97aa09c61694` is still `blocked` with no fresh Task Lifecycle retry after these feeder/runtime fixes.
4. Broader validation still fails on the same unrelated existing startup-guard assertion in `tests/monitor-workflow-startup-guards.test.mjs`.

## Symptom -> proof -> code path -> fix or blocker

### 1. Harness-native execution was dropping bound executor credentials before adapter launch

- Symptom:
  - Provider selection/auth health considered `azure-openai-responses-1775278346327` runnable, but live native execution still looked like it had no bound Azure key.
- Proof:
  - Pre-fix runtime inspection showed:
    - `selectionId = "azure-openai-responses-1775278346327"`
    - `providerEntry.authBindings.apiKeyEnv` present
    - but `providerConfig.apiKey.present = false`
  - `provider-registry.mjs` already applied `applyBoundCredentialEnv(...)` when resolving auth health and when building session config.
  - `agent/providers/_shared.mjs` `createSessionConfig()` resolved env-backed credentials, but only returned:
    - `credentials.apiKeyConfigured`
    - `credentials.oauthConfigured`
    - `credentials.subscriptionConfigured`
    - not the actual resolved credential values needed by native adapters.
- Code path:
  - `agent/providers/_shared.mjs` `createSessionConfig()`
  - runtime consumer: `agent/provider-kernel.mjs` → `registry.buildSessionConfig(...)` → native adapters (`shell/openai-native-adapter.mjs`, `shell/anthropic-native-adapter.mjs`, etc.)
- Fix:
  - Patched `agent/providers/_shared.mjs` so `createSessionConfig()` now carries resolved `apiKey`, `oauthToken`, and `subscriptionToken` in the returned provider config.
  - Added regression coverage in `tests/provider-kernel.test.mjs`.
- Validation:
  - Passed: `npx vitest run tests\provider-kernel.test.mjs -t "preserves explicit harness executor settings for non-primary native selections" --reporter verbose`
  - Passed: `npx vitest run tests\agent-pool.test.mjs -t "falls through to a runnable harness native executor when the configured primary cannot run" --reporter verbose`
  - Passed: `npm run build`
  - `npm test` failed on unrelated existing assertion in `tests/monitor-workflow-startup-guards.test.mjs`
  - `npm run prepush:check` failed on the same unrelated existing assertion

### 2. Source-side credential propagation is fixed, but the live Azure executor still fails upstream

- Symptom:
  - After the credential-propagation fix and daemon reload, `Task Replenish (Scheduled)` still fails before materializing tasks.
- Proof:
  - Post-fix runtime inspection from current source now reports:
    - `selectionId: "azure-openai-responses-1775278346327"`
    - `apiKeyPresent: true`
    - `endpoint: "https://sweden-central-openloans-resourc.openai.azure.com/openai/v1"`
  - Post-reload monitor log still shows:
    - `primary SDK "openai-native" failed ... OpenAI API error 401`
    - `Access denied due to invalid subscription key or wrong API endpoint`
  - Latest failed replenish artifact after reload:
    - run `2a883816-11b6-468d-bf68-96c0bab3f3e9`
    - `run-planner.items[0].summary` shows Azure `401`
    - workflow still fails at `materialize-tasks` because planner output is empty
- Code path:
  - Source-side credential propagation bug fixed
  - Remaining failure is now at the selected Azure resource / key pairing used by `azure-openai-responses-1775278346327`
- Blocker:
  - This is now a precise runtime/provider-config blocker, not another lost-credential source path in Bosun.

### 3. Representative real task path is still stalled end to end

- Task:
  - `8ed214ec-7eac-4887-8abd-97aa09c61694`
  - status: `blocked`
  - blockedReason: `repeated_no_commit_runs`
  - branch: `task/8ed214ec7eac-s-feat-server-normalize-wildcard-workspace-hint-`
  - base branch: `origin/main`
  - PR: none (`prNumber=null`, `prUrl=null`)
- Latest lifecycle run touching it:
  - `28150d83-0420-41b3-839d-f6fcd530bb00`
  - worktree: `C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-8ed214ec7eac-45df52a41a-recovery-moahxmwi`
  - executor/provider path: `azure-openai-responses`
- Workflow node path inspected:
  - `acquire-worktree` completed
  - `run-agent-plan` completed with a plan summary
  - `run-agent-tests` completed with blocked write/test-path summary
  - `run-agent-implement` completed with the old voice-approval symptom summary:
    - `run_workspace_command is denied`
    - `delegate_to_agent requires operator approval`
    - `No direct write/edit path is available`
- Current blocker:
  - no fresh Task Lifecycle retry has exercised this task since the newer feeder/runtime fixes; queue starvation still prevents a new end-to-end attempt

## Runtime proof and workflow behavior
- Branch: `bosun/codex-self-improvement-loop-commits`
- Local source vs `origin/main`: current (`ahead=191 behind=0`)
- Git status still includes pre-existing dirty work plus this cycle's source/test changes:
  - modified `agent/agent-launcher.mjs`
  - modified `agent/providers/_shared.mjs`
  - modified `infra/monitor.mjs`
  - modified `tests/agent-pool.test.mjs`
  - modified `tests/provider-kernel.test.mjs`
  - modified `tests/monitor-workflow-startup-guards.test.mjs`
- Active sink paths are advancing:
  - `.bosun\logs\monitor.log`
  - `.bosun\workflow-runs\index.json`
  - `.bosun\workflow-runs\_active-runs.json`
- Latest key workflow family behavior after reload:
  - `Task Replenish (Scheduled)` is still cycling and failing
  - `Task Planner` still does not trigger at `todo=4` / threshold `3`
  - `Task Batch Processor` still dispatches only `TASK-DIAG-LOCAL`
  - `Bosun PR Watchdog` is present but not producing merge throughput
- Throughput target is not met.

## Validation logs
- `npm test` log: `.bosun-monitor\npm-test-20260423-2152-provider-credential-fix.log`
- `npm run build` log: `.bosun-monitor\build-20260423-2154-provider-credential-fix.log`
- `npm run prepush:check` log: `.bosun-monitor\prepush-20260423-2154-provider-credential-fix.log`

## Narrowest next starting point
1. Validate the real Azure credential/resource pairing behind `harness.executors[id="azure-openai-responses-1775278346327"]`.
2. If that Azure endpoint/key is bad, make another executor genuinely runnable and preferred.
3. After provider correction, re-check in order:
   - a successful `Task Replenish (Scheduled)` run with parseable tasks
   - a non-diagnostic `Task Batch Processor` dispatch
   - a fresh Task Lifecycle retry for `8ed214ec-7eac-4887-8abd-97aa09c61694`
