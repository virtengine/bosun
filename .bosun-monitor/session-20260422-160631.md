# Bosun Monitor Session 2026-04-22 16:06:31

## Outcome

- Status: incident
- Branch: `bosun/codex-self-improvement-loop-commits`
- Package version: `0.43.0`
- Main freshness: `git rev-list --left-right --count origin/main...HEAD` => `0 190` (`0` behind, `190` ahead)
- Recent merged throughput: `git log origin/main --merges --since="2 hours ago" --oneline` returned no merges

## Open Blockers

1. Delayed startup history recovery still resumes only the newest interrupted task and leaves `8ed214ec-7eac-4887-8abd-97aa09c61694` stranded.
2. Task projection fields (`latestRunId`, `workflowName`, `sessionId`, `worktreePath`) are still blank in persisted task storage and `bosun task get` for both representative tasks.
3. `execution ledger write failed: Cannot create a string longer than 0x1fffffe8 characters` remains a recurring warning family.

## Symptom -> Proof -> Code Path -> Fix Or Blocker

### 1. Interrupted-run startup starvation: source fix landed, but live proof exposed a different startup-state bug

- Symptom:
  - On the old daemon, only `a6a0198e-a547-4eb7-872c-af2c83604b5f` ever re-entered `_active-runs.json`; `8ed214ec-7eac-4887-8abd-97aa09c61694` remained stranded `inprogress`.
  - After restarting on patched source, delayed history recovery still resumed only the `a6a...` task.
- Proof:
  - Before restart, `_active-runs.json` contained only `a6a...` (`runId=9fe8ae03-3f04-4ee7-ab52-5df8bd7f02a6`).
  - After restart on PID `112576`, startup logged:
    - `Detected 1 interrupted run(s): 9fe8ae03-3f04-4ee7-ab52-5df8bd7f02a6`
    - `Resuming 1 interrupted run(s)...`
    - `Resuming run 9fe8ae03-3f04-4ee7-ab52-5df8bd7f02a6 via retryRun(from_scratch) [task_claim_missing_on_resume]...`
  - `_active-runs.json` after delayed recovery contained only the new `a6a...` lifecycle run `0fc59217-88c4-43ee-aca5-1cabef9a3fe8`.
  - Direct index inspection still showed two paused resumable runs with distinct task identities:
    - `9fe8ae03-3f04-4ee7-ab52-5df8bd7f02a6` -> `a6a0198e-a547-4eb7-872c-af2c83604b5f`
    - `ca08ddd9-bc4d-4eb9-b485-69374996ccf2` -> `8ed214ec-7eac-4887-8abd-97aa09c61694`
  - Direct detail inspection confirmed `ca08...` carries the correct `8ed...` task identity and its own distinct `rootRunId`.
- Code path:
  - Patched: `workflow/workflow-engine.mjs` `resumeInterruptedRuns()`
  - Delayed caller: `infra/monitor.mjs` `workflow-history-unstick` startup recovery (`WORKFLOW_STARTUP_HISTORY_RECOVERY_DELAY_MS`, default `120000`)
- Fix applied this cycle:
  - Changed `resumeInterruptedRuns()` to queue `retryRun()` promises before awaiting them, so one long-lived resumed task cannot serially block later paused runs.
  - Added a focused regression in `tests/workflow-engine.test.mjs` that holds the first retry promise open and proves the second interrupted retry is still dispatched.
- Remaining blocker:
  - Live daemon behavior shows the delayed resume pass still collapses to the newest interrupted cohort only.
  - The strongest current hypothesis is that `_startupInterruptedRunIds` is being lost/null before the delayed `workflow-history-unstick` call, because the paused resumable set on disk still contains both distinct tasks while `resumeInterruptedRuns()` logs `Resuming 1 interrupted run(s)...`.

### 2. Task topology persistence is still blank in storage

- Symptom:
  - `bosun task get 8ed...` and `bosun task get a6a...` still show blank `latestRunId`, `workflowName`, `sessionId`, and `worktreePath`.
- Proof:
  - Both task queries after the patched restart still returned blank topology fields while remaining `status=inprogress` with `blockedReason=Repeated no-output agent starts detected; stopping retries`.
  - Direct Node reads of `.bosun/.cache/kanban-state.json` showed those fields absent from persisted task rows for both tasks.
- Code path:
  - Likely still in `workflow/workflow-nodes/actions.mjs` task-topology sync / task-store write path, not read-time projection.
- Blocker:
  - The earlier `action.run_agent` persistence repair from the prior continuation is still not live-proven here; persisted storage remains blank.

## Validation Status

- Focused regression passed:
  - `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/workflow-engine.test.mjs -t "does not block later interrupted resumes behind a long-running first retry"`
- Full suite passed:
  - `npm test` -> `__EXIT:0`
- Build passed:
  - `npm run build` -> `__EXIT:0`
- Pre-push validation:
  - `prepublish-check`, `syntax:check`, `prompt:lint`, and `smoke:packed-cli` all passed
  - `prepush:check` then entered `test:all`, which package.json defines as `npm run test:vitest && npm run test:node` (duplicate of the already-passed `npm test` coverage)
  - The redundant rerun was terminated after equivalence was confirmed from `package.json`

## Runtime State At End Of Cycle

- Daemon running: PID `112576`
- Active sink paths still advancing:
  - `.bosun/logs/monitor.log`
  - `.bosun/workflow-runs/index.json`
  - `.bosun/workflow-runs/_active-runs.json`
- Real workflow progress:
  - `a6a...` resumed into active run `0fc59217-88c4-43ee-aca5-1cabef9a3fe8`
  - `8ed...` remains stranded with latest run `ca08ddd9-bc4d-4eb9-b485-69374996ccf2` still `paused`, `resumable=true`
- Throughput target:
  - Not met; no non-monitor merges found in the last 2 hours

## Narrowest Next Starting Point

- Start at `workflow/workflow-engine.mjs` around the handoff between `_detectInterruptedRuns()` and `resumeInterruptedRuns()`.
- Prove why `_startupInterruptedRunIds` is no longer influencing the delayed `workflow-history-unstick` pass in `infra/monitor.mjs`:
  1. instrument or inspect every path that clears `_startupInterruptedRunIds`
  2. confirm whether the same engine instance survives from startup detection to delayed recovery
  3. add a focused regression for the live shape: two distinct paused resumable task runs, delayed resume call, and preserved startup interrupted set
