# Bosun Monitor Session 2026-04-23 21:01:59

- Status: incident
- Branch: `bosun/codex-self-improvement-loop-commits`
- Local source vs `origin/main`: current (`ahead=191 behind=0`)
- Recent non-monitor merge throughput: no `origin/main` merge commits in the last hour
- Daemon PID: `133512`
- Monitor child PID: `54484`

## Open blockers

1. Real task throughput is still stalled: task stats remain `draft=45`, `todo=4`, `inprogress=0`, `blocked=35`, and recent Task Batch runs still dispatch only `TASK-DIAG-LOCAL`.
2. The startup workflow-disablement bug is fixed and live, but the first enabled feeder runs now fail on provider auth before they can create or unblock real work.
3. Representative real task `8ed214ec-7eac-4887-8abd-97aa09c61694` is still `blocked` with no fresh Task Lifecycle retry after the daemon reload.
4. Broader validation is still noisy on an unrelated existing guard failure in `tests/monitor-workflow-startup-guards.test.mjs`.

## Symptom -> proof -> code path -> fix or blocker

### 1. Requested `workflowDefaults` templates were not being re-enabled at monitor startup

- Symptom:
  - This workspace explicitly requested workflow templates like `template-task-status-transition-manager`, `template-agent-session-monitor`, and `template-task-replenish`, but their installed copies stayed disabled in `.bosun/workflows`.
- Proof:
  - `.bosun/bosun.config.json` has `workflowDefaults.profile = "balanced"`, `autoInstall = true`, and an explicit template list including the status manager, agent session monitor, and task replenish templates.
  - Pre-fix installed workflow copies for those templates were `enabled: false`.
  - `infra/monitor.mjs` computed `requestedTemplateIds` from `workflowDefaults`/profile, but only iterated `typedWorkflowTemplateConfig.templateIds` when force-installing/enabling templates.
- Code path:
  - `infra/monitor.mjs` startup workflow sync block around `requestedTemplateIds` / `typedWorkflowTemplateConfig`.
- Fix:
  - Patched `infra/monitor.mjs` so startup sync now iterates the full requested template set, merges `workflowDefaults.templateOverridesById` with typed workflow overrides, respects `workflowDefaults.autoInstall` for default/profile templates, and marks synced workflows as coming from `workflowDefaults` or `workflows.config`.
  - Added a startup guard regression in `tests/monitor-workflow-startup-guards.test.mjs`.
- Live proof after reload:
  - Restarted the daemon (`76568 -> 133512`).
  - `.bosun/workflows` now shows the canonical requested copies enabled and tagged `configuredFrom: workflowDefaults`:
    - `0f366ec4-96dc-4315-99ea-e4644df7bf61` — `Agent Session Monitor` → `enabled=true`
    - `46100e3f-50f1-4367-9a2d-4a3270548278` — `Task Status Transition Manager` → `enabled=true`
    - `704f363c-dac1-4a0e-9945-68a090bf4b8d` — `Task Replenish (Scheduled)` → `enabled=true`
  - The accidental duplicate copies stayed disabled:
    - `81e33642-5b64-4dc5-b9b0-7f310a8cafc4` — `Agent Session Monitor` → `enabled=false`
    - `4a3520d5-b9bd-45fe-b03c-436bc1258521` — `Task Status Transition Manager` → `enabled=false`
  - Monitor log immediately showed schedule-triggered runs for `Agent Session Monitor`, `Task Replenish (Scheduled)`, `Dependency Audit`, `Workspace Hygiene`, and `PR Conflict Resolver`.

### 2. New narrowest blocker: enabled feeder workflows fail on provider auth/env before materializing real work

- Symptom:
  - The live feeder path is now executing, but agent-backed planner/fix steps fail before producing tasks or fixes.
- Proof:
  - `.bosun/workflow-runs/47d70dbc-df2e-47a7-ab75-3cca137e724e.json` (`Task Replenish (Scheduled)`)
    - `check-backlog.triggered = true`
    - `todoCount = 4`, `threshold = 5`
    - `run-planner.summary = "Agent error: Missing environment variable: \`AZURE_OPENAI_API_KEY\`."`
    - `materialize-tasks` retried 4 times and failed with `Planner output from "run-planner" did not include parseable tasks.`
  - `.bosun/workflow-runs/a89f630a-36c6-483a-ac45-9f07d790cf38.json` (`Dependency Audit`)
    - `auto-fix.summary = "Agent error: Missing environment variable: \`AZURE_OPENAI_API_KEY\`."`
    - runtime error payload shows Azure OpenAI `401` invalid subscription key / wrong endpoint
  - `.bosun/logs/monitor.log` now repeatedly shows:
    - `primary SDK "openai-native" failed ... Access denied due to invalid subscription key or wrong API endpoint`
    - fallback `codex` fails with `Codex Exec exited with code 1: Reading prompt from stdin...`
    - fallback `copilot` fails because `@github/copilot-sdk` cannot resolve `vscode-jsonrpc/node`
- Code path:
  - `workflow-templates/planning.mjs` `Task Replenish (Scheduled)` → `agent.run_planner`
  - runtime agent execution path selected in monitor log:
    - `SDK selected: openai-native (via harness primary executor "azure-openai-responses")`
- Blocker:
  - This is now an environment / executor-health blocker, not a workflow-disablement bug.
  - Until the Azure/OpenAI credentials+endpoint (or the default executor routing) are fixed, the newly-enabled feeder workflows cannot create real backlog work.

### 3. Task Batch is still mechanically healthy but starved of real dispatchable work

- Symptom:
  - Task Batch keeps cycling, but still only dispatches the diagnostic placeholder path.
- Proof:
  - Recent Task Batch runs:
    - `f3616f4a-4a3e-4c10-a6e1-72bec94dd457` → `taskId = TASK-DIAG-LOCAL`
    - earlier post-reload batch runs still show `TASK-DIAG-LOCAL`
  - Current task stats remain unchanged at `draft=45`, `todo=4`, `inprogress=0`, `blocked=35`.
  - `Task Planner` run `a27160e0-d24f-4619-8411-cc8a0dbe44bf` did not trigger because `todoCount=4` was above its threshold `3`, so the only live replenishment path is the hourly scheduled replenish workflow — and that path is now failing on provider auth.
- Representative real task path:
  - `8ed214ec-7eac-4887-8abd-97aa09c61694` remains `blocked`
  - no fresh Task Lifecycle retry has happened since the daemon reload
- Blocker:
  - The workflow-disablement issue is fixed, but the real queue is still starved because the first post-fix feeder path cannot generate tasks under the current agent credential/runtime state.

## Validation

- Focused new guard passed:
  - `npx vitest run tests\monitor-workflow-startup-guards.test.mjs -t "re-enables requested workflowDefaults templates during startup sync" --reporter verbose`
- `npm run build` passed.
- `npm run prepush:check` still fails on an unrelated existing assertion:
  - `tests/monitor-workflow-startup-guards.test.mjs`
  - `monitor workflow startup guards > runs task recovery immediately after startup workflow cleanup when lifecycle is delegated`
- Full-file run of `tests/monitor-workflow-startup-guards.test.mjs` still hits the same unrelated existing failure, but the new startup-sync guard passes.

## Runtime proof and workflow behavior

- Active sink paths are advancing:
  - `.bosun/logs/monitor.log`
  - `.bosun/workflow-runs/_active-runs.json`
  - `.bosun/workflow-runs/index.json`
- Startup schedule poll fired 8 workflow runs immediately after reload.
- Verified live non-monitor throughput after the fix:
  - `Agent Session Monitor` ran successfully
  - `Task Replenish (Scheduled)` ran and reached `run-planner`
  - `Dependency Audit` ran and reached `auto-fix`
  - `Workspace Hygiene` ran
- The live incident has moved:
  - **before** this cycle: requested task/reliability templates never re-enabled
  - **after** this cycle: requested templates are live, but agent-backed feeder nodes fail on provider auth/env

## Git / worktree state

- This cycle changed:
  - `infra/monitor.mjs`
  - `tests/monitor-workflow-startup-guards.test.mjs`
- Existing untracked monitor handoff note remains present:
  - `.bosun-monitor/session-20260423-203830.md`

## Narrowest next starting point

1. Fix executor credential health / routing so `agent.run_planner` stops selecting the dead Azure OpenAI path when `AZURE_OPENAI_API_KEY` is unavailable or invalid.
2. Reload the daemon and watch the next `Task Replenish (Scheduled)` run first; confirm `run-planner` returns parseable tasks and `materialize-tasks` succeeds.
3. Then inspect the first non-diagnostic Task Batch dispatch and the first fresh Task Lifecycle retry for a real task (preferably `8ed214ec-7eac-4887-8abd-97aa09c61694`) to confirm end-to-end recovery.
