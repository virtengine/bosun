incident

# Bosun Local Ops Monitor — 2026-04-25 05:02:38 local

## Startup / scope
- Resumed from the latest monitor context and current plan.
- Kept the daemon locked/off and stayed simulator-first.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Current open-blocker list
1. **Simulator gate still blocked** — no real non-monitor task has completed implementation -> validation -> PR -> review/fix -> merge.
2. **The old tests-session bind stall is stale** — the fresh simulator run advanced through plan, tests, and implementation.
3. **The worktree `cwd` leak is live-proven fixed** — implementation edits now land in the task worktree, not the repo root.
4. **Current narrow blocker: pre-PR validation failed and the autofix branch delegated into the wrong workflow despite `delegateTaskWorkflow: false`.**
5. **Throughput remains incident-level** — still no non-monitor merge landed.

## Fresh simulator evidence
- Cleared the stale simulator owner PID `3312`.
- Fresh simulator run started successfully:
  - parent run: `f2ea7ffe-5008-4028-8df2-fcc408b33157`
  - owner PID: `26920`
- The run claimed the task, reused the correct worktree, and advanced:
  - `run-agent-plan` completed
  - `run-agent-tests` completed
  - `run-agent-implement` completed
- `run-agent-implement` produced a real worktree-only patch:
  - task worktree showed `M workspace/shared-knowledge.mjs`
  - repo root still only showed the unrelated `M tests/fleet-coordinator.test.mjs`
- Worktree diff from the successful implementation pass:
  - `workspace/shared-knowledge.mjs`
  - extracted inline rate-limit evaluation into `isRateLimited(...)`
  - preserved keyed per-scope throttling and simplified the wait calculation
- The workflow auto-committed the implementation:
  - `bbcc52cb chore: auto-commit agent work (f82209df-e23)`

## What the simulator proved this cycle
### 1. The previous tests-session stall is no longer the live blocker
- `run-agent-tests` emitted normal tool activity and completed successfully.
- `run-agent-implement` emitted normal tool activity, edited the worktree, and completed successfully.

### 2. The earlier Bosun-native tool `cwd` fix is now live-proven
- The fresh real workflow-agent implementation pass wrote to:
  - `C:\Users\jON\Documents\source\repos\virtengine-gh\bosun\.bosun\worktrees\task-f82209dfe236-666ab4d38c`
- It did **not** leak the source edit back into the repo root.

## New narrowest blocker
### Symptom
- After implementation auto-committed, `pre-pr-validation` failed on:
  - `npm run prepush:check`
- The task-lifecycle workflow entered `auto-fix-validation`.
- Instead of staying local and repairing the validation failure, it launched a delegated child workflow that started solving an unrelated backend-planner problem and edited:
  - `tests/workflow-engine.test.mjs`

### Proof
- Parent run:
  - `.bosun\workflow-runs\f2ea7ffe-5008-4028-8df2-fcc408b33157.json`
- Relevant parent logs:
  - `pre-pr-validation` -> `Running: npm run prepush:check`
  - `pre-pr-validation-ok` -> expression evaluated `false`
  - `auto-fix-validation` -> only initial `Running agent...` log in the parent
- Parent node state:
  - `auto-fix-validation.status = running`
  - `auto-fix-validation.startedAt = null`
  - `auto-fix-validation.outputSummary = null`
- The parent run also recorded delegation transition keys for that node:
  - `assign:auto-fix-validation:eba1695c-8110-4414-b862-0df038a61f80:f82209df-e236-4a63-9ce8-67122a1ca96b`
- Active delegated child run:
  - `561bacf4-a663-4ad1-aa18-92531ffe9ec8`
- Child-run behavior:
  - planner/write-tests/implement flow of a backend agent workflow
  - `write-tests` edited `tests/workflow-engine.test.mjs`
  - later `implement` edited `workflow/workflow-nodes.mjs` and `workflow/workflow-nodes/agent.mjs`
- Child workflow definition on disk:
  - `.bosun\.bosun\workflows\711f1fa4-35e2-4855-be7a-248e83628f2f.json`
  - `name: "Task Completion Agent"`
  - generic backend test-first workflow, unrelated to the current persistent-memory validation failure

### Why this is a real bug
- `action.run_agent` only delegates when:
  - `node.config?.delegateTaskWorkflow !== false`
- The task-lifecycle template explicitly sets:
  - `run-agent-plan.delegateTaskWorkflow = false`
  - `run-agent-tests.delegateTaskWorkflow = false`
  - `run-agent-implement.delegateTaskWorkflow = false`
  - `auto-fix-validation.delegateTaskWorkflow = false`
- Live behavior contradicts that contract:
  - `auto-fix-validation` still delegated into a replacement workflow.

## Durable source fix landed this cycle
1. **Harden the run-agent delegation gate against materialized string booleans**
   - File:
     - `workflow/workflow-nodes/actions.mjs`
   - Change:
     - replaced the raw `node.config?.delegateTaskWorkflow !== false` check with:
       - `parseBooleanSetting(node.config?.delegateTaskWorkflow, true)`
   - Why:
     - the direct unit test already covered real boolean `false`, but the live engine/materialization path appears to be letting a boolean-like `"false"` through, which would make the old guard evaluate truthy and wrongly delegate.

2. **Added a regression for the engine-shaped `"false"` case**
   - File:
     - `tests/workflow-engine.test.mjs`
   - New coverage:
     - `runs task-backed agents locally when delegateTaskWorkflow is the string false`

## Validation completed this cycle
- `npm run build` passed.
- Focused vitest execution for the new regression did not complete cleanly in this environment before handoff, so only the build is fully confirmed in-cycle.

## Notes / hazards
- The current live simulator run and its delegated child both started **before** the source fix above was landed, so they do not prove the new delegation guard yet.
- The repo is still dirty with unrelated changes; avoid broad cleanup.
- `tests/workflow-engine.test.mjs` in the root checkout already had unrelated churn while this cycle was running, so treat diffs in that file carefully.

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first; keep the daemon locked/off.
3. Re-read:
   - `.bosun\workflow-runs\f2ea7ffe-5008-4028-8df2-fcc408b33157.json`
   - `.bosun\workflow-runs\561bacf4-a663-4ad1-aa18-92531ffe9ec8.json`
4. Stop/recycle the current simulator owner only after capturing enough evidence from the misdelegated autofix branch.
5. Rerun:
   - `bosun simulate task restart --json --config-dir .bosun --repo-root .`
6. On the fresh post-fix run, verify specifically that:
   - implementation still lands in the task worktree
   - `pre-pr-validation` failure, if it reproduces, keeps `auto-fix-validation` local
   - no delegated backend child workflow is launched when `delegateTaskWorkflow` is false
7. Continue only after that through review -> PR -> merge, or capture the next precise blocker with proof.

incident
