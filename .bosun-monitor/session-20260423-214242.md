# Bosun monitor handoff - 2026-04-23 21:42:42

Status: incident

## Summary
- Landed a launcher-side source fix in `agent/agent-launcher.mjs` so harness-mode native selection no longer hard-pins the configured primary executor when that executor is enabled but cannot authenticate.
- Added a focused regression in `tests/agent-pool.test.mjs` proving openai-native launches fall through to a runnable secondary Azure executor when the configured primary cannot run.
- Reloaded Bosun with `bosun --stop-daemon --config-dir .bosun --repo-root .` then `bosun --daemon --config-dir .bosun --repo-root . --no-update-check`; new daemon PID is `120744`.

## Validation
- Passed: `npx vitest run tests\agent-pool.test.mjs -t "falls through to a runnable harness native executor when the configured primary cannot run" --reporter verbose`
- Passed: `npm run build`
- Broader suite capture remains incomplete in this shell environment:
  - `npm test` and `npx vitest run` both timed out in the wrapper without returning a final status
  - follow-up process checks showed no lingering `npm` / `vitest` processes after those timeouts

## Runtime proof after reload
- The launcher fix is live. `monitor.log` now shows:
  - `SDK selected: openai-native (via harness primary executor "azure-openai-responses-1775278346327")`
  - before this fix, Bosun kept selecting the auth-dead configured primary `azure-openai-responses`
- Harness startup log now reflects the reloaded runtime:
  - `agentRuntime=harness harness.enabled=true primaryExecutor=azure-openai-responses defaultProvider=azure-openai-responses executors=3/3 primaryAdapter=codex-sdk routing=openai-native-adapter`

## Current narrowest blocker
- The new selected executor `azure-openai-responses-1775278346327` is the only auth-runnable native executor Bosun can see locally, but real calls still fail with Azure auth/endpoint errors.
- Concrete proof:
  - `Task Replenish (Scheduled)` run `841badfb-bd61-4231-903c-6f04194b4766`
    - `nodeOutputs["run-planner"].summary = "Agent error: Missing environment variable: \`AZURE_OPENAI_API_KEY\`."`
    - `nodeOutputs["run-planner"].items[0].summary` shows `OpenAI API error 401` / `invalid subscription key or wrong API endpoint`
    - workflow then fails at `materialize-tasks` because planner output is empty
  - `monitor.log` repeats the same `primary SDK "openai-native" failed ... 401 invalid subscription key or wrong API endpoint` after the selector fix
- The adapter path itself does not appear to be the remaining bug:
  - `shell/openai-native-adapter.mjs` already treats `/openai/v1` Azure endpoints as OpenAI-compatible and ignores classic deployment-path construction for that branch
  - repo tests already cover URL-shaped Azure `deployment` values in `tests/provider-kernel.test.mjs`

## Live throughput / task state
- `bosun task stats --json` after reload:
  - `draft=45`
  - `todo=4`
  - `blocked=35`
  - `inprogress=0`
- No non-monitor merge throughput in the last hour.
- Latest `Task Batch Processor` run is still diagnostics-only:
  - run `07354c81-6003-422c-93bc-36ead92eb884`
  - `taskId = "TASK-DIAG-LOCAL"`
- Representative real task still has no fresh lifecycle retry:
  - task `8ed214ec-7eac-4887-8abd-97aa09c61694` remains `status: "blocked"`
  - latest lifecycle run touching it is still `28150d83-0420-41b3-839d-f6fcd530bb00` from `2026-04-22T20:19:28Z`

## Open blockers
1. `azure-openai-responses-1775278346327` is selected correctly now, but its effective Azure credential/endpoint is still rejected at runtime.
2. Because replenish still fails, Task Batch keeps dispatching only `TASK-DIAG-LOCAL`, and real blocked tasks do not get fresh lifecycle retries.

## Narrowest next starting point
1. Validate the effective secondary Azure executor config in `.bosun/bosun.config.json`:
   - `harness.executors[id="azure-openai-responses-1775278346327"]`
   - bound credential value from `authBindings.apiKeyEnv`
   - endpoint/resource actually matching that key
2. If that executor cannot be fixed quickly, make another executor truly runnable and preferred.
3. After any credential/provider correction, re-check:
   - a fresh successful `Task Replenish (Scheduled)` run with parseable tasks
   - a fresh `Task Batch Processor` run dispatching a non-diagnostic task
   - a fresh `Task Lifecycle` retry for `8ed214ec-7eac-4887-8abd-97aa09c61694`
