# Bosun Monitor Session 2026-04-22 17:42:27

## Outcome

- Status: incident
- Branch: `bosun/codex-self-improvement-loop-commits`
- Package version: `0.43.0`
- Main freshness: `git rev-list --left-right --count origin/main...HEAD` => `0 190` (`0` behind, `190` ahead)
- Branch tracking: `git status --short --branch` => `ahead 2` of `origin/bosun/codex-self-improvement-loop-commits`
- Recent merged throughput: `git log origin/main --merges --since="2 hours ago" --oneline` returned no merges

## Open Blockers

1. The stale-session recovery fix in `action.run_agent` is now source-validated and loaded into a fresh daemon PID, but live runtime still has no automatic entrypoint that replays the stranded task-lifecycle rows, so the fix has not been exercised against a real resumed task.
2. Representative task rows still expose stale top-level projection fields (`workflowId`, `workflowName`, `latestRunId`, `sessionId`, `worktreePath`) even though `task.topology.*` remains populated.
3. `execution ledger write failed: Cannot create a string longer than 0x1fffffe8 characters` still appears on daemon startup.
4. Throughput target is still not met; no non-monitor merges landed in the last 2 hours, `_active-runs.json` is empty, and task-batch cycles are still `0/0`.

## Symptom -> Proof -> Code Path -> Fix Or Blocker

### 1. Task-backed stale-session recovery was reusing poisoned thread metadata keyed by `taskKey`

- Symptom:
  - Representative child retries `b53c998f-539d-49ce-b69e-c2191b1b2208` and `97c835c9-08b1-4f1e-9936-9d808996eaf2` failed at `run-agent-plan` with `Cannot read properties of null (reading 'sessionId')` even though recovery logged a fresh managed session retry.
- Proof:
  - `b53c998f-...` run detail shows:
    - `_workflowTaskId=8ed214ec-7eac-4887-8abd-97aa09c61694`
    - `parentRunId=3e50ac05-2db3-4a5c-ae1c-2ef9fdc2dbd3`
    - `retryMode=from_failed`
    - failed node `run-agent-plan`
    - `sdk=openai-native`
    - `cwd=C:/Users/jON/Documents/source/repos/virtengine-gh/bosun/.bosun/worktrees/task-8ed214ec7eac-45df52a41a-recovery-mo9mf9zx`
    - summary `Failed at Agent Plan: Cannot read properties of null (reading 'sessionId')`
  - Source tracing showed the stale-session retry path only changed `sessionId`; it did not invalidate the persistent thread cache keyed by the unchanged task-backed `taskKey`.
- Code path:
  - `workflow/workflow-nodes/actions.mjs`
    - task-backed `action.run_agent`
    - stale-session recovery catch for `Cannot read properties of null (reading 'sessionId')`
  - `agent/harness-agent-service.mjs`
  - `agent/agent-launcher.mjs`
  - persistent thread registry keyed by `taskKey`
- Fix applied:
  - `action.run_agent` now calls `harnessAgentService.invalidateThread(recoveryTaskKey)` before retrying with a fresh managed session.
- Regression added:
  - `tests/workflow-engine.test.mjs`
    - `invalidates a stale task-key thread before retrying with a fresh managed session`
- Validation:
  - Focused regression passed.
  - `npm test` passed.
  - `npm run build` passed.
  - `npm run prepush:check` passed end to end, including `prepublish-check`, `syntax:check`, `prompt:lint`, `smoke:packed-cli`, and `test:all`.

### 2. The fix is live in daemon PID `119492`, but no current runtime path re-enters the stranded task-lifecycle rows

- Symptom:
  - After restarting the daemon onto the patched source, runtime remains flat:
    - `_active-runs.json` is `[]`
    - task batch keeps logging `0/0 succeeded (0 failed)`
    - representative tasks remain stuck `inprogress` with the old blocked reason and stale projection fields
- Proof:
  - Daemon restart succeeded:
    - old PID `128712` stopped
    - new daemon PID `119492` started
  - Fresh runtime tail confirms the new process is running normally, but no new startup interrupted-run detection lines were emitted for the representative stalled families.
  - The delayed startup recovery pass on the new daemon also no-op'd:
    - `2026-04-22T07:42:25.640Z` startup `workflow-history-unstick` attempt
    - `2026-04-22T07:42:25.641Z` success with `durationMs: 1`
    - no accompanying `Detected ... interrupted run(s)` or `Resuming ... interrupted run(s)...` lines for the `07:40` restart window
  - `Select-String` over the recovery log families shows the newest `Detected ... interrupted run(s)` lines are still the old `06:55:52` cohort for `2416976b...`, `e2ef0b96...`, `3e50ac05...`, and `43774197...`; there are no new detection lines after the `07:40` restart.
  - Post-restart runtime state:
    - `_active-runs.json` => `[]`
    - repeated monitor tail lines:
      - `Task batch completed: 0/0 succeeded (0 failed)`
  - Current task-store state for both representative tasks still shows:
    - top-level `workflowId/workflowName/latestRunId/sessionId/worktreePath = null`
    - `status=inprogress`
    - `blockedReason="Repeated no-output agent starts detected; stopping retries"`
    - populated `task.topology.latestRunId/workflowName/sessionId`
- Blocker interpretation:
  - The source fix is loaded, but the existing stranded rows are no longer entering startup interrupted-run recovery.
  - That means Bosun currently has no automatic runtime path that would exercise the new `invalidateThread(taskKey)` logic against the affected real tasks.

### 3. Representative task path inspected end to end still terminates before any new live recovery

- Path inspected in depth:
  - task `8ed214ec-7eac-4887-8abd-97aa09c61694`
  - parent resumed row `3e50ac05-2db3-4a5c-ae1c-2ef9fdc2dbd3` (`resumeResult=resumed`, `retryMode=from_failed`)
  - child retry row `b53c998f-539d-49ce-b69e-c2191b1b2208`
  - failed node `run-agent-plan`
  - agent `openai-native`
  - worktree `C:/Users/jON/Documents/source/repos/virtengine-gh/bosun/.bosun/worktrees/task-8ed214ec7eac-45df52a41a-recovery-mo9mf9zx`
  - claim owner `null`
  - shared-state owner `null`
  - PR link `null`
  - review status `null`
- Current task row proof:
  - `status=inprogress`
  - `updatedAt=2026-04-22T06:52:29.886Z`
  - `blockedReason="Repeated no-output agent starts detected; stopping retries"`
  - `cooldownUntil=2026-04-22T05:51:54.942Z`
  - stale top-level fields remain `null`
  - `task.topology.latestRunId=2416976b-5875-4945-a4a3-cb87f72a8386`
- Parallel family remains consistent:
  - task `a6a0198e-a547-4eb7-872c-af2c83604b5f`
  - resumed parent `43774197-9d76-4364-b441-3d6bc02f981c`
  - child retry `97c835c9-08b1-4f1e-9936-9d808996eaf2`
  - same null `sessionId` crash family before the fix

### 4. Queue liveness does not equal workflow health

- Runtime proof:
  - `bosun task stats --json` reports:
    - `todo=4`
    - `inprogress=2`
    - `blocked=33`
    - `total=191`
  - despite that, post-restart task-batch cycles repeatedly log `0/0 succeeded (0 failed)`
- Interpretation:
  - scheduler/workflow polling is alive
  - real backlog work is not progressing through task execution
  - throughput target remains missed

## Validation Status

- Focused regression passed:
  - `npx vitest run tests/workflow-engine.test.mjs -t "invalidates a stale task-key thread before retrying with a fresh managed session"`
- Full suite passed:
  - `npm test`
- Build passed:
  - `npm run build`
- Pre-push validation passed fully:
  - `npm run prepush:check`
- Runtime reload performed:
  - daemon restarted onto patched source (`128712` -> `119492`)

## Runtime State At End Of Cycle

- Daemon running: PID `119492`
- Active sink paths advancing:
  - `.bosun/logs/monitor.log`
  - `.bosun/workflow-runs/index.json`
  - `.bosun/workflow-runs/_active-runs.json`
- Real workflow progress:
  - PR watchdog and task-batch workflows are scheduling and completing
  - task-batch still dispatches no real work (`0/0`)
  - representative task-lifecycle runs remain stranded and no fresh active run has been recreated
- Throughput target:
  - not met
- Local source vs main:
  - local source reflects latest merged `origin/main` (`0` behind)

## Narrowest Next Starting Point

- Leave the `action.run_agent` stale-thread invalidation fix in place; it is validated and live-loaded.
- Start at the runtime intake that decides whether stranded task-lifecycle failures can re-enter recovery after they have already become `paused` and `resumable=false`.
- Most likely next hop:
  1. inspect `workflow/workflow-engine.mjs::_detectInterruptedRuns()` and adjacent startup recovery intake rules for why PID `119492` did not detect the representative stalled families at all after restart
  2. inspect whether a separate path should rehydrate or reopen paused failed child retries like `b53c...` / `97c...` when the parent rows `3e50...` / `43774197...` are already marked `resumed`
  3. inspect why top-level task projection fields remain null while `task.topology.*` is still populated for the same rows
  4. if startup intake is intentionally excluding these rows, add a focused reproducer for the exact live shape: patched daemon restart, stranded paused/non-resumable task-lifecycle rows, no interrupted-run detection, empty `_active-runs.json`, and task-batch `0/0`