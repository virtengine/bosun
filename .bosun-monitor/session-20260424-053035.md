# Bosun monitor session 2026-04-24 05:30:35

## Open blockers
- Non-monitor merged PR throughput is still `0` in the last hour.
- Task family `97d41516-caca-4fa0-9175-dbe6ad4ce01d` is no longer primarily a startup-resume stall; fresh evidence shows retry run `bfbfaa72-23d5-4485-8ca6-a3469cb0b3b4` eventually reached `write-tests` and then failed with `Invalid string length`.
- Full validation is still blocked by unrelated repo issues:
  - `npm test` hangs in `pretest` at `node tools/generate-demo-defaults.mjs`
  - `npm run prepush:check` fails on pre-existing missing `package.json` `files` entries for `infra/storage-janitor.mjs` and several `shell/*` modules

## Symptom -> proof -> code path -> fix or blocker
- Symptom: historical interrupted same-task backend chains could select an older ancestor run over the newest descendant during startup dedupe.
  - Proof: `97d41516-...` family had ancestor `6ecf00dd-...` and newer descendants `47a0e0ce-...` / `93547f92-...`; the engine policy and existing regression test both preferred the ancestor when parent/child shared the same task identity.
  - Code path: `workflow/workflow-engine.mjs` `resolveRepresentativeTaskRun()`.
  - Fix: keep ancestor preference only across different workflow families; keep the newest descendant when parent and child share the same `workflowId`.
- Symptom: fresh live failure on the same family is now `Invalid string length`, not an idle resume stall.
  - Proof: run `bfbfaa72-23d5-4485-8ca6-a3469cb0b3b4` last wrote at `2026-04-24T05:28:12+10:00`, logged `Agent completed: success=true streamEvents=6`, then retried three times and ended `ERROR: Invalid string length` at `write-tests`. Reference run `6e3b686b-5c99-4716-a307-07b9b2ae2b06` completed `write-tests` and advanced into `implement`.
  - Code path: active-run detail persistence in `workflow/workflow-engine.mjs` writes full running detail snapshots; `_trimOversizedRunDetail()` previously returned the raw detail unchanged when `JSON.stringify(detail)` itself threw, allowing `writeJsonFileAtomically()` to fail with `Invalid string length`.
  - Fix: `_trimOversizedRunDetail()` now falls back to compacted/minimal persisted detail when direct serialization fails, and it opportunistically compacts oversized detail before deeper trimming.

## Validation
- Passed focused regression suite:
  - `prefers the interrupted same-task ancestor run over a newer child workflow run`
  - `keeps the newest same-workflow descendant over an older same-task ancestor run`
  - `compacts run detail when direct serialization throws invalid string length`
- `npm run build` passed.
- `npm test` still hangs in pretest (known unrelated blocker).
- `npm run prepush:check` still fails on pre-existing package manifest drift unrelated to this fix.

## Runtime state
- Branch remains `bosun/codex-self-improvement-loop-commits`.
- Daemon was restarted on the latest source and is running with pinned `.bosun` config/repo-root paths.
- Advancing sinks confirmed: `logs/daemon.log`, `.bosun/workflow-runs/index.json`, `.bosun/workflow-runs/_active-runs.json`, `.bosun/.cache/kanban-state.json`.
- Fresh runtime evidence shows real workflows are progressing (`6e3b686b-...` advanced from `write-tests` to `implement`), but throughput is still below target and no fresh non-monitor merge was observed.

## Local source vs main
- `HEAD` is `0` behind `origin/main` (`git rev-list --left-right --count origin/main...HEAD` => `0 191`), so local source reflects the latest merged `main`.

## Narrowest next handoff
- Start from fresh run `bfbfaa72-23d5-4485-8ca6-a3469cb0b3b4` and prove whether the restarted daemon plus the new active-run detail compaction stops the remaining `Invalid string length` failure on the next comparable backend `write-tests` attempt; use `6e3b686b-...` as the healthy reference and avoid more manual task resets unless a new live run exists.
