## Bosun Local Ops Monitor - 2026-04-26 17:46:31

### Startup sequence
- Read latest three notes before touching runtime state:
  - `.bosun-monitor/session-20260426-171250.md`
  - `.bosun-monitor/session-20260426-162427.md`
  - `.bosun-monitor/session-20260426-160322.md`
- Confirmed source branch:
  - `bosun/codex-self-improvement-loop-commits`
- Rebuilt the blocker list from the newest note before touching broader runtime state.

### Rebuilt open-blocker list from the newest note
1. Finish the live `e274...` simulator validation first because the previous cycle had a new source fix but no end-to-end proof yet.
2. If the real task still reused a contaminated branch or worktree family, patch the smallest source path that chooses or reuses that branch.
3. Treat root dependency drift as a second blocker unless it directly breaks simulator validation again.

### Baseline
- branch:
  - `bosun/codex-self-improvement-loop-commits`
- package version:
  - `0.43.0`
- local vs `origin/main` after refresh:
  - behind `0`
  - ahead `231`

### Real task path inspected in depth this cycle
- task:
  - `e274fdfc-65a8-44ff-8495-7fa6b1965896`
  - `[m] feat(workflow): persist node completion checkpoints for resume-safe execution`
- inspected artifacts and states:
  - blocked run `7fe03183-22f6-46ee-92ba-136a58c7f6d9`
  - restarted run `7b897e46-6633-4011-8348-fcff9b70b875`
  - corrected-base restart run `c163be43-06f4-4195-8257-5133a3c7293e`
  - task row, active-run index, branch diff vs `origin/main`, worktree family, simulator PID ownership

### What changed this cycle
1. **Retired the simulator base-branch contamination seam.**
   - Proof:
     - current task row now shows:
       - `baseBranch = origin/main`
     - corrected restart run `c163...` `acquire-worktree` output now shows:
       - `baseBranch = origin/main`
   - Root cause:
     - `task/task-simulate-cli.mjs`
     - `resolveTaskSimulationDefaultTargetBranch()` preferred the current local ops source branch
       - `bosun/codex-self-improvement-loop-commits`
     - `buildSimulationTaskInput()` also preserved already-stored task bases on that branch
   - Durable fix:
     - `task/task-simulate-cli.mjs`
       - stop treating the Bosun local ops branch as the simulator default target branch
       - rewrite stored task `baseBranch` back to the resolved default target branch when the stored value is the Bosun local ops branch
     - `tests/task-simulate-cli.test.mjs`
       - updated branch-preference expectations for the local ops branch
       - kept generic feature-branch preference coverage
       - added regression proving stored Bosun local-ops task bases get rewritten back to `origin/main`

2. **Extended stale task-branch recovery beyond the detached-path case.**
   - Root cause:
     - `workflow/workflow-nodes/actions.mjs`
     - stale-branch recreation only handled detached or invalidated branch paths
     - reusable or race-reused managed worktrees could still keep a no-PR task branch with hundreds of files of stale history
   - Durable fix in progress:
     - `workflow/workflow-nodes/actions.mjs`
       - `shouldRecreateDetachedTaskBranch()` broadened into stale-task-branch detection keyed off the diff threshold itself, not only “behind latest base”
       - added invalidation of stale reusable task branches in:
         - reusable worktree sync path
         - race-reuse path
       - existing branch recreation backup/delete path now uses the broader stale-task-branch predicate
     - `tests/workflow-task-lifecycle.test.mjs`
       - existing detached stale branch regression still covers the no-worktree case
       - existing attached-invalid regression now covers invalid attached worktrees
       - new regression added for:
         - healthy-looking attached managed worktree
         - no PR
         - oversized stale branch diff

### Validation
- Syntax checks passed:
  - `node --check task\task-simulate-cli.mjs`
  - `node --check tests\task-simulate-cli.test.mjs`
  - `node --check workflow\workflow-nodes\actions.mjs`
  - `node --check tests\workflow-task-lifecycle.test.mjs`
- Focused simulator-base tests passed while dependencies were present:
  - `tests/task-simulate-cli.test.mjs`
    - local ops branch no longer becomes the default target branch
    - installed workflow now carries `origin/main` under that branch
- Focused stale-branch regressions partially passed while dependencies were present:
  - detached stale task branch case
  - attached-invalid managed worktree case
- Final combined reruns for the newest reusable-worktree stale-branch patch were interrupted by recurring root dependency drift:
  - `node_modules\vitest\vitest.mjs`
    - disappeared again
  - `node_modules\@babel\runtime\helpers\interopRequireDefault.js`
    - disappeared again
  - latest snapshot:
    - `rootVitest = false`
    - `rootBabelRuntime = false`
- `npm run build` was **not** rerun this cycle because the newest branch-reuse fix is not fully validated yet.

### Live simulator evidence this cycle
- Pre-fix restart run:
  - `7b897e46-6633-4011-8348-fcff9b70b875`
  - stayed on contaminated branch state
  - stalled at:
    - `run-agent-tests`
  - proof of stall:
    - no live `vitest` PID
    - run file stopped updating over a multi-minute watch
- That stale simulator process still held the simulator slot:
  - PID `30404`
  - command:
    - `bosun ... simulate task restart --json --config-dir .bosun --repo-root .`
  - action taken:
    - stopped that exact PID so a corrected restart could run
- Corrected restart after the simulator base fix:
  - force-claimed the stale simulator slot
  - new active run:
    - `c163be43-06f4-4195-8257-5133a3c7293e`
  - current task row:
    - `status = inprogress`
    - `blockedReason = null`
    - `latestRunId = c163...`
    - `baseBranch = origin/main`
    - `branchName = task/e274fdfc65a8-m-feat-workflow-persist-node-completion-checkpoi`
    - `worktreePath = ...task-e274fdfc65a8-e7c58c9933-recovery-mofgh1o1`
  - current running node:
    - `run-agent-tests`
- But the branch is still contaminated:
  - branch exists locally
  - `commitCount vs origin/main = 234`
  - `changedFileCount vs origin/main = 798`
  - `acquire-worktree` output for `c163...` proves why:
    - `created = false`
    - `reused = true`
    - `reusedExistingBranch = true`
    - `baseBranch = origin/main`
  - so the base-branch fix took effect, but the reusable branch was still reused before the new stale-branch patch could be live-proven

### Throughput
- No new non-monitor merge landed this cycle.
- No new non-monitor merge landed in the previous cycle either.
- Per the runbook, that plus the repeated validation-drift family escalates the workspace to an incident.

### Current incident
1. **Reusable task branches can still preserve monitor-branch contamination even after simulator baseBranch is corrected.**
   - Symptom:
     - live `e274...` still carries `234` commits / `798` changed files vs `origin/main`
     - `acquire-worktree` reused the existing task branch/worktree instead of recreating it from the corrected base
   - Proof:
     - task row now says `baseBranch = origin/main`
     - active run `c163...` `acquire-worktree` output still says:
       - `reusedExistingBranch = true`
     - branch diff vs `origin/main` remains `798` files
   - Code path:
     - `workflow/workflow-nodes/actions.mjs`
     - reusable worktree sync / race-reuse path
   - Current state:
     - source fix is in place
     - final focused validation rerun is blocked by dependency drift before the live task can be restarted again under the newest reusable-branch patch

2. **Root dependency drift is now a repeated blocker family across validation attempts.**
   - Symptom:
     - `vitest` and `@babel/runtime` repeatedly disappear from the source repo between commands
   - Proof:
     - `npm install --include=dev` restored them more than once this cycle
     - latest final snapshot again shows:
       - `rootVitest = false`
       - `rootBabelRuntime = false`
   - Impact:
     - focused regression reruns fail before the newest reusable-branch fix can be fully proven
     - the same drift would also break the managed pre-push hook if the live task reaches push again

### Simulator gate / daemon state
- Simulator gate remains **historically proven end to end** from earlier merged real tasks:
  - PR `#485`
  - PR `#486`
- Daemon remained allowed because that gate had already been re-proven earlier.
- This cycle still stayed simulator-first because `e274...` remained the narrowest unresolved real blocker.

### Local source vs upstream
- After fetch:
  - behind `0`
  - ahead `231`
- Local source still reflects the latest merged `origin/main`.

### Narrowest next starting point
1. Re-read the newest three notes again.
2. Repair the root dependency drift just long enough to run the newest focused reusable-branch regression one more time.
3. If that regression passes, kill any stale simulator PID holding the slot, then restart `e274...` again.
4. Immediately confirm after restart:
   - `task.baseBranch = origin/main`
   - branch diff vs `origin/main` collapses far below `798`
   - `acquire-worktree` no longer returns `reusedExistingBranch = true` on the contaminated branch
5. Only then follow the real task through push, PR creation, review, and merge.

incident
