incident

# Bosun Local Ops Monitor — 2026-04-22 12:41 local

## Runtime State
- Monitor restarted onto current source this cycle.
- `bosun --daemon-status --config-dir .bosun --repo-root .` now reports monitor mode active with PID 56628.
- Repo-local runtime confirmed via `.bosun` paths from `bosun --where --config-dir .bosun --repo-root .`.
- Branch remains `bosun/codex-self-improvement-loop-commits` and is `0 behind / 190 ahead` of `origin/main`.
- Dirty working tree preserved intentionally; changes were not pushed.

## Active Sink Paths
- `.bosun/logs/monitor.log` advancing (`LastWriteTime 2026-04-22 12:34:55`).
- `.bosun/logs/monitor-error.log` advancing (`LastWriteTime 2026-04-22 12:34:54`).
- `.bosun/workflow-runs/_active-runs.json` advancing (`LastWriteTime 2026-04-22 12:34:55`).

## Throughput / Queue Health
- Current task stats after restart:
  - Draft 45
  - Todo 4
  - In Progress 2
  - In Review 0
  - Done 104
  - Blocked 33
- Throughput target still not met: done count is still flat at 104 and no non-monitor merge was proved this cycle.

## End-to-End Task Paths Inspected
- `8ed214ec-7eac-4887-8abd-97aa09c61694`
- `a6a0198e-a547-4eb7-872c-af2c83604b5f`

## Symptom -> Proof -> Code Path -> Fix
- Symptom: live tasks repeatedly hit `Acquire Worktree`, `worktree-ok=false`, `Recover Worktree`, `Retry Acquire WT`, then returned to `todo`.
- Proof before fix load:
  - fresh monitor-log tail showed both task IDs cycling through `Acquire Worktree -> worktree-ok result=false -> Retry Acquire WT -> inprogress -> todo` every minute.
  - orphaned managed worktree directories were previously proven on disk: surviving `.git` files pointed at missing `.git/worktrees/...` entries.
- Code path: `workflow/workflow-nodes/actions.mjs`, `action.acquire_worktree` / recovery path selection around managed worktree invalidation.
- Durable fix landed:
  - when a poisoned managed path survives reset, acquire retargets to a recovery-path worktree instead of retrying the same occupied path forever.
  - focused Windows regression added in `tests/workflow-task-lifecycle.test.mjs` for locked/orphaned managed worktree directories.

## Live Post-Restart Verification
- After restarting Bosun onto the patched source, both representative tasks changed behavior:
  - `Acquire Worktree` completed.
  - `worktree-ok` returned `true`.
  - `recover-worktree`, `retry-acquire-wt`, and all `WT Fail` / `Set Todo (WT Fail)` branches were skipped.
  - both tasks advanced into `Resolve Executor`, `Record HEAD`, `Build Prompt`, and `run-agent-plan`.
- Current human-readable task state:
  - `8ed214ec-7eac-4887-8abd-97aa09c61694` -> `inprogress`
  - `a6a0198e-a547-4eb7-872c-af2c83604b5f` -> `inprogress`
- Conclusion: the repeated poisoned-worktree loop is resolved in the live runtime.

## Recent Workflow Run Behavior
- Task Lifecycle now behaves correctly for the inspected worktree slice after restart.
- Residual live warning observed on startup: `execution ledger write failed: Cannot create a string longer than 0x1fffffe8 characters`.
- That ledger warning was not pursued this cycle because the narrow recurring incident was the worktree acquisition loop, which is now cleared.

## Additional Validation / Adjacent Fix
- `prepush:check` initially failed in `prepublish-check` because `lib/workflow-flowchart-utils.mjs` was imported by published files but missing from the `package.json` `files` array.
- Fixed by adding that file to `package.json`.

## Validation Performed
- Focused workflow validation:
  - `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/workflow-task-lifecycle.test.mjs -t "action.acquire_worktree"`
  - result: pass, including new Windows regression.
- Broader repo validation:
  - `npm test` -> completed cleanly (captured output ended with `pass 272`, `fail 0` in the Node test phase and no npm error markers).
  - `npm run build` -> pass.
  - `node tools/prepublish-check.mjs` -> pass after manifest update.
  - `npm run prepush:check` -> pass (`EXIT_CODE=0`).

## Git / Push / CI Status
- Local source changes validated but not pushed.
- No PR created this cycle.
- Local source reflects latest `origin/main` (`0 behind`).

## Recurring Blockers From Prior Sessions
- `poisoned managed worktree path survives reset` -> resolved in source and verified live after restart.
- `published local import missing from package files array` -> resolved locally and validated by `prepublish-check` / `prepush:check`.

## Narrowest Next Starting Point
- Continue from the two live `inprogress` task paths at `run-agent-plan`.
- If they block or fall back again, inspect the next downstream slice only:
  - `action.run_agent` / provider path for `plan-agent-ok`
  - or the execution-ledger size failure if it starts breaking run persistence again.
- Do not reopen worktree recovery unless `worktree-ok=false` reappears on the restarted monitor.