incident

# Bosun Local Ops Monitor — 2026-04-24 23:52:37 local

## Startup / scope
- Read the latest three monitor notes before touching runtime state:
  - `session-20260424-230708.md`
  - `session-20260424-215808.md`
  - `session-20260424-205542.md`
- Rebuilt the blocker list from `session-20260424-230708.md`.
- Stayed simulator-first and kept the daemon locked/off.
- Real task under inspection remained:
  - `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`

## Open-blocker list rebuilt this cycle
1. **Simulator gate still blocked** — no real non-monitor task has reached commit, PR creation, review/fix, merge, or local-source reflection.
2. **Nested delegated `openai-native` runtime is still broken** — the parent workflow now blocks correctly again, but deeper delegated child sessions still fail with Azure `DeploymentNotFound` even after inheriting the correct provider selection/config.
3. **Delegate-session cleanup still leaks** — after the parent workflow releases claim/slot, several nested delegate session files remain `status = active` with no terminal completion.
4. **Local broad test baseline still blocked** — full `npm test` remains unusable here because the existing `demo-defaults:sync` pretest hang is unchanged.

## Durable fixes landed this cycle
1. **Delegated provider/runtime inheritance bridge**
   - Files:
     - `agent/provider-kernel.mjs`
     - `agent/provider-session.mjs`
     - `shell/tool-executor.mjs`
     - `agent/tool-runtime-context.mjs`
     - `voice/voice-tools.mjs`
   - Changes:
     - provider sessions now retain resolved `providerSelection` / `providerConfig`
     - provider tool-call context now forwards `providerSelection`, `adapterName`, `model`, and `providerConfig`
     - tool runtime context normalization preserves `providerConfig`
     - `delegate_to_agent()` now forwards inherited native `providerSelection`, `providerId`, and `providerConfig` into delegated launches
   - Live effect:
     - delegate session metadata now persists the actual Azure selection and full provider config instead of dropping to plain `openai-native`

2. **Pinned workflow/tool delegate runtime**
   - File: `voice/voice-tools.mjs`
   - Change:
     - non-voice delegated live sessions now set `pinSdk: true` so workflow/tool delegation does not silently fail over across SDKs behind the workflow’s back
   - Why it mattered:
     - live nested delegates had been crossing into fallback runtimes and surfacing confusing downstream failures

3. **Workflow blocked-classifier hardening for active-delegate and read-only-analysis summaries**
   - Files:
     - `workflow/workflow-nodes/actions.mjs`
     - `tests/workflow-engine.test.mjs`
   - Added coverage for:
     - `background agent is still running`
     - `delegated agent is still running`
     - `Status: active / Latest output: none yet`
     - `session remains read-only for writes`
     - `cannot advance beyond analysis because the session remains read-only`
   - Why it mattered:
     - pre-fix live reruns were still leaking `run-agent-tests.success = true` or opening implementation off active/no-output delegates and read-only blocked summaries

## Validation completed this cycle
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\voice-tools.test.mjs -t "delegate_to_agent forwards native provider selection and config|delegate_to_agent prefers the managed parent session runtime|delegate_to_agent ignores stale explicit codex executor overrides"`
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\openai-native-adapter-tier1.test.mjs -t "dispatches through toolOrchestrator.execute with full session context"`
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\provider-kernel.test.mjs -t "passes resolved provider selection and config into tool execution context|preserves explicit harness executor settings for non-primary native selections"`
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\voice-tools.test.mjs -t "delegate_to_agent forwards native provider selection and config|delegate_to_agent skips voice-only approval defaults for workflow-agent contexts|delegate_to_agent prefers the managed parent session runtime"`
- `node .\tools\vitest-runner.mjs run --config vitest.config.mjs tests\workflow-engine.test.mjs -t "active delegated-agent summaries|read-only analysis blockers|tool-capability blocked summaries|read-only shell access blocker streams"`
- `bosun simulate task restart --json --config-dir .bosun --repo-root .`
- `npm run build`

## Live simulator evidence from this cycle

### 1. Provider inheritance fix landed live
- Rerun: `0dcdedd7-02a9-4539-8655-208239902240`
- First live proof:
  - `delegate-live-1777036816304-ad2b2d` family (same cycle lineage) now persisted:
    - `providerSelection = azure-openai-responses-1775278346327`
    - `providerId = azure-openai-responses`
    - full `providerConfig` including endpoint/apiVersion/model
- This replaced the earlier failure shape where delegated native sessions lost provider selection/config before launch.

### 2. Pre-classifier-hardening reruns still leaked success off blocked delegates
- Rerun: `356f93f5-2ef2-4f4f-9d82-3fd60157c358`
- Persisted proof before the last classifier patch:
  - `run-agent-tests.status = completed`
  - `run-agent-tests.success = true`
  - summary still said the background delegate was active / no output yet
  - `run-agent-implement.status = completed`
  - `run-agent-implement.success = true`
- Stream proof from the same run:
  - `The background agent is still running`
  - `Status: active`
  - `Latest output: none yet`
  - implementation session also self-reported read-only/write-blocked analysis-only status

### 3. Final rerun now blocks correctly again at tests phase
- Final rerun: `5f20bcb1-c46c-4c4e-8489-d0557a404ec1`
- Persisted proof:
  - `run-agent-tests.status = failed`
  - `run-agent-tests.success = false`
  - `run-agent-tests.blockedReason = blocked_by_env`
  - `tests-agent-ok.value = false`
  - `set-blocked-agent-tests-failed.success = true`
  - `run-agent-implement` not opened
  - `detect-commits` not opened
  - `release-claim.success = true`
  - `release-slot.success = true`
  - `.bosun\workflow-runs\_active-runs.json = []`
- Parent tests session file:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_5f20bcb1-c46c-4c4e-8489-d0557a404ec1_run-agent-tests_turn.json`
  - final `status = blocked_by_env`

### 4. The new live blocker is now fully below workflow classification again
- Latest nested delegate evidence after final rerun:
  - `delegate-live-1777038596796-1a5020`
    - `agent = openai-native`
    - `providerSelection = azure-openai-responses-1775278346327`
    - `status = failed`
    - terminal message: `OpenAI API error 404 ... DeploymentNotFound`
  - `delegate-live-1777038589217-2ee642`
    - same failure shape
  - `delegate-live-1777037864533-7ae6bb`
    - same failure shape
- Another completed delegate in the same family now reports the blocker explicitly:
  - `delegate-live-1777037694056-5211fa`
  - terminal message includes:
    - `AZURE_OPENAI_API_KEY missing`
    - Azure OpenAI `DeploymentNotFound` 404
    - no file changes
    - no tests/build
    - no commit
- This is materially narrower than the earlier workflow/runtime-selection failures: the workflow and classifier are now doing the right thing, but nested delegated native execution is still not reliably usable.

### 5. Delegate cleanup leak still persists
- Even after final workflow completion and claim/slot release, several nested delegate files remained active:
  - `delegate-live-1777038470707-ad2b2d`
  - `delegate-live-1777038573290-741b96`
  - `delegate-live-1777038481748-f271ad`
  - `delegate-live-1777038598458-23b67b`
- Re-check ~20s after workflow completion still showed:
  - `status = active`
  - some with only a child-session start message
  - one with `lastMessage = null`

## Runtime health
- Daemon remained **locked/off** this cycle.
- Because the simulator gate is still blocked, I did **not** unlock daemon monitoring or recheck daemon liveness as the main path.
- Workflow-level cleanup on the final rerun was correct:
  - `release-claim.success = true`
  - `release-slot.success = true`
  - `_active-runs.json = []`

## End-to-end task inspection
- Real task inspected end to end again:
  - task: `f82209df-e236-4a63-9ce8-67122a1ca96b`
  - title: `[m] fix(workspace): restore per-agent rate limiting for persistent memory writes`
  - PR: none
- Current terminal live state after final rerun `5f20...`:
  - tests now block correctly
  - implementation stays closed
  - no file changes verified
  - no tests run/passed by a write-capable agent
  - no commit created
  - no PR created
  - no merge

## Throughput
- Throughput target still **not met**.
- Recent merged PR throughput remains effectively zero for this monitor window:
  - `gh pr list --state merged --limit 5 --json ...`
  - latest merged PRs are still historical (`2026-04-20` and earlier), not new non-monitor completions from this cycle.

## Git / source state
- Branch remained:
  - `bosun/codex-self-improvement-loop-commits`
- Local branch still contains `origin/main`:
  - `git merge-base --is-ancestor origin/main HEAD` → true
- Current branch is ahead-only vs its tracked remote:
  - `ahead 3`
- Repository remains very dirty with unrelated user work; edits stayed surgical.

## Narrowest unresolved blocker now
**The current narrow blocker is no longer provider-selection loss or workflow success leakage. Nested delegated `openai-native` sessions now inherit the correct Azure provider selection/config, but they still fail deeper in execution with `DeploymentNotFound`, and their session records can remain active after the parent workflow has already blocked and released claim/slot.**

### Symptom
- Parent tests node blocks correctly and releases claim/slot.
- Nested delegated `openai-native` sessions still fail with Azure `DeploymentNotFound`.
- Some nested delegate session files remain `active` after parent workflow completion.

### Proof
- Workflow run:
  - `.bosun\workflow-runs\5f20bcb1-c46c-4c4e-8489-d0557a404ec1.json`
- Parent tests session:
  - `logs/sessions/f82209df-e236-4a63-9ce8-67122a1ca96b_agent_5f20bcb1-c46c-4c4e-8489-d0557a404ec1_run-agent-tests_turn.json`
  - `status = blocked_by_env`
- Failed nested delegates:
  - `logs/sessions/delegate-live-1777038596796-1a5020.json`
  - `logs/sessions/delegate-live-1777038589217-2ee642.json`
  - both show `OpenAI API error 404 ... DeploymentNotFound`
- Lingering active delegates:
  - `logs/sessions/delegate-live-1777038470707-ad2b2d.json`
  - `logs/sessions/delegate-live-1777038573290-741b96.json`
  - `logs/sessions/delegate-live-1777038481748-f271ad.json`
  - `logs/sessions/delegate-live-1777038598458-23b67b.json`

### Symptom -> proof -> code path -> fix or blocker
- **Fix landed**
  - Symptom:
    - delegated native sessions were dropping the parent provider selection/config before launch
  - Proof:
    - earlier live delegate files only had generic `openai-native` metadata and failed upstream of useful diagnosis
  - Code path fixed:
    - `agent/provider-kernel.mjs`
    - `agent/provider-session.mjs`
    - `shell/tool-executor.mjs`
    - `agent/tool-runtime-context.mjs`
    - `voice/voice-tools.mjs`
  - Live proof after fix:
    - final delegate files now persist `providerSelection`, `providerId`, and full `providerConfig`
- **Current blocker**
  - Symptom:
    - nested delegated native sessions still fail with `DeploymentNotFound`
    - some delegate files stay `active` after parent release
  - Proof:
    - latest delegate session files above
  - Suspect code paths:
    1. nested native delegate env/auth materialization
       - `voice/voice-tools.mjs`
       - `agent/provider-kernel.mjs`
       - `agent/provider-session.mjs`
       - `shell/openai-native-adapter.mjs`
    2. delegate-session terminal cleanup after blocked child failure
       - `voice/voice-tools.mjs`
       - `infra/session-tracker.mjs`
       - managed external-session finalization path in `agent/agent-launcher.mjs`

## Required next cycle
1. Start from the latest three notes again.
2. Stay simulator-first; do **not** unlock the daemon.
3. Resume from the latest reproduced run:
   - `bosun simulate task restart --json --config-dir .bosun --repo-root .`
4. Trace why nested delegated native sessions still surface:
   - `AZURE_OPENAI_API_KEY missing`
   - `DeploymentNotFound`
   even while metadata shows the correct inherited Azure selection/config.
5. Trace why nested delegate files remain `active` after the parent workflow has already blocked and released claim/slot.
6. Only after a real non-monitor task reaches implementation -> PR -> review/fix -> merge should daemon monitoring be re-enabled in parallel.

incident
