status: incident

# Bosun Local Ops Monitor — 2026-04-24 05:12:15

## Open blockers
- Non-monitor merged PR throughput remains `0` in the last hour.
- The previously interrupted real task families `3be828a2-d3e5-4225-b1f0-5d28905a865e` and `97d41516-caca-4fa0-9175-dbe6ad4ce01d` still have no PR and are not yet proven through validation / PR handoff after the latest restart.
- The newest daemon window is clean so far, but the latest source fix is not yet proven end to end on a real task path.

## What changed this cycle
- Landed a second surgical oversized-request fix in `shell/openai-native-adapter.mjs`.
  - The first fix only clamped oversized string history fields.
  - This cycle extends the guard to oversized **structured** payloads that were still being stringified later into OpenAI request items:
    - assistant tool-call arguments
    - `function_call.arguments`
    - `function_call_output.output`
  - Added a Responses API request-builder safety net to clamp oversized `input[].content[].text`, tool arguments, tool outputs, and `instructions` immediately before request dispatch.
- Added focused regression coverage in `tests/openai-native-adapter-tier1.test.mjs`.

## Symptom -> proof -> code path -> fix
- Symptom:
  - after the first OpenAI native history fix, real runs got farther but still hit fresh `Invalid string length` failures in later `write-tests` / `implement` agent passes
- Proof:
  - mixed live log evidence before the second patch showed:
    - some `write-tests` passes completing
    - then fresh `implement` / `write-tests` failures with `Invalid string length`
  - that ruled out the original planner-only frontier and pointed at later-round payload growth
  - code inspection showed the remaining gap:
    - `sanitizeHistoryEntryForRequest(...)` only truncated `function_call_output.output` when it was already a string
    - `historyEntryToResponsesInput(...)` later did unchecked `JSON.stringify(entry.output ?? "")` and `JSON.stringify(entry.arguments ?? {})`
- Code path:
  - `shell/openai-native-adapter.mjs`
    - `sanitizeHistoryEntryForRequest(...)`
    - `historyEntryToResponsesInput(...)`
    - `buildResponsesRequest(...)`
- Fix:
  - oversized structured tool arguments / outputs are now clamped before serialization at history-storage time
  - Responses API request assembly now re-clamps request text fields as a final safety net

## Validation
- Passed: `node tools/vitest-runner.mjs run --config vitest.config.mjs tests/openai-native-adapter-tier1.test.mjs`
- Passed: `npm run build`
- Passed direct structured-payload probe:
  - oversized structured assistant tool-call args and tool outputs sanitized to `8,000,000` chars

## Runtime re-check after restart
- Restarted with:
  - `node cli.mjs --terminate --config-dir .bosun --repo-root . --no-update-check`
  - `node cli.mjs --daemon --config-dir .bosun --repo-root . --no-update-check`
- Daemon after restart:
  - PID `11556`
- Raw newest daemon tail after restart showed:
  - startup recovery / normal workflow startup
  - no fresh `Invalid string length`
  - no fresh `string_above_max_length`
  - no fresh `node:FAILED run-agent-plan`
  - no fresh `node:FAILED write-tests`
  - no fresh `node:FAILED implement`
- Startup recovery did detect interrupted runs including:
  - `ce7cc34b-...`
  - `3d13ef22-...`
  - `93547f92-...`

## End-to-end task inspection
- Historical live path re-checked:
  - task `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
  - still `inprogress`
  - `prUrl = null`
  - `latestRunId = 93547f92-0a20-41a4-a30f-2e9a18e33832`
  - latest backend run file remained on its pre-restart timestamp in this cycle, so the restarted daemon has not yet proven fresh forward progress on this family
- Historical live path re-checked:
  - task `3be828a2-d3e5-4225-b1f0-5d28905a865e`
  - still `inprogress`
  - `prUrl = null`
  - `latestRunId = 3d13ef22-b5c0-4668-b2f3-3e4a38d87e5a`
  - run detail still shows the family at backend agent `write-tests` / `implement` frontier, but no fresh post-restart terminal outcome yet
- Fresh post-restart task path inspected:
  - task `aab1550f-1980-4c00-be2a-ae047b0d457c`
  - lifecycle run `17ef0ae8-3a30-4df4-a59a-ebcefa0e7c76`
  - task is still `todo`
  - lifecycle run is active after restart, proving new task intake is alive even though the older interrupted families are not yet proven through completion

## Throughput / health
- `bosun task stats --json --config-dir .bosun --repo-root .`
  - `draft=45`
  - `todo=11`
  - `inprogress=30`
  - `done=104`
  - `blocked=35`
- Last-hour merged non-monitor PR count: `0`

## Git / working tree
- Local code changes now include:
  - `shell/openai-native-adapter.mjs`
  - `tests/openai-native-adapter-tier1.test.mjs`
- Not pushed
- No PR

## Next start point
1. Continue from the two real interrupted families:
   - `3be828a2-d3e5-4225-b1f0-5d28905a865e`
   - `97d41516-caca-4fa0-9175-dbe6ad4ce01d`
2. Verify whether the second OpenAI native fix fully suppresses fresh oversized-request failures on real post-restart agent turns.
3. Prove at least one real task reaches:
   - validation
   - `push-branch`
   - `create-pr`
4. If fresh `Invalid string length` returns again, start from **fresh post-restart** evidence only and isolate the remaining field path.

incident
